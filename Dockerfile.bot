FROM node:20-alpine
WORKDIR /app
RUN npm install grammy
COPY scripts/telegram-bot.mjs .

HEALTHCHECK --interval=60s --timeout=5s --start-period=10s --retries=3 \
  CMD node -e "process.exit(0)" || exit 1

CMD ["node", "telegram-bot.mjs"]
