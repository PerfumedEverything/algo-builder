FROM node:20-alpine
WORKDIR /app

RUN apk add --no-cache libc6-compat

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY tsconfig.json ./
COPY scripts/price-stream-worker.ts ./scripts/

RUN npx tsx --version

CMD ["npx", "tsx", "scripts/price-stream-worker.ts"]
