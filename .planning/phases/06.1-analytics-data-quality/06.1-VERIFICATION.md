---
phase: 06.1-analytics-data-quality
verified: 2026-03-26T14:00:00Z
status: passed
score: 15/15 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Open portfolio analytics tab and verify all 3 metric cards (Concentration, Benchmark, Dividend Yield) render with real data"
    expected: "ConcentrationCard shows HHI value with green/yellow/red indicator; BenchmarkCard shows portfolio% vs IMOEX%; DividendYieldCard shows weighted yield%"
    why_human: "Requires live broker connection and MOEX API availability — not testable with static analysis"
  - test: "Switch correlation period dropdown to 30/60/90/180 days"
    expected: "Heatmap re-fetches and updates without full page reload"
    why_human: "Requires interactive browser session with live broker connection"
  - test: "Verify asset type chart colors match: STOCK=blue, ETF=purple, BOND=green, CURRENCY=amber, FUTURES=red"
    expected: "Each bar in the asset type chart has the correct color per instrument type"
    why_human: "Visual rendering requires browser session"
---

# Phase 6.1: Analytics Data Quality & Depth Verification Report

**Phase Goal:** Fix all analytics data correctness bugs and add deeper metrics (Herfindahl concentration, IMOEX benchmark comparison, aggregate dividend yield, instrument-level P&L, configurable correlation period) that make AculaTrade portfolio analytics deeper than T-Invest.
**Verified:** 2026-03-26T14:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Asset type chart renders correct colors for STOCK, ETF, BOND, CURRENCY, FUTURES | VERIFIED | `TYPE_COLORS` in `asset-type-chart.tsx` uses `stock`, `etf`, `bond`, `currency`, `futures` lowercase keys; `getColor` calls `type.toLowerCase()` — mapping is correct |
| 2 | Break-even strategies (pnl === 0) appear in trade success breakdown as separate category | VERIFIED | `getTradeSuccessBreakdown` has explicit `else { result.breakEven.count += 1 }` branch; `TradeSuccessChart` renders `breakEven` slice and stats block |
| 3 | Trade success uses Promise.all batch instead of sequential N+1 loop | VERIFIED | `const allStats = await this.operationService.getStatsForStrategies(strategyIds)` — single batch call, no per-strategy await in loop |
| 4 | getCorrelationMatrix accepts days parameter (30/60/90/180) | VERIFIED | Signature: `async getCorrelationMatrix(userId: string, days = 90)` — days used as `days * 24 * 60 * 60 * 1000` |
| 5 | Herfindahl index calculated from position weights with correct thresholds | VERIFIED | `getConcentrationIndex` computes `hhi += weight * weight`, thresholds: `< 0.15 = diversified`, `< 0.25 = moderate`, else `concentrated`; dominant flag at `weight > 0.4` |
| 6 | IMOEX benchmark return calculated for requested period | VERIFIED | `getBenchmarkComparison` fetches `moex.getImoexCandles(fromStr, tillStr)`, computes `((endPrice - startPrice) / startPrice) * 100` |
| 7 | Aggregate dividend yield is weighted average from MOEX dividends data | VERIFIED | `getAggregateDividendYield` calls `moex.getDividends(pos.ticker)` via Promise.all, filters last 12 months, computes `(totalDiv / pos.currentPrice) * 100` weighted by position weight |
| 8 | Instrument-level P&L aggregation groups strategies by ticker | VERIFIED | `instrumentMap` in `getTradeSuccessBreakdown` groups by `strategy.instrument`, populates `byInstrument: InstrumentPnl[]` sorted by totalPnl |
| 9 | User sees Herfindahl concentration card with green/yellow/red indicator | VERIFIED | `ConcentrationCard` wired in `portfolio/page.tsx` with `portfolioAnalytics?.concentration`, LEVEL_CONFIG maps diversified/moderate/concentrated to emerald/amber/red |
| 10 | User sees portfolio return vs IMOEX benchmark delta | VERIFIED | `BenchmarkCard` wired in `portfolio/page.tsx` with `portfolioAnalytics?.benchmarkComparison`, renders portfolioReturn%, benchmarkReturn%, delta% with color |
| 11 | User sees aggregate dividend yield as weighted average | VERIFIED | `DividendYieldCard` wired in `portfolio/page.tsx` with `portfolioAnalytics?.aggregateDividendYield` |
| 12 | User sees P&L table per individual instrument | VERIFIED | `InstrumentPnlTable` wired in `portfolio/page.tsx` with `portfolioAnalytics?.tradeSuccessBreakdown?.byInstrument` |
| 13 | User can select correlation period 30/60/90/180 days | VERIFIED | `CorrelationHeatmap` has `period` + `onPeriodChange` props; `handleCorrelationPeriodChange` in page re-fetches `getCorrelationMatrixAction(days)` |
| 14 | Break-even strategies shown in trade success chart | VERIFIED | `TradeSuccessChart` destructures `breakEven` from data, adds slice to `chartData` array, renders conditional stats block |
| 15 | Asset type chart renders correct colors | VERIFIED | `TYPE_COLORS` map uses lowercase keys matching `instrumentType.toLowerCase()` output |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/core/types/analytics.ts` | Extended analytics types | VERIFIED | Contains `ConcentrationIndex`, `BenchmarkComparison`, `AggregateDividendYield`, `InstrumentPnl`, updated `TradeSuccessBreakdown` with `breakEven` + `byInstrument`, updated `PortfolioAnalytics` |
| `src/server/services/portfolio-analytics-service.ts` | All new analytics methods | VERIFIED | Contains `getConcentrationIndex`, `getBenchmarkComparison`, `getAggregateDividendYield`, `getTradeSuccessBreakdown` (with batch + breakEven + byInstrument), `getCorrelationMatrix(userId, days)` |
| `src/components/portfolio/asset-type-chart.tsx` | Fixed color mapping | VERIFIED | `TYPE_COLORS` keys: `stock`, `etf`, `bond`, `currency`, `futures` — no `share` key |
| `src/server/actions/analytics-actions.ts` | Updated server actions | VERIFIED | `getCorrelationMatrixAction(days = 90)`, `getPortfolioAnalyticsAction` returns full `PortfolioAnalytics` including concentration/benchmark/dividendYield |
| `src/components/portfolio/concentration-card.tsx` | HHI concentration card | VERIFIED | Exports `ConcentrationCard`, renders level indicator with color-coded icon |
| `src/components/portfolio/benchmark-card.tsx` | IMOEX benchmark comparison | VERIFIED | Exports `BenchmarkCard`, shows portfolioReturn vs benchmarkReturn with delta |
| `src/components/portfolio/dividend-yield-card.tsx` | Aggregate dividend yield display | VERIFIED | Exports `DividendYieldCard`, renders `weightedYield` with color thresholds |
| `src/components/portfolio/instrument-pnl-table.tsx` | Instrument-level P&L table | VERIFIED | Exports `InstrumentPnlTable`, renders rows per ticker with strategyCount and P&L |
| `src/app/(dashboard)/portfolio/page.tsx` | Analytics tab with all new components | VERIFIED | Imports and renders `ConcentrationCard`, `BenchmarkCard`, `DividendYieldCard`, `InstrumentPnlTable`, updated `CorrelationHeatmap` with period selector |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `portfolio/page.tsx` | `analytics-actions.ts` | `getPortfolioAnalyticsAction` call in `fetchAnalytics` | WIRED | Page imports and calls action, stores result in `portfolioAnalytics` state |
| `portfolio/page.tsx` | `analytics-actions.ts` | `getCorrelationMatrixAction(correlationPeriod)` | WIRED | Called in `fetchAnalytics` and `handleCorrelationPeriodChange` |
| `correlation-heatmap.tsx` | period selector state | `onPeriodChange` callback | WIRED | Component receives `period` + `onPeriodChange` props; page passes `handleCorrelationPeriodChange` |
| `portfolio-analytics-service.ts` | `analytics.ts` | `import ConcentrationIndex` | WIRED | Line 9: `ConcentrationIndex, BenchmarkComparison, AggregateDividendYield, InstrumentPnl` imported |
| `getPortfolioAnalyticsAction` | `getConcentrationIndex` | `analyticsService.getConcentrationIndex(positions)` | WIRED | Called in action, result included in response |
| `InstrumentPnlTable` | `tradeSuccessBreakdown.byInstrument` | `data` prop at call site | WIRED | `data={portfolioAnalytics?.tradeSuccessBreakdown?.byInstrument ?? []}` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `concentration-card.tsx` | `data.hhi`, `data.level` | `analyticsService.getConcentrationIndex(positions)` — positions from `brokerService.getPortfolio` | Yes — computed from live portfolio positions | FLOWING |
| `benchmark-card.tsx` | `data.portfolioReturn`, `data.benchmarkReturn` | `getBenchmarkComparison` — calls `moex.getImoexCandles` and broker portfolio | Yes — live MOEX IMOEX candles + portfolio prices | FLOWING |
| `dividend-yield-card.tsx` | `data.weightedYield` | `getAggregateDividendYield` — calls `moex.getDividends(pos.ticker)` per position | Yes — live MOEX dividend data filtered to past 12 months | FLOWING |
| `instrument-pnl-table.tsx` | `data` (InstrumentPnl[]) | `getTradeSuccessBreakdown` → `getStatsForStrategies` batch | Yes — aggregated from real strategy operation stats via batch DB query | FLOWING |
| `trade-success-chart.tsx` | `breakEven.count` | `getTradeSuccessBreakdown` — `else { result.breakEven.count += 1 }` branch | Yes — counted from real strategy P&L stats | FLOWING |
| `correlation-heatmap.tsx` | `matrix`, `period` | `getCorrelationMatrixAction(days)` → `getCorrelationMatrix(userId, days)` | Yes — candles from broker, Pearson correlation via simple-statistics | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `ConcentrationIndex` type exported from barrel | `grep "from.*analytics" src/core/types/index.ts` | `export * from "./analytics"` — wildcard re-export covers all new types | PASS |
| Asset type chart has no "share" key | `grep -c "share" asset-type-chart.tsx` | Not found (only lowercase keys: stock, etf, bond, currency, futures) | PASS |
| Batch query replaces N+1 loop | `grep "getStatsForStrategies" portfolio-analytics-service.ts` | Found — single batch call before loop | PASS |
| breakEven in service | `grep "breakEven" portfolio-analytics-service.ts` | Found at line 119 (result init) and line 139 (increment) | PASS |
| days param in correlation action | `grep "days = 90" analytics-actions.ts` | Found at line 39 | PASS |
| TypeScript errors in production code | `npx tsc --noEmit` | Zero errors in production code (test file errors are pre-existing, unrelated to phase 6.1) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| AQFIX-01 | 06.1-01, 06.1-02 | Asset type chart correctly maps STOCK/ETF/BOND/CURRENCY/FUTURES to colors | SATISFIED | `TYPE_COLORS` uses correct lowercase keys matching `instrumentType.toLowerCase()` |
| AQFIX-02 | 06.1-01, 06.1-02 | Break-even strategies counted separately in trade success breakdown | SATISFIED | `else { result.breakEven.count += 1 }` in service; `TradeSuccessChart` renders breakEven slice |
| AQFIX-03 | 06.1-01 | Trade success breakdown uses batch query instead of N+1 | SATISFIED | `getStatsForStrategies(strategyIds)` batch call replaces per-strategy await loop |
| AQFIX-04 | 06.1-01, 06.1-02 | Portfolio shows Herfindahl concentration index with warning at >40% | SATISFIED | `getConcentrationIndex` computes HHI; `ConcentrationCard` renders with level indicator; dominant positions flagged at `weight > 0.4` |
| AQFIX-05 | 06.1-01, 06.1-02 | Portfolio shows return vs IMOEX benchmark for matching period | SATISFIED | `getBenchmarkComparison` fetches IMOEX candles; `BenchmarkCard` renders portfolioReturn/benchmarkReturn/delta |
| AQFIX-06 | 06.1-01, 06.1-02 | Portfolio shows aggregate dividend yield based on position weights | SATISFIED | `getAggregateDividendYield` uses `moex.getDividends` per position; `DividendYieldCard` renders weighted yield |
| AQFIX-07 | 06.1-01, 06.1-02 | Trade success shows P&L breakdown by individual instrument | SATISFIED | `byInstrument: InstrumentPnl[]` in `getTradeSuccessBreakdown`; `InstrumentPnlTable` renders per-ticker P&L |
| AQFIX-08 | 06.1-01, 06.1-02 | User can switch correlation period between 30/60/90/180 days | SATISFIED | `CorrelationHeatmap` has period dropdown; `handleCorrelationPeriodChange` re-fetches with new days param |

All 8 AQFIX requirements are SATISFIED.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | No TODOs, placeholders, empty implementations, or hardcoded stub data detected in phase 6.1 files | — | — |

### Human Verification Required

#### 1. Live Metric Cards Rendering

**Test:** Connect broker, open Portfolio > Аналитика tab, observe the 3 top cards
**Expected:** ConcentrationCard shows HHI% with green/yellow/red shield icon; BenchmarkCard shows portfolio% vs IMOEX% with delta; DividendYieldCard shows weighted yield%
**Why human:** Requires live T-Invest broker connection and MOEX ISS API availability

#### 2. Correlation Period Selector

**Test:** On analytics tab, use the period dropdown in the correlation heatmap to switch between 30/60/90/180 days
**Expected:** Heatmap updates with new correlation values without full page reload; loading spinner shows briefly
**Why human:** Requires interactive browser session and live broker data

#### 3. Asset Type Color Correctness

**Test:** With a portfolio containing STOCK, ETF, and BOND positions, open the analytics tab and inspect the asset type bar chart
**Expected:** Stocks = blue (#3b82f6), ETF = purple (#8b5cf6), Bonds = green (#10b981)
**Why human:** Visual rendering requires browser session

### Gaps Summary

No gaps found. All 8 AQFIX requirements have complete end-to-end implementation:

- **Service layer** (`portfolio-analytics-service.ts`): All 5 new methods implemented, all 3 bugs fixed
- **Types** (`analytics.ts`): All new types exported via barrel, `PortfolioAnalytics` expanded
- **Actions** (`analytics-actions.ts`): `getPortfolioAnalyticsAction` returns full analytics including concentration/benchmark/dividends; `getCorrelationMatrixAction` accepts configurable `days`
- **UI components**: 4 new components created (`ConcentrationCard`, `BenchmarkCard`, `DividendYieldCard`, `InstrumentPnlTable`), 2 updated (`TradeSuccessChart` with breakEven, `CorrelationHeatmap` with period selector)
- **Portfolio page**: All components wired into analytics tab with proper data flow, state management for correlation period, and lazy loading on tab click

The only TypeScript errors in the project are in pre-existing test files (`fifo-calculator.test.ts`, `operation-service.test.ts`) unrelated to this phase.

---

_Verified: 2026-03-26T14:00:00Z_
_Verifier: Claude (gsd-verifier)_
