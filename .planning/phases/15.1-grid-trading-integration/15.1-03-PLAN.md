---
phase: 15.1-grid-trading-integration
plan: 03
type: execute
wave: 3
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - src/__tests__/grid-integration.test.ts
autonomous: false
requirements:
  - GRID-01
  - GRID-02
  - GRID-03
  - GRID-04
  - GRID-05
  - GRID-06
  - GRID-07
  - GRID-08
  - GRID-09
  - GRID-10
must_haves:
  truths:
    - "All grid engine tests pass (25+ total)"
    - "All grid service tests pass"
    - "All grid bug regression tests pass"
    - "Grid strategies render on /strategies page without crash"
    - "Grid creation dialog opens and shows instrument select"
    - "Grid card shows correct P&L, range, levels"
    - "Stop Grid action works from strategy card"
    - "No double P&L on multi-fill (BUG-01 verified)"
    - "No TypeScript errors in entire project"
  artifacts:
    - path: "src/__tests__/grid-integration.test.ts"
      provides: "Integration tests verifying end-to-end grid flow"
      min_lines: 50
  key_links:
    - from: "src/__tests__/grid-integration.test.ts"
      to: "src/lib/grid-engine.ts"
      via: "Full cycle test: create grid -> process ticks -> verify P&L"
      pattern: "processTick|pnlDelta"
---

<objective>
Comprehensive audit of all grid code: run full test suite, add integration tests, verify all bugs are fixed, and get human approval.

Purpose: Zero tolerance for bugs in grid trading. This plan runs every test, adds an end-to-end integration test, and pauses for human verification of the UI.
Output: Clean audit result, 25+ passing tests, human-verified UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-grid-trading-integration/15.1-RESEARCH.md
@.planning/phases/15.1-grid-trading-integration/15.1-01-SUMMARY.md
@.planning/phases/15.1-grid-trading-integration/15.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write integration test + run full audit</name>
  <files>src/__tests__/grid-integration.test.ts</files>
  <read_first>
    - src/core/types/grid.ts (final GridLevel type with pnlDelta)
    - src/lib/grid-engine.ts (full fixed engine)
    - src/server/services/grid-trading-service.ts (full fixed service)
    - src/server/services/grid-ai-service.ts (stdDev removed)
    - src/server/repositories/grid-repository.ts (full repo)
    - src/components/strategy/grid-strategy-card.tsx (verify no config.entry access)
    - src/components/strategy/grid-strategy-dialog.tsx (verify instrument select flow)
    - src/app/(dashboard)/strategies/page.tsx (verify isGridConfig discrimination)
    - src/__tests__/grid-engine.test.ts (count tests)
    - src/__tests__/grid-trading-service.test.ts (count tests)
    - src/__tests__/grid-bugs-regression.test.ts (count tests)
    - src/__tests__/grid-comprehensive.test.ts (count tests)
    - src/__tests__/grid-ui.test.ts (count tests)
  </read_first>
  <action>
    **1. Create `src/__tests__/grid-integration.test.ts`** — end-to-end GridEngine integration test:

    Test "Full grid lifecycle: 50 ticks with multiple cycles":
    - Create grid: lower=100, upper=110, levels=6, amountPerOrder=100, fee=0.001, distribution=ARITHMETIC
    - Initialize state at currentPrice=105
    - Process 50 price ticks that oscillate between 98 and 112 (sinusoidal pattern: prices = Array.from({length: 50}, (_, i) => 105 + 7 * Math.sin(i * 0.4)))
    - For each tick, call processTick with all levels (including previously filled). After each tick, apply fills and counter orders to the levels array (simulate what service does).
    - After all ticks: verify totalPnl > 0 (grid should profit from oscillation), verify no NaN in any pnlDelta, verify all pnlDeltas sum to total pnlDelta.

    Test "Grid with SL trigger stops processing":
    - Create grid with stopLoss=97
    - Process ticks: 105, 103, 100, 96 (below SL)
    - On tick 4 (price 96), verify stopLoss would trigger (price <= config.stopLoss)

    Test "Grid with TP trigger stops processing":
    - Create grid with takeProfit=115
    - Process ticks: 105, 108, 112, 116 (above TP)
    - On tick 4 (price 116), verify takeProfit would trigger

    Test "Grid P&L matches manual calculation for known sequence":
    - 3-level grid: prices [100, 105, 110], amountPerOrder=1000, feeRate=0.001
    - Initialize at 106: level 0 (100, BUY), level 1 (105, BUY), level 2 (110, SELL)
    - Tick 1: price 104 -> BUY at level 1 fills (qty = 1000/105 = 9.5238)
    - Tick 2: price 111 -> SELL at level 2 fills. Paired with... (level 1 buy). P&L = 111 * (1000/110) * (1-0.001) - 104 * 9.5238 * (1+0.001) = manual calc
    - Assert pnlDelta matches expected value within 0.01 tolerance

    **2. Run ALL grid tests** and count:
    ```bash
    npx vitest run src/__tests__/grid-*.test.* --reporter=verbose
    ```
    Count total passing tests. Target: 25+.

    **3. Code audit checklist** (read each file, verify):
    - [ ] grid.ts: GridLevel has pnlDelta field
    - [ ] grid-engine.ts: calculateLevels validates inputs (throws on bad values)
    - [ ] grid-engine.ts: processTick sets filled.pnlDelta per SELL (not total)
    - [ ] grid-engine.ts: P&L uses pairedBuy.quantity for cost, level.quantity for revenue
    - [ ] grid-engine.ts: counter order uses level.quantity (filled buy's qty) for SELL counter
    - [ ] grid-trading-service.ts: passes filled.pnlDelta to fillOrder (not tickResult.pnlDelta)
    - [ ] grid-trading-service.ts: uses getOrdersByGridId (not getPendingOrders) to include FILLED buys
    - [ ] grid-trading-service.ts: checks SL/TP and calls stopGrid
    - [ ] grid-ai-service.ts: no unused stdDev computation
    - [ ] grid-strategy-card.tsx: no reference to config.entry or config.exit
    - [ ] strategies/page.tsx: isGridConfig check before rendering
    - [ ] strategies/page.tsx: Grid IDs excluded from getOperationStatsForStrategiesAction

    If ANY check fails, fix it before proceeding.

    **4. TypeScript full check**:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <acceptance_criteria>
    - File src/__tests__/grid-integration.test.ts exists with 4+ test cases
    - npx vitest run src/__tests__/grid-*.test.* exits 0 (all pass)
    - Total grid test count >= 25 (sum across all grid test files)
    - npx tsc --noEmit exits 0
    - grep "config.entry" src/components/strategy/grid-strategy-card.tsx returns no match
    - grep "filled.pnlDelta" src/server/services/grid-trading-service.ts returns match
    - grep "getOrdersByGridId" src/server/services/grid-trading-service.ts returns match
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/grid-*.test.* --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>All grid tests pass (25+), integration test covers full lifecycle, code audit confirms all bugs fixed, TypeScript clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Grid integration on strategies page</name>
  <files>none</files>
  <action>
    Present the completed Grid integration to the user for manual UI verification. All automated tests passed in Task 1. This checkpoint verifies the visual and interactive aspects that automated tests cannot cover.

    Steps for human to verify:
    1. Open http://localhost:3000/strategies
    2. If Grid strategies exist from Phase 15 testing, verify they appear with blue "Grid Bot" badge
    3. Verify Grid card shows: range (lower-upper), levels count, distribution, P&L, buy/sell counts
    4. Click "Novaya strategiya" -> verify dropdown with two options: "Индикаторная стратегия" and "Grid Bot"
    5. Click "Grid Bot" -> verify dialog opens with instrument search
    6. Search for an instrument (e.g., BTCUSDT) -> select it -> verify GridForm appears with price prefilled
    7. If a Grid strategy is ACTIVE, verify "Остановить" action in dropdown menu works
    8. Verify no crashes, no console errors, no "undefined" text on Grid cards
    9. Open /terminal -> verify Grid panel still works (not broken by integration)
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/grid-*.test.* --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms: Grid cards render correctly, creation dialog works, stop action works, no crashes or console errors, terminal Grid panel unbroken.</done>
</task>

</tasks>

<verification>
Full test suite:
```bash
npx vitest run src/__tests__/grid-*.test.* --reporter=verbose
```
TypeScript:
```bash
npx tsc --noEmit
```
Test count verification:
```bash
npx vitest run src/__tests__/grid-*.test.* --reporter=verbose 2>&1 | grep -c "PASS\|FAIL"
```
</verification>

<success_criteria>
- 25+ grid tests passing across all test files
- Integration test covers full lifecycle (50 ticks, P&L accuracy, SL/TP)
- Code audit confirms every bug fixed
- Human verified: Grid cards render, creation works, stop works, no crashes
- TypeScript compiles clean
- Zero bugs in grid code
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-grid-trading-integration/15.1-03-SUMMARY.md`
</output>
