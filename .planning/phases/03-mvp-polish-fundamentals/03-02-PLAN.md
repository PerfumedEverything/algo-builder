---
phase: 03-mvp-polish-fundamentals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/terminal/page.tsx
  - src/app/(dashboard)/strategies/page.tsx
  - src/app/(dashboard)/signals/page.tsx
  - src/server/actions/signal-actions.ts
autonomous: true
requirements: [POL-03, POL-04, POL-05, POL-06, POL-09]

must_haves:
  truths:
    - "Terminal has 'Create Strategy' and 'Create Signal' buttons when instrument is selected"
    - "Signal creation saves ticker without @ suffix"
    - "Terminal page works on mobile (timeframes wrap, chart full width)"
    - "Portfolio chart timeframes display correct data (1D = daily candles over weeks)"
  artifacts:
    - path: "src/app/(dashboard)/terminal/page.tsx"
      provides: "Create strategy/signal buttons, mobile layout"
      contains: "Создать стратегию"
    - path: "src/server/actions/signal-actions.ts"
      provides: "cleanTicker applied to signal instrument"
      contains: "cleanTicker"
    - path: "src/app/(dashboard)/strategies/page.tsx"
      provides: "Auto-open dialog from createFor query param"
      contains: "createFor"
  key_links:
    - from: "src/app/(dashboard)/terminal/page.tsx"
      to: "src/app/(dashboard)/strategies/page.tsx"
      via: "router.push with createFor query param"
      pattern: "createFor"
    - from: "src/server/actions/signal-actions.ts"
      to: "src/lib/ticker-utils.ts"
      via: "cleanTicker import"
      pattern: "cleanTicker"
---

<objective>
Add terminal-to-strategy/signal creation flow, fix signal @ suffix bug, and polish terminal mobile layout.

Purpose: Users can seamlessly go from terminal analysis to strategy/signal creation; signals save clean tickers; terminal works on all screens.
Output: Terminal action buttons, signal cleanTicker fix, mobile-responsive terminal.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mvp-polish-fundamentals/03-RESEARCH.md

@src/app/(dashboard)/terminal/page.tsx
@src/app/(dashboard)/strategies/page.tsx
@src/server/actions/signal-actions.ts
@src/lib/ticker-utils.ts
</context>

<interfaces>
From src/app/(dashboard)/terminal/page.tsx:
```typescript
// State: instrument is BrokerInstrument | null
// PERIOD_CONFIG already has "1d": { days: 365, interval: "1d" } — POL-05 already fixed in terminal
```

From src/lib/ticker-utils.ts:
```typescript
export const cleanTicker = (ticker: string): string => {
  return ticker.replace(/@$/, "")
}
```

From src/server/actions/signal-actions.ts:
```typescript
export const createSignalAction = async (
  // accepts instrument: string among other fields
)
```

Sidebar (already correct per research):
```typescript
// mainNav[1] = { href: "/terminal", label: "Терминал" }
// POL-04 is already satisfied — Terminal is 2nd position
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Terminal action buttons + strategies/signals page createFor param + POL-05 verify</name>
  <files>src/app/(dashboard)/terminal/page.tsx, src/app/(dashboard)/strategies/page.tsx, src/app/(dashboard)/signals/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/terminal/page.tsx
    - src/app/(dashboard)/strategies/page.tsx
    - src/app/(dashboard)/signals/page.tsx
    - src/components/layout/sidebar.tsx (verify POL-04 already done)
    - src/components/broker/portfolio-view.tsx (verify POL-05 chart config)
  </read_first>
  <action>
    Per D-POL-09: Add "Создать стратегию" and "Создать сигнал" buttons in terminal.

    1. **Terminal page** (`terminal/page.tsx`):
       - Import `useRouter` from `next/navigation`, `Plus` and `Bell` icons from `lucide-react`, `Button` from `@/components/ui/button`
       - When `instrument` is not null, render two buttons in the toolbar area (the `flex items-center gap-4 flex-wrap` div, after ChartPeriodSelector):
         ```tsx
         <Button size="sm" variant="outline" onClick={() => router.push(`/strategies?createFor=${instrument.ticker}`)}>
           <Plus className="h-3.5 w-3.5 mr-1.5" />
           Создать стратегию
         </Button>
         <Button size="sm" variant="outline" onClick={() => router.push(`/signals?createFor=${instrument.ticker}`)}>
           <Bell className="h-3.5 w-3.5 mr-1.5" />
           Создать сигнал
         </Button>
         ```

    2. **Strategies page** (`strategies/page.tsx`):
       - Import `useSearchParams` from `next/navigation`
       - Read `searchParams.get("createFor")` in a `useEffect`
       - If `createFor` is present: call `setDialogOpen(true)` to open the strategy creation dialog
       - Clear the URL param after opening: `window.history.replaceState({}, "", "/strategies")`

    3. **Signals page** (`signals/page.tsx`):
       - Same pattern: read `createFor` param, auto-open signal creation dialog
       - Clear URL param after opening

    4. **POL-04 verification**: Sidebar already has Terminal at index 1 (position 2). This is a no-op. Add a comment in SUMMARY noting POL-04 was already satisfied.

    5. **POL-05 verification**: Read `portfolio-view.tsx` chart config. If the chart period configuration is incorrect (e.g., "1d" shows only 1 day of intraday data instead of daily candles over weeks), fix it to match the terminal's correct PERIOD_CONFIG. If already correct, note in SUMMARY that POL-05 is satisfied in portfolio-view.tsx as well.

    6. **POL-06 mobile**: In terminal/page.tsx, ensure the toolbar `div` at line ~185 uses `flex-wrap gap-2` and buttons have `size="sm"`. The chart container should have `w-full` without fixed min-width. The top area `flex items-center gap-4 flex-wrap` already supports wrapping. Ensure the two new buttons also wrap properly by keeping them inside the same flex container.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - terminal/page.tsx contains string "Создать стратегию"
    - terminal/page.tsx contains string "Создать сигнал"
    - terminal/page.tsx contains `router.push` with `createFor`
    - strategies/page.tsx contains `createFor`
    - strategies/page.tsx contains `setDialogOpen(true)` triggered by createFor param
    - signals/page.tsx contains `createFor`
    - sidebar.tsx line 33 still has `/terminal` at index 1 (POL-04 verified)
    - portfolio-view.tsx chart config verified or fixed (POL-05)
  </acceptance_criteria>
  <done>Terminal shows create buttons when instrument selected. Clicking routes to strategies/signals page and auto-opens dialog. POL-05 chart config verified in portfolio-view.</done>
</task>

<task type="auto">
  <name>Task 2: Signal cleanTicker fix (POL-03)</name>
  <files>src/server/actions/signal-actions.ts</files>
  <read_first>
    - src/server/actions/signal-actions.ts
    - src/lib/ticker-utils.ts
    - src/server/services/signal-service.ts
  </read_first>
  <action>
    Per D-POL-03: Apply `cleanTicker` to signal instrument on save.

    In `signal-actions.ts`, the `createSignalAction` function (line ~36):
    - Import `cleanTicker` from `@/lib/ticker-utils`
    - Before passing data to SignalService, apply: `const instrument = cleanTicker(data.instrument)`
    - Pass cleaned instrument to the service call

    Same fix in `updateSignalAction` (line ~64):
    - Apply cleanTicker to the instrument field if it exists in the update data

    Pattern reference: `strategy-checker.ts` already does `cleanTicker(strategy.instrument)` at method entry.
  </action>
  <verify>
    <automated>grep -c "cleanTicker" src/server/actions/signal-actions.ts && node -e "const s='SBER@'; console.log(s.replace(/@$/, '') === 'SBER' ? 'PASS: cleanTicker pattern works' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - signal-actions.ts contains `import.*cleanTicker.*from "@/lib/ticker-utils"`
    - signal-actions.ts `createSignalAction` calls `cleanTicker` on the instrument field
    - signal-actions.ts `updateSignalAction` calls `cleanTicker` on the instrument field
    - grep count of "cleanTicker" in signal-actions.ts is >= 3 (1 import + 2 usages)
  </acceptance_criteria>
  <done>Signal creation and update always save clean tickers without @ suffix.</done>
</task>

</tasks>

<verification>
- Terminal page shows "Создать стратегию" and "Создать сигнал" when instrument selected
- Clicking buttons navigates to strategies/signals page with dialog open
- Signal save strips @ suffix from instrument
- Portfolio-view chart config verified for POL-05
- `npx vitest run --reporter=verbose` passes
</verification>

<success_criteria>
1. Terminal has action buttons linking to strategy/signal creation
2. Strategies and signals pages auto-open dialog from createFor param
3. Signal instrument always saved without @ suffix
4. Terminal layout works on mobile screens
5. Portfolio chart timeframes verified correct (POL-05)
</success_criteria>

<output>
After completion, create `.planning/phases/03-mvp-polish-fundamentals/03-02-SUMMARY.md`
</output>
