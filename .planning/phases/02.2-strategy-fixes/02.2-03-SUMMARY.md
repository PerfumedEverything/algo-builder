---
phase: 02.2-strategy-fixes
plan: "03"
subsystem: signal-checker, signal-repository, signal-trigger-handler
tags: [bug-fix, crossing-detection, null-safety, redis-locks, candle-retry, tests, tdd]
dependency_graph:
  requires:
    - "02.2-01: IndicatorCalculator null returns, PriceCache.acquireLock/releaseLock, DB migrations"
    - "02.2-02: crossing-detector.ts with evaluateCrossing, getIndicatorKey, evaluateConditions, getIndicatorValue"
  provides:
    - "SignalRow.lastIndicatorValues field in TypeScript type"
    - "Signal checker with real CROSSES detection via crossing-detector"
    - "Signal checker null-safety: null indicators skip conditions"
    - "Signal checker Redis dedup lock on checkSignal/checkByInstrument"
    - "Signal checker candle fetch retry"
    - "CheckResult.error flag for system errors"
    - "lastIndicatorValues persisted to DB after each check"
    - "SignalTriggerHandler extracted class (mirrors StrategyTriggerHandler)"
  affects:
    - signal-checker.ts
    - signal-repository.ts
    - signal-trigger-handler.ts (new)
tech_stack:
  added: []
  patterns:
    - "Delegate to crossing-detector.ts for all indicator evaluation (DRY)"
    - "Lazy getters for db/priceCache/triggerHandler (test isolation)"
    - "extractHandler pattern: SignalTriggerHandler (parallel to StrategyTriggerHandler)"
    - "try/finally for Redis lock release"
    - "catch/delay/retry pattern for broker.getCandles"
key_files:
  created:
    - src/server/services/signal-trigger-handler.ts
    - src/__tests__/signal-checker.test.ts (rewritten with comprehensive cases)
  modified:
    - src/server/repositories/signal-repository.ts
    - src/server/services/signal-checker.ts
    - vitest.config.ts
decisions:
  - "SignalChecker delegates all evaluation to crossing-detector.ts — DRY, single source of truth for both pipelines"
  - "SignalTriggerHandler extracted to stay under 150 lines — same pattern as StrategyTriggerHandler"
  - "vitest.config.ts exclude: .claude/worktrees/** — prevents stale test files from failing CI"
  - "Lazy getters instead of constructor init for db/priceCache — allows unit tests without Supabase env"
metrics:
  duration: ~6 min
  completed_date: "2026-03-25"
  tasks_completed: 2
  files_changed: 5
---

# Phase 02.2 Plan 03: Signal Checker Fixes Summary

Signal checker fixed with real CROSSES crossing detection via shared crossing-detector.ts, null-safety for all indicators, Redis dedup lock on both checkSignal and checkByInstrument, candle fetch retry, CheckResult error flag, and lastIndicatorValues persistence to DB. signal-checker.ts reduced from 403 to 138 lines via delegation to crossing-detector.ts and extraction of SignalTriggerHandler.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add lastIndicatorValues to SignalRow type | e9d625c | signal-repository.ts |
| 2 | Fix signal-checker.ts + tests (TDD) | 68a0bd9 | signal-checker.ts, signal-trigger-handler.ts, signal-checker.test.ts, vitest.config.ts |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Config] Added worktrees exclude to vitest.config.ts**
- **Found during:** Task 2 test run
- **Issue:** `.claude/worktrees/` contains stale test files from previous agents with OLD test expectations (CROSSES_ABOVE simple comparison). Vitest was discovering them and reporting false failures.
- **Fix:** Added `exclude: [".claude/worktrees/**", "node_modules/**"]` to vitest.config.ts
- **Files modified:** vitest.config.ts
- **Commit:** 68a0bd9

**2. [Rule 2 - Refactoring] Extracted SignalTriggerHandler class**
- **Found during:** Task 2 implementation
- **Issue:** signal-checker.ts would exceed 150-line limit with handleTriggered inline. Plan spec said under 150 lines.
- **Fix:** Extracted `SignalTriggerHandler` to `signal-trigger-handler.ts` following same pattern as `StrategyTriggerHandler` from plan 02. signal-checker.ts is now 138 lines.
- **Files modified:** signal-trigger-handler.ts (created), signal-checker.ts
- **Commit:** 68a0bd9

**3. [Rule 1 - Refactoring] Delegated getIndicatorValue to crossing-detector.ts**
- **Found during:** Task 2 implementation
- **Issue:** signal-checker.ts had its own duplicate `getIndicatorValue` implementation (same code as crossing-detector.ts). DRY violation.
- **Fix:** Removed duplicate implementation, imported `getIndicatorValue` from crossing-detector.ts.
- **Files modified:** signal-checker.ts
- **Commit:** 68a0bd9

## Known Stubs

None — all signal checker logic is fully implemented.

## Self-Check: PASSED

- signal-checker.ts: FOUND
- signal-trigger-handler.ts: FOUND
- signal-checker.test.ts: FOUND
- Commit e9d625c: FOUND
- Commit 68a0bd9: FOUND
