---
phase: 02.2-strategy-fixes
verified: 2026-03-25T11:05:00Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 2.2: Strategy Pipeline Fix — Verification Report

**Phase Goal:** All 16 bugs in strategy/signal checker pipeline are fixed so automated strategies work reliably with any condition type
**Verified:** 2026-03-25T11:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CROSSES_ABOVE/CROSSES_BELOW triggers only on actual value crossing (not simple comparison) | VERIFIED | `evaluateCrossing` in `crossing-detector.ts` line 26-27: `prev < target && current >= target` / `prev > target && current <= target`; 70/70 tests pass |
| 2 | All indicators return null (not 0) when insufficient candle data — callers skip evaluation | VERIFIED | `indicator-calculator.ts` has guards on all 5 methods; `getIndicatorValue` in crossing-detector returns null from all branches including SUPPORT/RESISTANCE/default |
| 3 | Race conditions in signal triggerCount and strategy positionState are eliminated via atomic operations | VERIFIED | `strategy-trigger-handler.ts` line 41: `.eq("positionState", "NONE")` — no `.or()` pattern; no `?? Infinity` or `.or("positionState` found in any file |
| 4 | Operation recording failures roll back state changes | VERIFIED | `strategy-trigger-handler.ts` lines 56-59: catch block rolls back positionState to `expectedState` with logged error |
| 5 | Redis-based dedup lock prevents concurrent cron+realtime double-checks | VERIFIED | `strategy-checker.ts` lines 55, 70: `acquireLock("strategy"...)`; `signal-checker.ts` lines 47, 60: `acquireLock("signal"...)`; `price-cache.ts` implements both methods |
| 6 | Unit tests cover every fixed bug scenario | VERIFIED | 70 tests pass across 3 test files; indicator-calculator.test.ts (137 lines), strategy-checker-conditions.test.ts (228 lines), signal-checker.test.ts (132 lines) |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/server/services/indicator-calculator.ts` | Null-returning indicator methods | VERIFIED | All 5 methods return `number \| null` or compound `\| null`; 158 lines (within limit) |
| `src/__tests__/indicator-calculator.test.ts` | Unit tests for all indicator null-safety | VERIFIED | 137 lines, 16 test cases, all pass |
| `src/server/services/price-cache.ts` | Redis lock methods + 5m TTL=600 | VERIFIED | `acquireLock` and `releaseLock` present at lines 77-86; `"5m": 600` at line 10 |
| `prisma/migrations/20260325_add_last_indicator_values.sql` | lastIndicatorValues JSONB column | VERIFIED | Adds `lastIndicatorValues JSONB DEFAULT '{}'` to both Strategy and Signal tables |
| `prisma/migrations/20260325_fix_position_state_default.sql` | positionState DEFAULT + backfill | VERIFIED | `SET DEFAULT 'NONE'` + UPDATE backfill present |
| `src/server/services/crossing-detector.ts` | evaluateCrossing and getIndicatorKey logic | VERIFIED | 99 lines; exports `evaluateCrossing`, `getIndicatorKey`, `getIndicatorValue`, `evaluateCondition`, `evaluateConditions`, `compareCondition` |
| `src/server/services/strategy-checker.ts` | Fixed strategy checker with all bugs addressed | VERIFIED | 149 lines (at limit); imports crossing-detector; `error?: boolean` present; lock used; lastIndicatorValues persisted |
| `src/server/repositories/strategy-repository.ts` | lastIndicatorValues in StrategyRow type | VERIFIED | Line 17: `lastIndicatorValues: Record<string, number> \| null` |
| `src/__tests__/strategy-checker-conditions.test.ts` | CROSSES + null handling + error flag tests | VERIFIED | 228 lines, 34 test cases, all pass |
| `src/server/services/signal-checker.ts` | Fixed signal checker with all bugs addressed | VERIFIED | 138 lines; imports crossing-detector; `error?: boolean` present; lock used; lastIndicatorValues persisted |
| `src/server/repositories/signal-repository.ts` | lastIndicatorValues in SignalRow type | VERIFIED | Line 20: `lastIndicatorValues: Record<string, number> \| null` |
| `src/__tests__/signal-checker.test.ts` | Comprehensive signal checker tests | VERIFIED | 132 lines, 20 test cases, all pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `indicator-calculator.ts` | `crossing-detector.ts` (consumers) | return type `number \| null` | VERIFIED | All 5 methods return nullable; callers in crossing-detector handle null at lines 38-68 |
| `price-cache.ts` | `strategy-checker.ts` | acquireLock before processing | VERIFIED | Lines 55, 70 in strategy-checker.ts call `acquireLock("strategy", ...)` |
| `price-cache.ts` | `signal-checker.ts` | acquireLock before processing | VERIFIED | Lines 47, 60 in signal-checker.ts call `acquireLock("signal", ...)` |
| `crossing-detector.ts` | `strategy-checker.ts` | imports evaluateCrossing, getIndicatorKey | VERIFIED | Line 8: `import { evaluateConditions, getIndicatorValue, getIndicatorKey, type EvalContext } from "./crossing-detector"` |
| `crossing-detector.ts` | `signal-checker.ts` | imports evaluateCrossing, getIndicatorKey | VERIFIED | Lines 7-9: `import { getIndicatorValue, evaluateCondition as coreEval, evaluateConditions, getIndicatorKey, type EvalContext } from "./crossing-detector"` |
| `strategy-checker.ts` | DB Strategy table | persists lastIndicatorValues | VERIFIED | Line 120: `this.db.from("Strategy").update({ lastIndicatorValues: updatedValues, ... })` |
| `signal-checker.ts` | DB Signal table | persists lastIndicatorValues | VERIFIED | `persistIndicatorValues` method at lines 91-98 |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase fixes backend pipeline logic only. No UI components render data from these files.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All indicator null-safety tests pass | `npx vitest run src/__tests__/indicator-calculator.test.ts` | 16/16 pass | PASS |
| CROSSES detection tests pass | `npx vitest run src/__tests__/strategy-checker-conditions.test.ts` | 34/34 pass | PASS |
| Signal checker tests pass | `npx vitest run src/__tests__/signal-checker.test.ts` | 20/20 pass | PASS |
| TypeScript errors in production files | `npx tsc --noEmit` (non-test files) | 0 errors | PASS |

---

### Requirements Coverage

The STFIX-01 through STFIX-10 requirement IDs appear in PLAN frontmatter but are **absent from REQUIREMENTS.md**. Phase 2.2 was inserted as a critical hotfix after REQUIREMENTS.md was last updated (2026-03-23). The STFIX IDs are self-contained within the phase planning documents rather than the v1 milestone requirements registry.

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| STFIX-01 | 02.2-01, 02.2-02 | CROSSES detection (migration + implementation) | SATISFIED | crossing-detector.ts + DB migration |
| STFIX-02 | 02.2-01 | IndicatorCalculator null-safety | SATISFIED | indicator-calculator.ts + tests |
| STFIX-03 | 02.2-01 | Unknown indicator returns null | SATISFIED | crossing-detector.ts default case line 67 |
| STFIX-04 | 02.2-01, 02.2-02 | positionState DEFAULT migration + atomic guard | SATISFIED | SQL migration + trigger handler `.eq("positionState", "NONE")` |
| STFIX-05 | 02.2-03 | Signal checker CROSSES + null-safety | SATISFIED | signal-checker.ts + signal-checker.test.ts |
| STFIX-06 | 02.2-02 | positionState atomic guard in strategy checker | SATISFIED | strategy-trigger-handler.ts line 41 |
| STFIX-07 | 02.2-02 | Operation rollback on failure | SATISFIED | strategy-trigger-handler.ts lines 56-59 |
| STFIX-08 | 02.2-02 | CheckResult error flag | SATISFIED | `error?: boolean` in both checker files |
| STFIX-09 | 02.2-02 | Candle fetch retry | SATISFIED | try/catch with 1s delay + retry in both checkers |
| STFIX-10 | 02.2-03 | Signal checker Redis lock | SATISFIED | acquireLock/releaseLock in signal-checker.ts |
| STFIX-01-migration | 02.2-01 | DB migration for lastIndicatorValues | SATISFIED | 20260325_add_last_indicator_values.sql |
| STFIX-04-migration | 02.2-01 | DB migration for positionState default | SATISFIED | 20260325_fix_position_state_default.sql |

**REQUIREMENTS.md coverage note:** STFIX-01 through STFIX-10 are not registered in `.planning/REQUIREMENTS.md`. This is expected — the file covers v1 milestone requirements defined before the phase was inserted. No STFIX IDs appear in the tracking table, and no orphaned IDs exist (REQUIREMENTS.md does not reference these IDs at all). The phase is a hotfix insertion; its requirements live in PLAN frontmatter only.

---

### Anti-Patterns Found

| File | Pattern Checked | Result |
|------|----------------|--------|
| `strategy-checker.ts` | `.or("positionState` | NONE FOUND |
| `strategy-checker.ts` | `?? Infinity` | NONE FOUND |
| `strategy-checker.ts` | `return 0` in getIndicatorValue | NONE FOUND (logic extracted to crossing-detector) |
| `signal-checker.ts` | `?? Infinity` | NONE FOUND |
| `signal-checker.ts` | `return 0` in getIndicatorValue | NONE FOUND |
| `crossing-detector.ts` | VOLUME `return 0` | INTENTIONAL — volume 0 is valid, guarded by `avgVol > 0` check |
| `crossing-detector.ts` | `c.value ?? 0` | INTENTIONAL — condition threshold default |
| All phase files | `TODO/FIXME/placeholder` | NONE FOUND |

No blockers detected.

---

### Human Verification Required

#### 1. Production dedup under concurrent load

**Test:** Deploy to VPS, activate a strategy with PRICE GREATER_THAN condition. Trigger it simultaneously from cron worker and a realtime price update for the same instrument within the same 5-second window.
**Expected:** Strategy fires exactly once — single Telegram notification, single operation recorded, positionState updated once.
**Why human:** Redis lock dedup requires running worker and cron simultaneously in production; cannot be verified programmatically without infrastructure.

---

### Gaps Summary

No gaps found. All 6 observable truths verified, all 12 required artifacts exist and are substantive, all 7 key links confirmed wired. 70 unit tests pass. TypeScript compiles cleanly in production files.

The only notable finding is that STFIX-01 through STFIX-10 are not present in REQUIREMENTS.md — this is a known documentation gap for an inserted hotfix phase, not a code gap.

---

_Verified: 2026-03-25T11:05:00Z_
_Verifier: Claude (gsd-verifier)_
