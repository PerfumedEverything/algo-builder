---
phase: 14.2-moex-evening-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/market-hours.ts
  - src/__tests__/terminal/market-hours.test.ts
  - src/app/(dashboard)/terminal/page.tsx
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-5
must_haves:
  truths:
    - "isMarketOpen() returns true at 19:00 MSK (evening session)"
    - "isMarketOpen() returns true at 23:49 MSK (end of evening session)"
    - "isMarketOpen() returns false at 23:51 MSK (after evening session)"
    - "isMarketOpen() returns false on weekends"
    - "Daily stats refresh every 60s while instrument selected and market open"
    - "Stats interval cleared on instrument change or unmount"
  artifacts:
    - path: "src/lib/market-hours.ts"
      provides: "isMarketOpen with evening session support"
      contains: "inEvening"
    - path: "src/__tests__/terminal/market-hours.test.ts"
      provides: "Unit tests for isMarketOpen evening session"
      contains: "evening session"
    - path: "src/app/(dashboard)/terminal/page.tsx"
      provides: "Periodic stats refresh via setInterval"
      contains: "setInterval"
  key_links:
    - from: "src/app/(dashboard)/terminal/page.tsx"
      to: "src/lib/market-hours.ts"
      via: "import { isMarketOpen }"
      pattern: "isMarketOpen\\(\\)"
---

<objective>
Fix MOEX evening session support: extend isMarketOpen() to cover 18:40-23:50 MSK, add periodic stats refresh in terminal so H/L/Vol updates during evening session.

Purpose: Terminal currently shows stale stats after 18:50 MSK because isMarketOpen() returns false and stats are not refreshed. This causes price bar H/L/Vol to miss evening session data.
Output: Working isMarketOpen() with evening session, periodic stats refresh in terminal page, unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.2-moex-evening-session/14.2-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix isMarketOpen() to include evening session + unit tests</name>
  <files>src/lib/market-hours.ts, src/__tests__/terminal/market-hours.test.ts</files>
  <read_first>
    - src/lib/market-hours.ts
    - src/__tests__/terminal/terminal-utils.test.ts
  </read_first>
  <behavior>
    - isMarketOpen(Monday 10:00 MSK) returns true (main session)
    - isMarketOpen(Monday 18:39 MSK) returns true (end of main session)
    - isMarketOpen(Monday 18:40 MSK) returns true (start of evening session)
    - isMarketOpen(Monday 19:00 MSK) returns true (middle of evening session)
    - isMarketOpen(Monday 23:49 MSK) returns true (end of evening session)
    - isMarketOpen(Monday 23:50 MSK) returns false (after evening session)
    - isMarketOpen(Monday 09:49 MSK) returns false (before pre-open)
    - isMarketOpen(Monday 09:50 MSK) returns true (pre-open)
    - isMarketOpen(Saturday 12:00 MSK) returns false (weekend)
    - isMarketOpen(Sunday 19:00 MSK) returns false (weekend)
  </behavior>
  <action>
1. Create test file `src/__tests__/terminal/market-hours.test.ts` with tests for all behaviors above. Use UTC dates in test fixtures:
   - 10:00 MSK = 07:00 UTC → `new Date('2026-03-30T07:00:00Z')` (Monday)
   - 18:39 MSK = 15:39 UTC → `new Date('2026-03-30T15:39:00Z')`
   - 18:40 MSK = 15:40 UTC → `new Date('2026-03-30T15:40:00Z')`
   - 19:00 MSK = 16:00 UTC → `new Date('2026-03-30T16:00:00Z')`
   - 23:49 MSK = 20:49 UTC → `new Date('2026-03-30T20:49:00Z')`
   - 23:50 MSK = 20:50 UTC → `new Date('2026-03-30T20:50:00Z')`
   - 09:49 MSK = 06:49 UTC → `new Date('2026-03-30T06:49:00Z')`
   - 09:50 MSK = 06:50 UTC → `new Date('2026-03-30T06:50:00Z')`
   - Saturday 12:00 MSK = 09:00 UTC → `new Date('2026-03-28T09:00:00Z')` (Saturday)
   - Sunday 19:00 MSK = 16:00 UTC → `new Date('2026-03-29T16:00:00Z')` (Sunday)

2. Run tests — they MUST fail (RED phase) because current code returns false for evening session times.

3. Update `src/lib/market-hours.ts`:
   - Replace line `return mskMinutes >= 590 && mskMinutes < 1130` with:
   ```
   const inMain = mskMinutes >= 590 && mskMinutes < 1120
   const inEvening = mskMinutes >= 1120 && mskMinutes < 1430
   return inMain || inEvening
   ```
   - Where 590 = 09:50 MSK (pre-open, unchanged), 1120 = 18:40 MSK (evening start), 1430 = 23:50 MSK (evening end)
   - Keep everything else unchanged (weekend check, mskOffsetMinutes)

4. Run tests — they MUST pass (GREEN phase).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/terminal/market-hours.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/market-hours.ts contains `const inMain = mskMinutes >= 590 && mskMinutes < 1120`
    - src/lib/market-hours.ts contains `const inEvening = mskMinutes >= 1120 && mskMinutes < 1430`
    - src/lib/market-hours.ts contains `return inMain || inEvening`
    - src/__tests__/terminal/market-hours.test.ts contains `evening session`
    - src/__tests__/terminal/market-hours.test.ts contains at least 10 test cases (it blocks)
    - `npx vitest run src/__tests__/terminal/market-hours.test.ts` exits 0
  </acceptance_criteria>
  <done>isMarketOpen() returns true for 09:50-23:50 MSK on weekdays (main + evening session), false outside. All 10+ test cases pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add periodic stats refresh in terminal page during market hours</name>
  <files>src/app/(dashboard)/terminal/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/terminal/page.tsx
    - src/lib/market-hours.ts
    - src/server/actions/chart-actions.ts
  </read_first>
  <action>
In `src/app/(dashboard)/terminal/page.tsx`, add a new `useEffect` hook that periodically refreshes daily session stats every 60 seconds while an instrument is selected and the market is open.

Insert the following `useEffect` AFTER the existing `handleInstrumentSelect` callback (around line 209), before `handleQuickSelect`:

```typescript
useEffect(() => {
  if (!instrument?.figi) return
  const id = setInterval(() => {
    if (isMarketOpen()) fetchDailyStats(instrument.figi)
  }, 60_000)
  return () => clearInterval(id)
}, [instrument, fetchDailyStats])
```

This pattern is consistent with the existing order book refresh interval at lines 146-150 which uses the same `setInterval` + cleanup pattern.

Also update `fetchDailyStats` to pass the current `period` to `getDailySessionStatsAction` so that `periodOpen` is fetched for daily/weekly views:

Change line 189 from:
```typescript
const res = await getDailySessionStatsAction(figi)
```
To:
```typescript
const res = await getDailySessionStatsAction(figi, period)
```

And update the `useCallback` dependency array from `[]` to `[period]`:
```typescript
const fetchDailyStats = useCallback(async (figi: string) => {
  try {
    const res = await getDailySessionStatsAction(figi, period)
    if (res.success) {
      setDailyStats(res.data)
    }
  } catch {
    setDailyStats(null)
  }
}, [period])
```

The `period` variable is already defined in the component state at the top.
  </action>
  <verify>
    <automated>npx vitest run && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/terminal/page.tsx contains `setInterval(() =>` with `isMarketOpen()` check and `fetchDailyStats`
    - src/app/(dashboard)/terminal/page.tsx contains `60_000` (60-second interval for stats refresh)
    - src/app/(dashboard)/terminal/page.tsx contains `getDailySessionStatsAction(figi, period)`
    - src/app/(dashboard)/terminal/page.tsx contains `}, [period])` as dependency array for fetchDailyStats useCallback
    - src/app/(dashboard)/terminal/page.tsx contains `return () => clearInterval(id)` cleanup for the stats interval
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Terminal page refreshes daily stats every 60s during market hours (including evening session). Stats show updated H/L/Vol as evening candles arrive. Period is passed to getDailySessionStatsAction for correct periodOpen on daily/weekly views.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (including new market-hours tests)
2. `isMarketOpen(new Date('2026-03-30T16:00:00Z'))` returns `true` (19:00 MSK Monday = evening session)
3. Terminal page has a 60s interval that calls `fetchDailyStats` when `isMarketOpen()` is true
4. No new npm dependencies added
</verification>

<success_criteria>
- isMarketOpen() covers both main (09:50-18:40 MSK) and evening (18:40-23:50 MSK) sessions
- Terminal stats refresh every 60s during extended market hours
- All existing tests pass with no regressions
- Price bar H/L/Vol will update during evening session via periodic refresh
</success_criteria>

<output>
After completion, create `.planning/phases/14.2-moex-evening-session/14.2-01-SUMMARY.md`
</output>
