---
phase: 07-portfolio-optimization
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/server/actions/analytics-actions.ts
  - src/core/config/ai-prompts.ts
  - src/components/portfolio/markowitz-comparison.tsx
  - src/components/portfolio/rebalancing-actions.tsx
  - src/app/(dashboard)/portfolio/page.tsx
autonomous: false
requirements: [PORT-06, PORT-07, PORT-08]
must_haves:
  truths:
    - "User sees current vs optimal portfolio weights as two side-by-side donut charts"
    - "User sees concrete rebalancing actions table with BUY/SELL/HOLD, lot count, and RUB value"
    - "User can click Full AI Portfolio Analysis button and receive streaming markdown analysis"
    - "Optimization section shows graceful message when < 2 positions"
    - "AI context assembly stays under 50k chars"
  artifacts:
    - path: "src/server/actions/analytics-actions.ts"
      provides: "getMarkowitzOptimizationAction, getFullPortfolioAiAnalysisAction"
      contains: "getMarkowitzOptimizationAction"
    - path: "src/core/config/ai-prompts.ts"
      provides: "fullPortfolio AI prompt block"
      contains: "fullPortfolio"
    - path: "src/components/portfolio/markowitz-comparison.tsx"
      provides: "Side-by-side donut chart component"
      contains: "MarkowitzComparison"
    - path: "src/components/portfolio/rebalancing-actions.tsx"
      provides: "Rebalancing action list component"
      contains: "RebalancingActions"
    - path: "src/app/(dashboard)/portfolio/page.tsx"
      provides: "Wiring of new components into analytics tab"
      contains: "MarkowitzComparison"
  key_links:
    - from: "src/server/actions/analytics-actions.ts"
      to: "src/server/services/portfolio-analytics-service.ts"
      via: "getMarkowitzOptimization() call"
      pattern: "analyticsService.getMarkowitzOptimization"
    - from: "src/server/actions/analytics-actions.ts"
      to: "src/server/actions/ai-analysis-actions.ts"
      via: "analyzeWithAiAction() call"
      pattern: "analyzeWithAiAction"
    - from: "src/app/(dashboard)/portfolio/page.tsx"
      to: "src/server/actions/analytics-actions.ts"
      via: "getMarkowitzOptimizationAction import"
      pattern: "getMarkowitzOptimizationAction"
    - from: "src/components/portfolio/markowitz-comparison.tsx"
      to: "recharts"
      via: "PieChart, Pie, Cell imports"
      pattern: "PieChart"
---

<objective>
Add server actions, UI components, and portfolio page wiring for Markowitz optimization display, rebalancing actions table, and full AI portfolio analysis button.

Purpose: Complete PORT-06 (side-by-side donut), PORT-07 (rebalancing table), PORT-08 (full AI analysis). This is the user-facing layer on top of Plan 01's backend.
Output: Two new server actions, new AI prompt, two new React components, updated portfolio page, visual checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-portfolio-optimization/07-RESEARCH.md
@.planning/phases/07-portfolio-optimization/07-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan consumes -->

From src/core/types/analytics.ts (added by Plan 01):
```typescript
export type MarkowitzWeights = {
  ticker: string
  currentWeight: number
  optimalWeight: number
  currentValue: number
}[]

export type RebalancingAction = {
  ticker: string
  action: 'BUY' | 'SELL' | 'HOLD'
  lots: number
  valueRub: number
}

export type MarkowitzResult = {
  weights: MarkowitzWeights
  rebalancingActions: RebalancingAction[]
  expectedReturn: number
  expectedVolatility: number
  sharpe: number
  insufficientData: boolean
}
```

From src/server/services/portfolio-analytics-service.ts (added by Plan 01):
```typescript
async getMarkowitzOptimization(userId: string, days?: number): Promise<MarkowitzResult | null>
```

From src/server/actions/ai-analysis-actions.ts:
```typescript
export const analyzeWithAiAction = async (
  block: AiAnalysisBlock,
  userMessage: string,
): Promise<ApiResponse<string>>
```

From src/core/config/ai-prompts.ts:
```typescript
export type AiAnalysisBlock = "lot" | "chart" | "risk" | "fundamental" | "optimization" | "portfolio"
export const AI_PROMPTS: Record<AiAnalysisBlock, string> = { ... }
```

From src/components/portfolio/ai-analysis-button.tsx:
```typescript
type AiAnalysisButtonProps = {
  title: string
  triggerLabel?: string
  triggerLabelMobile?: string
  triggerIcon?: React.ReactNode
  analyzeAction: () => Promise<{ success: boolean; data?: string; error?: string }>
  onResult?: (result: string) => void
  variant?: "default" | "outline" | "ghost"
  size?: "default" | "sm" | "icon"
}
export const AiAnalysisButton = ({ ... }: AiAnalysisButtonProps) => { ... }
```

From src/components/portfolio/sector-donut.tsx (pattern reference for PieChart):
```typescript
import { PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer } from "recharts"
// innerRadius={55} outerRadius={85} paddingAngle={2}
```

From src/app/(dashboard)/portfolio/page.tsx:
```typescript
// Analytics tab currently has: ConcentrationCard, BenchmarkCard, DividendYieldCard (top row),
// CorrelationHeatmap + SectorDonut + AssetTypeChart (middle), TradeSuccessChart + InstrumentPnlTable (bottom)
// State: analyticsLoading, correlationMatrix, portfolioAnalytics
// fetchAnalytics() loads both correlation and analytics in parallel
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions + AI prompt + UI components</name>
  <files>
    src/server/actions/analytics-actions.ts,
    src/core/config/ai-prompts.ts,
    src/components/portfolio/markowitz-comparison.tsx,
    src/components/portfolio/rebalancing-actions.tsx
  </files>
  <read_first>
    src/server/actions/analytics-actions.ts,
    src/server/actions/ai-analysis-actions.ts,
    src/core/config/ai-prompts.ts,
    src/core/types/analytics.ts,
    src/components/portfolio/sector-donut.tsx,
    src/components/portfolio/ai-analysis-button.tsx,
    src/app/(dashboard)/portfolio/page.tsx
  </read_first>
  <action>
    **Part A: Add AI prompt block** in `src/core/config/ai-prompts.ts`:

    1. Add `"fullPortfolio"` to the `AiAnalysisBlock` union type:
       ```typescript
       export type AiAnalysisBlock = "lot" | "chart" | "risk" | "fundamental" | "optimization" | "portfolio" | "fullPortfolio"
       ```

    2. Add prompt entry to `AI_PROMPTS`:
       ```
       fullPortfolio: `Ты старший инвестиционный советник с экспертизой в теории Марковица и управлении частными портфелями на Московской бирже.

    Проведи комплексный анализ портфеля по четырём направлениям:

    1. **Риск-метрики** — оцени Шарпа, бету, VaR, максимальную просадку. Что критично?
    2. **Фундаментальные показатели** — P/E, дивидендная доходность ключевых позиций. Переоценённые / недооценённые?
    3. **Корреляции и диверсификация** — высококоррелированные пары, концентрация. Где слабые места?
    4. **Оптимизация Марковица** — сравни текущие и оптимальные веса. Какие действия по ребалансировке приоритетны?

    Завершай конкретными рекомендациями: что продать, что докупить, чего избегать.
    Формат: структурированный markdown на русском языке с таблицами где уместно.`
       ```

    **Part B: Add server actions** in `src/server/actions/analytics-actions.ts`:

    1. Add imports at top: `MarkowitzResult` from `@/core/types`, `analyzeWithAiAction` from `./ai-analysis-actions`, `getRiskMetricsAction` from `./risk-actions` (check actual export name — it may be `getRiskMetricsAction` or similar)

    2. Add `getMarkowitzOptimizationAction`:
       ```typescript
       export const getMarkowitzOptimizationAction = async (): Promise<ApiResponse<MarkowitzResult | null>> => {
         try {
           const userId = await getCurrentUserId()
           const result = await analyticsService.getMarkowitzOptimization(userId)
           return successResponse(result)
         } catch (e) {
           return errorResponse((e as Error).message)
         }
       }
       ```

    3. Add `getFullPortfolioAiAnalysisAction` — assembles context from all analytics + calls AI:
       ```typescript
       export const getFullPortfolioAiAnalysisAction = async (): Promise<ApiResponse<string>> => {
         try {
           const userId = await getCurrentUserId()
           const portfolio = await brokerService.getPortfolio(userId)
           const positions = portfolio?.positions ?? []

           const [analytics, correlation, optimization] = await Promise.all([
             getPortfolioAnalyticsAction(),
             getCorrelationMatrixAction(),
             getMarkowitzOptimizationAction(),
           ])

           const lines: string[] = []
           lines.push(`Портфель: ${positions.length} позиций`)

           // Sector
           if (analytics.success && analytics.data) {
             const sectors = analytics.data.sectorAllocation.slice(0, 5)
             lines.push(`\nСекторы: ${sectors.map(s => `${s.sector} ${s.percent.toFixed(1)}%`).join(', ')}`)
             const conc = analytics.data.concentration
             lines.push(`Концентрация (HHI): ${conc.hhi.toFixed(4)} — ${conc.level}`)
             if (analytics.data.benchmarkComparison) {
               const b = analytics.data.benchmarkComparison
               lines.push(`Доходность портфеля: ${b.portfolioReturn}%, IMOEX: ${b.benchmarkReturn}%, дельта: ${b.delta}%`)
             }
             lines.push(`Дивидендная доходность (взвеш.): ${analytics.data.aggregateDividendYield.weightedYield}%`)
           }

           // Correlation high pairs
           if (correlation.success && correlation.data && correlation.data.highPairs.length > 0) {
             lines.push(`\nВысокие корреляции (>0.7):`)
             for (const p of correlation.data.highPairs.slice(0, 10)) {
               lines.push(`  ${p.a} — ${p.b}: ${p.corr}`)
             }
           }

           // Markowitz
           if (optimization.success && optimization.data) {
             const opt = optimization.data
             lines.push(`\nОптимизация Марковица:`)
             lines.push(`| Тикер | Текущий вес | Оптимальный вес |`)
             lines.push(`|-------|-------------|-----------------|`)
             for (const w of opt.weights) {
               lines.push(`| ${w.ticker} | ${(w.currentWeight * 100).toFixed(1)}% | ${(w.optimalWeight * 100).toFixed(1)}% |`)
             }
             lines.push(`Ожидаемая доходность: ${(opt.expectedReturn * 100).toFixed(1)}%`)
             lines.push(`Волатильность: ${(opt.expectedVolatility * 100).toFixed(1)}%`)
             lines.push(`Sharpe: ${opt.sharpe.toFixed(2)}`)

             if (opt.rebalancingActions.filter(a => a.action !== 'HOLD').length > 0) {
               lines.push(`\nРебалансировка:`)
               for (const a of opt.rebalancingActions.filter(r => r.action !== 'HOLD')) {
                 lines.push(`  ${a.action} ${a.ticker}: ${a.lots} лотов (~${Math.round(a.valueRub)}₽)`)
               }
             }
           }

           // Top positions fundamentals
           const top5 = positions.slice(0, 5)
           if (top5.length > 0) {
             lines.push(`\nТоп-5 позиций:`)
             for (const p of top5) {
               lines.push(`  ${p.ticker} (${p.name}): ${p.quantity} шт., цена ${p.currentPrice}₽, доходность ${p.expectedYield.toFixed(1)}%`)
             }
           }

           const message = lines.join('\n')
           return analyzeWithAiAction('fullPortfolio', message)
         } catch (e) {
           return errorResponse((e as Error).message)
         }
       }
       ```

    **Part C: Create MarkowitzComparison component** at `src/components/portfolio/markowitz-comparison.tsx`:

    - "use client" component
    - Props: `{ data: MarkowitzResult | null; loading: boolean }`
    - Loading state: spinner in card (same pattern as SectorDonut)
    - Null/insufficientData state: "Недостаточно позиций для оптимизации (минимум 2 акции/ETF)"
    - Two side-by-side PieChart donuts using recharts (same pattern as SectorDonut):
      - Left: "Текущее" — data from `data.weights` using `currentWeight` as value
      - Right: "Оптимальное (Марковиц)" — data from `data.weights` using `optimalWeight` as value
      - Same `innerRadius={40} outerRadius={70} paddingAngle={2}` pattern
    - Below donuts: small stats row showing expectedReturn, expectedVolatility, sharpe
    - Color palette: use consistent colors per ticker across both donuts (map ticker index to color array)
    - Tailwind card wrapper: `rounded-xl border border-border bg-card p-4`
    - Title: `<h3 className="mb-3 text-sm font-semibold">Оптимизация портфеля</h3>`

    **Part D: Create RebalancingActions component** at `src/components/portfolio/rebalancing-actions.tsx`:

    - "use client" component
    - Props: `{ actions: RebalancingAction[]; loading: boolean }`
    - Loading: spinner in card
    - Empty/no actions: "Портфель близок к оптимальному"
    - Table with columns: Тикер, Действие, Лотов, Сумма (₽)
    - Action column: green text + ArrowUp icon for BUY, red text + ArrowDown icon for SELL, gray for HOLD
    - Filter out HOLD actions from display (only show BUY/SELL)
    - Use Intl.NumberFormat for RUB formatting (same as SectorDonut's formatRub)
    - Icons from lucide: `ArrowUpCircle`, `ArrowDownCircle`
    - Tailwind card wrapper: `rounded-xl border border-border bg-card p-4`
    - Title: `<h3 className="mb-3 text-sm font-semibold">Ребалансировка</h3>`
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/core/config/ai-prompts.ts contains `"fullPortfolio"` in AiAnalysisBlock union
    - src/core/config/ai-prompts.ts AI_PROMPTS has `fullPortfolio:` key with prompt text
    - src/server/actions/analytics-actions.ts contains `export const getMarkowitzOptimizationAction`
    - src/server/actions/analytics-actions.ts contains `export const getFullPortfolioAiAnalysisAction`
    - src/server/actions/analytics-actions.ts imports `analyzeWithAiAction`
    - src/components/portfolio/markowitz-comparison.tsx contains `export const MarkowitzComparison`
    - src/components/portfolio/markowitz-comparison.tsx contains `PieChart` from recharts
    - src/components/portfolio/markowitz-comparison.tsx contains text "Текущее" and "Оптимальное"
    - src/components/portfolio/rebalancing-actions.tsx contains `export const RebalancingActions`
    - src/components/portfolio/rebalancing-actions.tsx contains `ArrowUpCircle` or `ArrowUp` from lucide
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>
    Server actions callable, AI prompt registered, both UI components render correctly with loading/empty/data states. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire components into portfolio page analytics tab</name>
  <files>
    src/app/(dashboard)/portfolio/page.tsx
  </files>
  <read_first>
    src/app/(dashboard)/portfolio/page.tsx,
    src/components/portfolio/markowitz-comparison.tsx,
    src/components/portfolio/rebalancing-actions.tsx,
    src/components/portfolio/ai-analysis-button.tsx,
    src/server/actions/analytics-actions.ts
  </read_first>
  <action>
    Update `src/app/(dashboard)/portfolio/page.tsx`:

    1. Add imports:
       ```typescript
       import { getMarkowitzOptimizationAction, getFullPortfolioAiAnalysisAction } from "@/server/actions/analytics-actions"
       import { MarkowitzComparison } from "@/components/portfolio/markowitz-comparison"
       import { RebalancingActions } from "@/components/portfolio/rebalancing-actions"
       import { AiAnalysisButton } from "@/components/portfolio/ai-analysis-button"
       import type { MarkowitzResult } from "@/core/types"
       ```

    2. Add state:
       ```typescript
       const [markowitzResult, setMarkowitzResult] = useState<MarkowitzResult | null>(null)
       const [markowitzLoading, setMarkowitzLoading] = useState(false)
       ```

    3. Update `fetchAnalytics` callback to also fetch Markowitz data in parallel:
       ```typescript
       const fetchAnalytics = useCallback(async () => {
         setAnalyticsLoading(true)
         setMarkowitzLoading(true)
         try {
           const [corrRes, analyticsRes, markowitzRes] = await Promise.all([
             getCorrelationMatrixAction(correlationPeriod),
             getPortfolioAnalyticsAction(),
             getMarkowitzOptimizationAction(),
           ])
           if (corrRes.success && corrRes.data) setCorrelationMatrix(corrRes.data)
           if (analyticsRes.success && analyticsRes.data) setPortfolioAnalytics(analyticsRes.data)
           if (markowitzRes.success) setMarkowitzResult(markowitzRes.data ?? null)
         } finally {
           setAnalyticsLoading(false)
           setMarkowitzLoading(false)
         }
       }, [correlationPeriod])
       ```

    4. Add new section BELOW the existing TradeSuccessChart + InstrumentPnlTable grid, INSIDE the analytics TabsContent:
       ```tsx
       {/* Optimization section */}
       <div className="grid grid-cols-1 gap-4 lg:grid-cols-2">
         <MarkowitzComparison
           data={markowitzResult}
           loading={analyticsLoading || markowitzLoading}
         />
         <RebalancingActions
           actions={markowitzResult?.rebalancingActions ?? []}
           loading={analyticsLoading || markowitzLoading}
         />
       </div>

       {/* Full AI Analysis button */}
       <div className="flex justify-center">
         <AiAnalysisButton
           title="Комплексный анализ портфеля"
           triggerLabel="Полный AI-анализ портфеля"
           triggerLabelMobile="AI-анализ"
           analyzeAction={getFullPortfolioAiAnalysisAction}
         />
       </div>
       ```

    5. Ensure the MarkowitzResult type import is added to the existing type imports from `@/core/types`.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(dashboard)/portfolio/page.tsx imports MarkowitzComparison
    - src/app/(dashboard)/portfolio/page.tsx imports RebalancingActions
    - src/app/(dashboard)/portfolio/page.tsx imports AiAnalysisButton
    - src/app/(dashboard)/portfolio/page.tsx imports getMarkowitzOptimizationAction
    - src/app/(dashboard)/portfolio/page.tsx imports getFullPortfolioAiAnalysisAction
    - src/app/(dashboard)/portfolio/page.tsx contains `useState<MarkowitzResult | null>`
    - src/app/(dashboard)/portfolio/page.tsx contains `<MarkowitzComparison`
    - src/app/(dashboard)/portfolio/page.tsx contains `<RebalancingActions`
    - src/app/(dashboard)/portfolio/page.tsx contains `analyzeAction={getFullPortfolioAiAnalysisAction}`
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>
    Portfolio analytics tab shows Markowitz optimization section (two donuts + rebalancing table) and Full AI Analysis button. All data loads in parallel with existing analytics. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of optimization and AI analysis</name>
  <what-built>
    Markowitz portfolio optimization (side-by-side donut charts showing current vs optimal weights), rebalancing actions table (BUY/SELL with lot counts), and Full AI Portfolio Analysis button — all integrated into the portfolio analytics tab.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/portfolio
    2. Click the "Аналитика" tab
    3. Scroll down past existing analytics sections
    4. Verify: Two side-by-side donut charts appear — "Текущее" (left) and "Оптимальное (Марковиц)" (right)
    5. Verify: Below donuts, expected return / volatility / Sharpe ratio stats are shown
    6. Verify: Rebalancing actions table shows BUY (green) and SELL (red) with lot counts and RUB amounts
    7. Verify: "Полный AI-анализ портфеля" blue button appears at the bottom
    8. Click the AI analysis button
    9. Verify: Loading spinner appears, then a comprehensive markdown analysis covering risk, fundamentals, correlations, and optimization recommendations
    10. If portfolio has < 2 stocks/ETFs: verify graceful "Недостаточно позиций" message instead of donuts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose
npx next build 2>&1 | tail -10
</verification>

<success_criteria>
- Side-by-side donut charts display current vs Markowitz-optimal portfolio weights
- Rebalancing actions table shows BUY/SELL with correct lot counts
- Full AI Portfolio Analysis button triggers comprehensive analysis covering all 4 dimensions
- All components handle loading and empty states gracefully
- Build passes, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/07-portfolio-optimization/07-02-SUMMARY.md`
</output>
