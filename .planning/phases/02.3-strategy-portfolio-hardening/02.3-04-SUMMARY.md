---
phase: 02.3-strategy-portfolio-hardening
plan: 04
subsystem: database
tags: [sql, migration, tickers, useCallback, paper-portfolio]

# Dependency graph
requires:
  - phase: 02.3-strategy-portfolio-hardening
    provides: cleanTicker utility that removes @ suffix from tickers on save
provides:
  - SQL migration script to clean existing dirty @ tickers in Strategy and Signal tables
  - Verified stable useCallback in paper-portfolio-view (no re-render issues)
affects: [production-deploy, strategy-checker, signal-checker]

# Tech tracking
tech-stack:
  added: []
  patterns: [manual SQL migration via Supabase SQL Editor]

key-files:
  created:
    - scripts/migrate-clean-tickers.sql
  modified: []

key-decisions:
  - "Migration runs via BEGIN/COMMIT transaction to ensure atomicity"
  - "Verification SELECT queries included after COMMIT to confirm 0 dirty rows"
  - "paper-portfolio-view useCallback already had correct empty deps [] — no change needed"

patterns-established:
  - "Manual SQL migrations stored in scripts/ directory for Supabase SQL Editor execution"

requirements-completed: [HARD-14, HARD-15]

# Metrics
duration: 5min
completed: 2026-03-25
---

# Phase 02.3 Plan 04: Ticker Cleanup Migration & Paper Portfolio Stability Summary

**SQL migration script to strip @ suffix from existing dirty tickers in Strategy/Signal tables, and verified paper-portfolio-view useCallback already stable.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-25T08:50:00Z
- **Completed:** 2026-03-25T08:55:00Z
- **Tasks:** 2
- **Files modified:** 1 created, 0 modified

## Accomplishments

- Created `scripts/migrate-clean-tickers.sql` with transactional UPDATE for both Strategy and Signal tables using REPLACE to strip @ suffix
- Included post-migration verification SELECTs that should return 0 rows after execution
- Confirmed paper-portfolio-view already has stable `useCallback` with empty deps `[]` and `useEffect([fetchData])` — no unnecessary re-renders

## Task Commits

Each task was committed atomically:

1. **Task 1: Create SQL migration script for ticker @ cleanup** - `d41efbf` (chore)
2. **Task 2: Fix paper-portfolio-view.tsx useCallback stability** - No commit needed (already correct)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `scripts/migrate-clean-tickers.sql` - Transactional SQL migration: UPDATE Strategy + Signal WHERE instrument LIKE '%@', REPLACE to strip @, with verification queries

## Decisions Made

- Migration wrapped in BEGIN/COMMIT transaction for atomicity — if one UPDATE fails, neither applies
- Verification queries after COMMIT confirm clean state (should return 0 rows)
- paper-portfolio-view useCallback confirmed correct at lines 30-40: `useCallback(async () => {...}, [])` with `useEffect(() => { fetchData() }, [fetchData])`

## Deviations from Plan

None - plan executed exactly as written. Task 2 was a no-op because the component already had correct stable useCallback pattern.

## Issues Encountered

None. Pre-existing TypeScript error in `src/__tests__/price-cache.test.ts` is unrelated to this plan (test mock type mismatch, out of scope).

## User Setup Required

**Production step required:** Run `scripts/migrate-clean-tickers.sql` via Supabase SQL Editor on production to clean existing dirty tickers. The verification queries at the bottom should return 0 rows after execution.

## Next Phase Readiness

- Ticker @ suffix cleanup is ready for production execution
- After running migration, all existing Strategy and Signal records will have clean tickers
- paper-portfolio-view is confirmed stable, no changes needed

---
*Phase: 02.3-strategy-portfolio-hardening*
*Completed: 2026-03-25*
