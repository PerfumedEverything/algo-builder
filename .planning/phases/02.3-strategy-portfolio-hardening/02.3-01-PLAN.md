---
phase: 02.3-strategy-portfolio-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/services/price-cache.ts
  - src/__tests__/price-cache.test.ts
autonomous: true
requirements:
  - HARD-01
  - HARD-02
  - HARD-03

must_haves:
  truths:
    - "getPrice returns null when short-term cache expired (no 7-day fallback)"
    - "Price TTL is 120s for intraday freshness"
    - "Redis lock TTL is 10s to prevent premature expiry"
  artifacts:
    - path: "src/server/services/price-cache.ts"
      provides: "Price cache with fresh TTLs and no stale fallback"
      contains: "PRICE_TTL = 120"
    - path: "src/__tests__/price-cache.test.ts"
      provides: "Tests for price cache behavior"
  key_links:
    - from: "src/server/services/price-cache.ts"
      to: "strategy-checker.ts, signal-checker.ts"
      via: "getPrice() called during condition evaluation"
      pattern: "priceCache\\.getPrice"
---

<objective>
Harden price cache: remove stale 7-day fallback, reduce TTL to 120s, increase lock TTL to 10s.

Purpose: Strategies on 1m/5m candles need fresh prices. Week-old fallback causes false triggers. Short lock TTL causes double triggers under slow network.
Output: Updated price-cache.ts with tests covering new behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-strategy-portfolio-hardening/02.3-CONTEXT.md
</context>

<interfaces>
From src/server/services/price-cache.ts:
```typescript
export class PriceCache {
  async setPrice(instrumentId: string, price: number): Promise<void>
  async getPrice(instrumentId: string): Promise<number | null>
  async acquireLock(type: "strategy" | "signal", id: string): Promise<boolean>
  async releaseLock(type: "strategy" | "signal", id: string): Promise<void>
}
```
Current constants:
- PRICE_TTL = 600 (10min) — must become 120 (2min)
- LAST_PRICE_TTL = 604800 (7 days) — must be removed
- Lock TTL = 5 — must become 10
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write price cache tests for new behavior</name>
  <files>src/__tests__/price-cache.test.ts</files>
  <read_first>src/server/services/price-cache.ts, src/lib/redis.ts</read_first>
  <behavior>
    - Test 1: getPrice returns price when short-term cache exists
    - Test 2: getPrice returns null when short-term cache expired (NOT fallback to lastprice)
    - Test 3: setPrice stores only in short-term key (no lastprice key)
    - Test 4: acquireLock uses 10s TTL
  </behavior>
  <action>
Create test file `src/__tests__/price-cache.test.ts`. Mock `@/lib/redis` with a fake redis object (vi.mock). Test that:

1. `getPrice("SBER")` returns price from `price:SBER` key when present.
2. `getPrice("SBER")` returns null when `price:SBER` returns null — it must NOT attempt `lastprice:SBER` lookup (per D-01).
3. `setPrice("SBER", 300)` calls `redis.set` with key `price:SBER`, TTL 120 — does NOT set `lastprice:SBER` (per D-01, D-02).
4. `acquireLock("strategy", "123")` calls `redis.set` with key `lock:strategy:123`, "EX", 10, "NX" (per D-03).

Use vitest. Mock redis as `{ get: vi.fn(), set: vi.fn(), del: vi.fn(), keys: vi.fn(), publish: vi.fn() }`.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx vitest run src/__tests__/price-cache.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PRICE_TTL" src/__tests__/price-cache.test.ts (tests reference TTL)
    - grep -q "lastprice" src/__tests__/price-cache.test.ts (tests verify no fallback)
    - grep -q "EX.*10" src/__tests__/price-cache.test.ts (tests verify lock TTL)
  </acceptance_criteria>
  <done>All 4 tests written and initially fail (RED) because price-cache.ts still has old behavior</done>
</task>

<task type="auto">
  <name>Task 2: Update price-cache.ts — remove stale fallback, reduce TTL, increase lock TTL</name>
  <files>src/server/services/price-cache.ts</files>
  <read_first>src/server/services/price-cache.ts, src/__tests__/price-cache.test.ts</read_first>
  <action>
Modify `src/server/services/price-cache.ts` with these exact changes (per D-01, D-02, D-03):

1. Change `PRICE_TTL = 600` to `PRICE_TTL = 120` (per D-02).
2. Remove `LAST_PRICE_PREFIX` constant entirely (per D-01).
3. Remove `LAST_PRICE_TTL` constant entirely (per D-01).
4. In `setPrice()`: remove the second `redis.set` call that writes to `lastprice:` key. Keep only the `price:` key write with 120s TTL.
5. In `getPrice()`: remove the fallback block that reads from `lastprice:` key. After checking `price:` key, if no data, return null directly.
6. In `acquireLock()`: change TTL from 5 to 10 in the `redis.set` call (per D-03).

Do NOT change: setCandles, getCandles, publishPriceUpdate, getAllTrackedInstruments, releaseLock.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx vitest run src/__tests__/price-cache.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PRICE_TTL = 120" src/server/services/price-cache.ts
    - ! grep -q "LAST_PRICE" src/server/services/price-cache.ts
    - ! grep -q "lastprice" src/server/services/price-cache.ts
    - grep -q '"EX", 10' src/server/services/price-cache.ts
  </acceptance_criteria>
  <done>Price cache returns null for expired prices (no stale fallback), TTL is 120s, lock TTL is 10s. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/price-cache.test.ts` — all pass
- `grep "PRICE_TTL = 120" src/server/services/price-cache.ts` — confirmed
- `grep -c "lastprice" src/server/services/price-cache.ts` — returns 0
- `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
- getPrice() returns null when short-term cache expired (no 7-day fallback)
- PRICE_TTL is 120 seconds
- Lock TTL is 10 seconds
- LAST_PRICE_PREFIX and LAST_PRICE_TTL fully removed
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-strategy-portfolio-hardening/02.3-01-SUMMARY.md`
</output>
