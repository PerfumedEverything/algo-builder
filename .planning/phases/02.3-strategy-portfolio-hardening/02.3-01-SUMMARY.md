---
phase: 02.3-strategy-portfolio-hardening
plan: 01
subsystem: infra
tags: [redis, price-cache, ttl, vitest, tdd]

# Dependency graph
requires: []
provides:
  - "PriceCache with 2-minute TTL and no stale 7-day lastprice fallback"
  - "Lock TTL increased to 10s to prevent double-trigger under slow network"
  - "4 tests covering getPrice, setPrice, acquireLock new behavior"
affects: [strategy-checker, signal-checker]

# Tech tracking
tech-stack:
  added: []
  patterns: [TDD red-green for price cache behavior verification]

key-files:
  created:
    - src/__tests__/price-cache.test.ts
  modified:
    - src/server/services/price-cache.ts

key-decisions:
  - "LAST_PRICE_PREFIX and LAST_PRICE_TTL fully removed — no 7-day stale fallback, getPrice returns null on cache miss"
  - "PRICE_TTL reduced from 600 to 120 — 2-minute freshness for 1m/5m candle strategies"
  - "Lock TTL increased from 5 to 10 — prevents duplicate strategy/signal triggers under slow network"

patterns-established:
  - "getPrice returns null | number — callers must handle null as no-price condition (strategy condition returns false)"

requirements-completed: [HARD-01, HARD-02, HARD-03]

# Metrics
duration: 7min
completed: 2026-03-25
---

# Phase 02.3 Plan 01: Price Cache Hardening Summary

**Price cache hardened with 2-minute TTL, 7-day stale fallback removed, lock TTL doubled to 10s — all verified by 4 vitest tests via TDD**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-25T08:44:00Z
- **Completed:** 2026-03-25T08:51:21Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Removed `LAST_PRICE_PREFIX`/`LAST_PRICE_TTL` and all `lastprice:` Redis reads/writes — strategies no longer trigger on week-old prices
- `PRICE_TTL` reduced from 600s to 120s — 2-minute freshness window matches 1m/5m candle trading cadence
- Lock TTL doubled from 5s to 10s — prevents double-trigger race condition under slow network
- 4 vitest tests written first (TDD RED), then implementation made them pass (GREEN)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write price cache tests (RED)** - `065eb05` (test)
2. **Task 2: Update price-cache.ts (GREEN)** - `06eef99` (feat)

**Plan metadata:** (docs commit pending)

_Note: TDD tasks have two commits — test first, then implementation._

## Files Created/Modified

- `src/__tests__/price-cache.test.ts` - 4 tests: getPrice with cache, getPrice returns null on miss, setPrice no lastprice, acquireLock EX 10
- `src/server/services/price-cache.ts` - Removed LAST_PRICE constants and writes; PRICE_TTL=120; lock TTL=10

## Decisions Made

- `LAST_PRICE_PREFIX` and `LAST_PRICE_TTL` fully removed — no partial removal, no migration period. Stale prices cause false strategy triggers, clean break is correct.
- `PRICE_TTL = 120` chosen per plan spec for 1m/5m candle freshness window
- Lock TTL 10s per plan spec — gives enough time for strategy evaluation under slow T-Invest API responses

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

Minor: initial test file had vi.mock hoisting issue (redisMock referenced before initialization). Fixed by using vi.fn() directly inside the mock factory and importing the mock reference after the mock declaration. Resolved in same task, no extra commit needed.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Price cache hardened and tested, ready for 02.3-02 (strategy P&L fix or next hardening task)
- `strategy-checker.ts` and `signal-checker.ts` both call `priceCache.getPrice()` — they now receive null on cache miss (already handle null per 02.2-01 decisions)

## Self-Check: PASSED

- FOUND: src/__tests__/price-cache.test.ts
- FOUND: src/server/services/price-cache.ts
- FOUND: 02.3-01-SUMMARY.md
- FOUND: commit 065eb05 (test RED)
- FOUND: commit 06eef99 (feat GREEN)
- All 4 vitest tests pass

---
*Phase: 02.3-strategy-portfolio-hardening*
*Completed: 2026-03-25*
