---
phase: 08-ai-assistant-deep-upgrade
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/components/strategy/ai-wizard-dialog.tsx
  - src/components/strategy/backtest-preview.tsx
autonomous: false
requirements: [AIUP-08]

must_haves:
  truths:
    - "After strategy creation in wizard step 3, backtest runs automatically on 3-month historical data"
    - "Backtest results displayed inline: total trades, win rate, net P&L, max drawdown"
    - "Conversation continues in wizard — chat state not reset on strategy creation (chatKey not incremented)"
    - "Wizard passes instrument context (ticker, timeframe, figi) to AiChat for enriched AI responses"
  artifacts:
    - path: "src/components/strategy/backtest-preview.tsx"
      provides: "BacktestPreview component showing backtest results inline"
      exports: ["BacktestPreview"]
    - path: "src/components/strategy/ai-wizard-dialog.tsx"
      provides: "Updated wizard with backtest preview in step 3 and conversation continuity"
  key_links:
    - from: "src/components/strategy/ai-wizard-dialog.tsx"
      to: "src/server/actions/backtest-actions.ts"
      via: "runBacktestAction call after strategy save"
      pattern: "runBacktestAction"
    - from: "src/components/strategy/ai-wizard-dialog.tsx"
      to: "src/components/strategy/ai-chat.tsx"
      via: "instrumentContext prop for enriched AI context"
      pattern: "instrumentContext"
---

<objective>
Add backtest preview after strategy creation and wire conversation continuity in the wizard.

Purpose: After a user creates a strategy through the wizard, they immediately see how it would have performed on 3 months of historical data. The wizard also passes instrument context to AiChat so AI gets enriched market data, and conversation persists across strategy creations.

Output: BacktestPreview component, updated AiWizardDialog with backtest + continuity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-RESEARCH.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-01-SUMMARY.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-02-SUMMARY.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-03-SUMMARY.md
@src/components/strategy/ai-wizard-dialog.tsx
@src/components/strategy/ai-chat.tsx
@src/server/actions/backtest-actions.ts
@src/server/services/backtest-service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BacktestPreview component and wire into wizard</name>
  <files>
    src/components/strategy/backtest-preview.tsx,
    src/components/strategy/ai-wizard-dialog.tsx
  </files>
  <read_first>
    src/components/strategy/ai-wizard-dialog.tsx,
    src/components/strategy/ai-chat.tsx,
    src/server/actions/backtest-actions.ts,
    src/server/services/backtest-service.ts,
    src/hooks/use-ai-stream.ts
  </read_first>
  <action>
    1. Create `src/components/strategy/backtest-preview.tsx`:
       - Props: `{ result: BacktestResult; loading: boolean; error?: string }`
       - Import BacktestResult type from `@/server/services`
       - Render a compact card showing:
         - Total trades count
         - Win rate % (wins / total * 100)
         - Net P&L in rubles (formatted with toLocaleString)
         - Max drawdown %
         - Sharpe ratio if available
       - Loading state: skeleton with Loader2 spinner + "Запускаю бэктест..."
       - Error state: muted error text
       - Use Tailwind: `rounded-lg border p-4` card style, grid layout for metrics
       - Under 80 lines

    2. Update `src/components/strategy/ai-wizard-dialog.tsx`:

       a) BACKTEST PREVIEW (AIUP-08):
          - Add state: `backtestResult`, `backtestLoading`, `backtestError`
          - In `handleStrategyGenerated` callback (or after form submission in step "form"):
            - After strategy is saved (onSuccess called), trigger backtest:
            - Need strategyId — modify `onSuccess` callback to return strategyId, or use a ref
            - Alternative approach: add a new callback `onStrategyCreated(strategyId: string)` to StrategyForm
            - Call `runBacktestAction(strategyId, { fromDate: 3monthsAgo, toDate: now, positionSize: 100000 })`
            - Store result in backtestResult state
          - Render `<BacktestPreview>` component below StrategyForm in step "form" when backtestResult is set

       b) CONVERSATION CONTINUITY (AIUP-09):
          - STOP incrementing chatKey in `handleProceedToStrategy` — remove `setChatKey((k) => k + 1)`
          - Only increment chatKey when wizard reopens (already handled by useEffect([open]))
          - When strategy is created, the AiChat's useAiStream addSystemMessage handles the marker

       c) INSTRUMENT CONTEXT:
          - Pass `instrumentContext` prop to AiChat:
            ```
            <AiChat
              key={chatKey}
              onGenerated={handleStrategyGenerated}
              onStrategyExtracted={setExtractedStrategy}
              analysisContext={analysisResult ?? undefined}
              instrumentContext={initialInstrument ? { ticker: initialInstrument, timeframe: "1h" } : undefined}
            />
            ```
          - The AiChat component passes this to useAiStream which sends it to /api/ai/chat

       d) Ensure `handleProceedToStrategy` no longer resets chat on navigation to strategy step (remove setChatKey increment but keep setExtractedStrategy(null) and setStep("strategy"))
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/components/strategy/backtest-preview.tsx
    - grep -q "BacktestPreview" src/components/strategy/backtest-preview.tsx
    - grep -q "runBacktestAction" src/components/strategy/ai-wizard-dialog.tsx
    - grep -q "backtestResult\|backtestLoading" src/components/strategy/ai-wizard-dialog.tsx
    - grep -q "instrumentContext" src/components/strategy/ai-wizard-dialog.tsx
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    BacktestPreview shows trade count, win rate, P&L, max drawdown after strategy creation. Wizard passes instrument context to AiChat. Conversation preserved across strategy creations.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete AI assistant upgrade flow</name>
  <what-built>
    Complete AI Assistant upgrade:
    1. AI uses thinking mode (deepseek-reasoner) with visible reasoning indicator
    2. Responses stream character by character
    3. Rich context: market data, portfolio, fundamentals, senior timeframe
    4. Quick action buttons after strategy proposal
    5. Backtest preview after strategy creation (3-month historical)
    6. Conversation continues after strategy creation
  </what-built>
  <how-to-verify>
    1. Open terminal, select an instrument (e.g., SBER)
    2. Click AI analysis button to open wizard
    3. Wait for analysis to complete, click "Build strategy from analysis"
    4. In the AI chat: observe streaming text (characters should appear one by one)
    5. While AI thinks: verify "Analysing..." indicator appears with expandable reasoning
    6. After AI proposes a strategy: verify quick action buttons appear (Create, More variants, Adjust risks)
    7. Click "Create this strategy" button
    8. After creation: verify backtest preview appears (trades, win rate, P&L)
    9. After creation: verify you can continue chatting ("now make one for GAZP")
    10. Verify no chat reset — previous messages still visible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <files>N/A</files>
  <action>Human verification — test the complete AI assistant flow end-to-end</action>
  <verify>Manual verification by user</verify>
  <done>User confirms all 10 verification steps pass</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
test -f src/components/strategy/backtest-preview.tsx && echo "PASS: BacktestPreview exists"
grep -q "runBacktestAction" src/components/strategy/ai-wizard-dialog.tsx && echo "PASS: Backtest wired"
</verification>

<success_criteria>
- BacktestPreview renders after strategy creation with trades, win rate, P&L, drawdown
- Wizard passes instrument context to AiChat for enriched AI responses
- Conversation persists after strategy creation — user can continue chatting
- Full flow verified by user: analysis -> strategy -> creation -> backtest -> continue
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-assistant-deep-upgrade/08-04-SUMMARY.md`
</output>
