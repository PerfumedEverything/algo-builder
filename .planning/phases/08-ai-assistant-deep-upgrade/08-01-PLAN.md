---
phase: 08-ai-assistant-deep-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/providers/ai/types.ts
  - src/server/services/ai-context-service.ts
  - src/server/services/index.ts
  - src/__tests__/ai-context-service.test.ts
autonomous: true
requirements: [AIUP-02, AIUP-03, AIUP-06, AIUP-07]

must_haves:
  truths:
    - "AiContextService assembles market data, portfolio positions, fundamentals, and senior timeframe candles into a structured context string"
    - "Context string stays within 50k char limit by truncating candles to last 100 bars"
    - "Senior timeframe mapping resolves correctly (1m->5m, 5m->15m, etc.)"
    - "Portfolio summary includes ticker, weight %, P&L for each position"
    - "Fundamentals include P/E, dividend yield when available"
  artifacts:
    - path: "src/server/providers/ai/types.ts"
      provides: "Extended AI types with streaming, thinking, quick actions"
      exports: ["AiStreamChunk", "QuickAction", "AiChatMessage", "AiChatResponse", "AiProvider"]
    - path: "src/server/services/ai-context-service.ts"
      provides: "Context assembly service for AI chat"
      exports: ["AiContextService"]
    - path: "src/__tests__/ai-context-service.test.ts"
      provides: "Unit tests for context assembly"
  key_links:
    - from: "src/server/services/ai-context-service.ts"
      to: "src/server/services/broker-service.ts"
      via: "BrokerService.getCandles, getPortfolio"
      pattern: "broker.*getCandles|broker.*getPortfolio"
    - from: "src/server/services/ai-context-service.ts"
      to: "src/server/services/fundamental-service.ts"
      via: "FundamentalService.getMetrics"
      pattern: "FundamentalService"
---

<objective>
Create the foundation types and context assembly service for the AI upgrade.

Purpose: All subsequent plans depend on the extended AI types (streaming chunks, quick actions) and the AiContextService that gathers market data, portfolio, fundamentals, and multi-timeframe candles into a structured prompt context.

Output: Extended types.ts, AiContextService class, unit tests proving context assembly correctness.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-RESEARCH.md
@src/server/providers/ai/types.ts
@src/server/providers/ai/deepseek-provider.ts
@src/server/services/broker-service.ts
@src/server/services/fundamental-service.ts
@src/server/services/portfolio-health-service.ts
@src/server/services/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AI types and create AiContextService</name>
  <files>
    src/server/providers/ai/types.ts,
    src/server/services/ai-context-service.ts,
    src/server/services/index.ts,
    src/__tests__/ai-context-service.test.ts
  </files>
  <read_first>
    src/server/providers/ai/types.ts,
    src/server/providers/ai/deepseek-provider.ts,
    src/server/services/broker-service.ts,
    src/server/services/fundamental-service.ts,
    src/server/services/portfolio-health-service.ts,
    src/server/services/index.ts,
    src/server/actions/terminal-actions.ts
  </read_first>
  <behavior>
    - Test 1: assembleContext returns string containing "OHLCV" section with candle data when candles available
    - Test 2: assembleContext returns string containing "Portfolio" section with position tickers and weights when portfolio available
    - Test 3: assembleContext returns string containing senior timeframe section when senior candles available
    - Test 4: assembleContext returns string containing fundamentals (P/E, dividends) when fundamentals available
    - Test 5: assembleContext truncates candles to last 100 bars maximum
    - Test 6: assembleContext result length does not exceed 50000 chars
    - Test 7: SENIOR_TIMEFRAME map resolves 1m->5m, 5m->15m, 15m->1h, 1h->4h, 4h->1d, 1d->1w
    - Test 8: assembleContext handles partial failures gracefully (one service fails, others still included) via Promise.allSettled
    - Test 9: assembleContext includes order book data when available (best bid/ask, depth)
  </behavior>
  <action>
    1. Extend `src/server/providers/ai/types.ts`:
       - Keep existing AiChatMessage, AiChatResponse, AiProvider
       - Add `AiStreamChunk = { type: "thinking" | "content" | "strategy" | "done"; content: string }`
       - Add `QuickAction = { label: string; action: "CREATE" | "MORE" | "ADJUST_RISKS"; payload?: Record<string, unknown> }`
       - Extend AiChatMessage to include optional `thinkingContent?: string`
       - Extend AiChatResponse to include optional `thinkingContent?: string`
       - Add `chatWithThinking` method to AiProvider interface: `chatWithThinking(messages: AiChatMessage[], forceCreate?: boolean): AsyncGenerator<AiStreamChunk>`
       - Add `AiContextParams = { ticker: string; timeframe: string; userId: string; figi?: string }`

    2. Create `src/server/services/ai-context-service.ts` (class, OOP per project rules):
       - Export `SENIOR_TIMEFRAME` map: { "1m": "5m", "5m": "15m", "15m": "1h", "1h": "4h", "4h": "1d", "1d": "1w", "1w": "1w" }
       - Class `AiContextService` with static method `assembleContext(params: AiContextParams): Promise<string>`
       - Inside: use Promise.allSettled to fetch in parallel:
         a) Candles via BrokerService (last 100 bars only — NOT 500+ warmup)
         b) Senior timeframe candles (last 50 bars) using SENIOR_TIMEFRAME map
         c) Portfolio via BrokerService.getPortfolio(userId)
         d) Fundamentals via FundamentalService.getMetrics(ticker, 0) — price=0 is fine for P/E lookup
         e) Order book via existing getOrderBookAction or direct broker call (depth=5)
         f) PortfolioHealthService summary if portfolio available
       - Format into structured text sections:
         ```
         === Рыночные данные ({ticker}, {timeframe}) ===
         Последние 100 свечей: [date, O, H, L, C, V] ...

         === Старший таймфрейм ({seniorTF}) ===
         Последние 50 свечей: ...

         === Стакан ===
         Лучший бид: X, Лучший аск: Y, Спред: Z%

         === Портфель пользователя ===
         Позиция: TICKER — X%, P&L: +Y₽
         Health Score: XX/100
         Проблемы: ...

         === Фундаментальные данные ({ticker}) ===
         P/E: X, Дивидендная доходность: Y%
         ```
       - Truncate total to 50000 chars if exceeded (cut candles first)
       - Each section wrapped in try/catch via allSettled — partial failure = omit that section

    3. Add `AiContextService` export to `src/server/services/index.ts`

    4. Write tests in `src/__tests__/ai-context-service.test.ts`:
       - Mock BrokerService, FundamentalService, PortfolioHealthService
       - Test all 9 behaviors listed above
       - Use vitest vi.mock for service mocking
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx vitest run src/__tests__/ai-context-service.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AiStreamChunk" src/server/providers/ai/types.ts
    - grep -q "QuickAction" src/server/providers/ai/types.ts
    - grep -q "chatWithThinking" src/server/providers/ai/types.ts
    - grep -q "SENIOR_TIMEFRAME" src/server/services/ai-context-service.ts
    - grep -q "assembleContext" src/server/services/ai-context-service.ts
    - grep -q "Promise.allSettled" src/server/services/ai-context-service.ts
    - grep -q "AiContextService" src/server/services/index.ts
    - npx vitest run src/__tests__/ai-context-service.test.ts passes with all tests green
  </acceptance_criteria>
  <done>
    Extended AI types exported. AiContextService assembles enriched context from candles, portfolio, fundamentals, order book, and senior timeframe. All 9 unit tests pass. Context truncated to 50k chars max.
  </done>
</task>

</tasks>

<verification>
npx vitest run src/__tests__/ai-context-service.test.ts --reporter=verbose
grep -q "AiStreamChunk" src/server/providers/ai/types.ts && echo "PASS: AiStreamChunk type exists"
grep -q "AiContextService" src/server/services/index.ts && echo "PASS: AiContextService exported"
</verification>

<success_criteria>
- AiStreamChunk, QuickAction, AiContextParams types exported from types.ts
- AiContextService.assembleContext() returns structured context string with market data, portfolio, fundamentals
- Senior timeframe mapping works for all timeframes
- Context respects 50k char limit
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-assistant-deep-upgrade/08-01-SUMMARY.md`
</output>
