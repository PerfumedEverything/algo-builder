---
phase: 08-ai-assistant-deep-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/server/providers/ai/deepseek-provider.ts
  - src/app/api/ai/chat/route.ts
  - src/server/providers/ai/index.ts
autonomous: true
requirements: [AIUP-01, AIUP-05]

must_haves:
  truths:
    - "AI uses deepseek-reasoner for analysis queries, producing reasoning_content shown as thinking indicator"
    - "AI uses deepseek-chat with tool_calls only when user explicitly asks to create a strategy"
    - "AI responses stream character by character via SSE from /api/ai/chat endpoint"
    - "Route Handler validates auth before processing — returns 401 for unauthenticated requests"
    - "reasoning_content is stripped from message history before sending to API (prevents 400 error)"
  artifacts:
    - path: "src/server/providers/ai/deepseek-provider.ts"
      provides: "Extended provider with chatWithThinking async generator and model routing"
      contains: "deepseek-reasoner"
    - path: "src/app/api/ai/chat/route.ts"
      provides: "SSE streaming endpoint for AI chat"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/ai/chat/route.ts"
      to: "src/server/providers/ai/deepseek-provider.ts"
      via: "chatWithThinking method call"
      pattern: "chatWithThinking"
    - from: "src/app/api/ai/chat/route.ts"
      to: "src/server/services/ai-context-service.ts"
      via: "AiContextService.assembleContext for enriched context"
      pattern: "assembleContext"
---

<objective>
Implement DeepSeek thinking mode with model routing and SSE streaming endpoint.

Purpose: This is the core backend upgrade — switching from blocking Server Action to streaming Route Handler, and from deepseek-chat to deepseek-reasoner for analysis. The provider now has two modes: analysis (reasoner, streaming, no tools) and strategy creation (chat, tools, blocking).

Output: Extended DeepSeekProvider with chatWithThinking generator, POST /api/ai/chat SSE endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-RESEARCH.md
@.planning/phases/08-ai-assistant-deep-upgrade/08-01-SUMMARY.md
@src/server/providers/ai/types.ts
@src/server/providers/ai/deepseek-provider.ts
@src/server/providers/ai/index.ts
@src/server/actions/helpers.ts
@src/core/config/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DeepSeekProvider with chatWithThinking and model routing</name>
  <files>
    src/server/providers/ai/deepseek-provider.ts,
    src/server/providers/ai/index.ts
  </files>
  <read_first>
    src/server/providers/ai/deepseek-provider.ts,
    src/server/providers/ai/types.ts,
    src/server/providers/ai/index.ts,
    src/core/config/env.ts,
    src/server/actions/strategy-actions.ts
  </read_first>
  <action>
    1. In `deepseek-provider.ts`, add an `async *chatWithThinking(messages, forceCreate?)` method implementing AiProvider interface:
       - If `forceCreate` is true OR last user message contains strategy creation keywords ("создай", "применить", "давай", "да, создай", "окей, создавай"):
         - Use `deepseek-chat` model WITH `tools: [generateStrategyTool]`
         - Non-streaming call (tool calls don't stream well)
         - If tool_call returned: yield `{ type: "strategy", content: JSON.stringify(parsed) }` then yield `{ type: "done", content: "" }`
         - If no tool_call: yield `{ type: "content", content: choice.content }` then yield done
       - Otherwise (analysis/discussion path):
         - Use `deepseek-reasoner` model, NO tools array, NO temperature
         - Stream: `stream: true`
         - For each chunk: if `delta.reasoning_content` -> yield `{ type: "thinking", content: delta.reasoning_content }`
         - If `delta.content` -> yield `{ type: "content", content: delta.content }`
         - After loop: yield `{ type: "done", content: "" }`

    2. CRITICAL: When building apiMessages from chat history, STRIP any `reasoning_content` or `thinkingContent` fields — DeepSeek API returns 400 if reasoning_content is in input messages. Only pass `{ role, content }`.

    3. Add strategy-creation keyword detection method `private needsToolCall(messages: AiChatMessage[], forceCreate?: boolean): boolean` — checks last user message for Russian approval keywords.

    4. Update CHAT_SYSTEM_PROMPT to include a new instruction block at the top:
       ```
       Ты теперь используешь режим "мышления" — перед ответом ты глубоко анализируешь данные.
       Если тебе передан контекст с рыночными данными, портфелем или фундаменталом — используй их в анализе.
       Если видишь проблемы в портфеле (концентрация >40%, высокая корреляция) — предупреди пользователя.
       ```

    5. Keep existing `generateStrategy()` and `chatAboutStrategy()` methods unchanged (backward compat). The new `chatWithThinking` is additive.

    6. In `src/server/providers/ai/index.ts` — ensure `getAiProvider()` factory returns the provider instance. If it currently returns just `AiProvider`, the interface now includes `chatWithThinking` — no change needed if already typed correctly.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "chatWithThinking" src/server/providers/ai/deepseek-provider.ts
    - grep -q "deepseek-reasoner" src/server/providers/ai/deepseek-provider.ts
    - grep -q "reasoning_content" src/server/providers/ai/deepseek-provider.ts
    - grep -q "needsToolCall" src/server/providers/ai/deepseek-provider.ts
    - TypeScript compiles without errors (npx tsc --noEmit)
  </acceptance_criteria>
  <done>
    DeepSeekProvider has chatWithThinking async generator. Analysis queries use deepseek-reasoner (streaming, no tools). Strategy creation uses deepseek-chat with tool_calls. reasoning_content stripped from history. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SSE streaming Route Handler at /api/ai/chat</name>
  <files>
    src/app/api/ai/chat/route.ts
  </files>
  <read_first>
    src/server/providers/ai/deepseek-provider.ts,
    src/server/providers/ai/types.ts,
    src/server/providers/ai/index.ts,
    src/server/services/ai-context-service.ts,
    src/server/actions/helpers.ts,
    src/lib/supabase/server.ts,
    src/app/api/prices/stream/route.ts
  </read_first>
  <action>
    1. Create `src/app/api/ai/chat/route.ts` — a POST Route Handler:

    2. Auth check at the top:
       - Import `createClient` from `@/lib/supabase/server`
       - Call `supabase.auth.getUser()` — if no user, return `new Response("Unauthorized", { status: 401 })`
       - Get userId from User table (same pattern as helpers.ts getCurrentUserId but without "use server")

    3. Parse request body: `{ messages: AiChatMessage[], context?: { ticker?: string; timeframe?: string; figi?: string }, forceCreate?: boolean }`

    4. If context.ticker provided, call `AiContextService.assembleContext({ ticker, timeframe, userId, figi })` and prepend as system message.

    5. Get provider via `getAiProvider()` factory.

    6. Call `provider.chatWithThinking(enrichedMessages, forceCreate)` — this returns an async generator.

    7. Create a ReadableStream that:
       - For each yielded AiStreamChunk, encode as SSE: `data: ${JSON.stringify(chunk)}\n\n`
       - After generator completes, send `data: [DONE]\n\n` and close controller

    8. Return `new Response(readable, { headers: { "Content-Type": "text/event-stream", "Cache-Control": "no-cache, no-transform", "Connection": "keep-alive" } })`

    9. Wrap entire handler in try/catch — on error, return `new Response(JSON.stringify({ error: message }), { status: 500 })`

    10. Max 150 lines per file rule — the handler should be well under this.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - test -f src/app/api/ai/chat/route.ts
    - grep -q "text/event-stream" src/app/api/ai/chat/route.ts
    - grep -q "ReadableStream" src/app/api/ai/chat/route.ts
    - grep -q "chatWithThinking" src/app/api/ai/chat/route.ts
    - grep -q "assembleContext" src/app/api/ai/chat/route.ts
    - grep -q "auth" src/app/api/ai/chat/route.ts
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>
    POST /api/ai/chat endpoint streams AI responses as SSE. Auth validated. Context enriched with market data when ticker provided. Thinking chunks sent as type:"thinking", content chunks as type:"content", strategy as type:"strategy".
  </done>
</task>

</tasks>

<verification>
npx tsc --noEmit
test -f src/app/api/ai/chat/route.ts && echo "PASS: Route handler exists"
grep -q "deepseek-reasoner" src/server/providers/ai/deepseek-provider.ts && echo "PASS: Reasoner model used"
grep -q "text/event-stream" src/app/api/ai/chat/route.ts && echo "PASS: SSE headers set"
</verification>

<success_criteria>
- DeepSeekProvider routes analysis to deepseek-reasoner, strategy creation to deepseek-chat
- /api/ai/chat returns SSE stream with thinking + content chunks
- Auth check prevents unauthorized access
- AiContextService enriches the prompt when ticker context provided
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/08-ai-assistant-deep-upgrade/08-02-SUMMARY.md`
</output>
