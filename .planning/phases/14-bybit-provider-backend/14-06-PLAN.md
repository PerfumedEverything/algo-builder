---
phase: 14-bybit-provider-backend
plan: 06
type: execute
wave: 3
depends_on: [14-01, 14-04]
files_modified:
  - src/server/providers/ai/ai-prompts.ts
  - src/server/providers/ai/ai-crypto-prompts.ts
  - src/components/settings/broker-switch.tsx
  - src/server/actions/settings-actions.ts
autonomous: true
requirements: [BYBIT-11, BYBIT-12, BYBIT-13]

must_haves:
  truths:
    - "User can switch between T-Invest and Bybit in settings"
    - "Switching broker updates brokerType in DB"
    - "When brokerType is BYBIT: crypto pairs shown, 24/7 mode, 8 decimal precision, USDT currency"
    - "AI prompts include crypto-specific guidance when user has BYBIT broker"
    - "AI prompt selection is conditional on brokerType"
  artifacts:
    - path: "src/server/providers/ai/ai-crypto-prompts.ts"
      provides: "Crypto-specific AI prompts"
      contains: "BTCUSDT"
    - path: "src/components/settings/broker-switch.tsx"
      provides: "T-Invest / Bybit toggle in settings"
      contains: "brokerType"
    - path: "src/server/actions/settings-actions.ts"
      provides: "Server action for broker switch"
      contains: "saveBrokerType"
  key_links:
    - from: "src/components/settings/broker-switch.tsx"
      to: "src/server/actions/settings-actions.ts"
      via: "switchBrokerAction server action"
      pattern: "switchBrokerAction"
    - from: "src/server/providers/ai/ai-prompts.ts"
      to: "src/server/providers/ai/ai-crypto-prompts.ts"
      via: "conditional prompt selection"
      pattern: "CRYPTO_SYSTEM_PROMPT"
---

<objective>
Add multi-broker UI switch and crypto-adapted AI prompts.

Purpose: Per D-05 — one broker per user via global switch. Per D-09 — AI prompts adapted for crypto with appropriate ticker mappings, risk profiles, and 24/7 awareness. Per D-04/D-12 — Bybit mode shows crypto pairs with USDT and 8 decimals.
Output: Broker switch component, settings action, crypto AI prompts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-bybit-provider-backend/14-CONTEXT.md
@.planning/phases/14-bybit-provider-backend/14-RESEARCH.md
@.planning/phases/14-bybit-provider-backend/14-01-SUMMARY.md

<interfaces>
From src/server/repositories/broker-repository.ts (after Plan 01):
- saveBrokerType(userId, brokerType): Promise<void>
- saveBybitCredentials(userId, apiKey, apiSecret): Promise<void>
- getBrokerType(userId): Promise<string>
- BrokerSettingsRow.brokerType, .bybitApiKey, .bybitApiSecret

From src/server/providers/ai/ai-prompts.ts:
- SYSTEM_PROMPT — trading strategy assistant prompt
- INDICATOR_HINTS — indicator suggestions array
- RISK_PROFILES — risk profile descriptions
- Currently all RU-market focused (SBER, GAZP, LKOH, YNDX tickers)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crypto AI prompts and conditional selection</name>
  <files>src/server/providers/ai/ai-crypto-prompts.ts, src/server/providers/ai/ai-prompts.ts</files>
  <read_first>
    - src/server/providers/ai/ai-prompts.ts
  </read_first>
  <action>
**Create `src/server/providers/ai/ai-crypto-prompts.ts`** (per D-09):

```typescript
export const CRYPTO_SYSTEM_PROMPT = `You are a trading strategy assistant for the AculaTrade platform (Bybit crypto).
Your task is to generate complete trading strategy configurations based on user requests.
Always use the create_strategy function to return the result.
Generate a descriptive Russian name and description for each strategy.
Extract the instrument from the user's request:
- "биткоин" / "BTC" → "BTCUSDT"
- "эфир" / "ETH" → "ETHUSDT"
- "солана" / "SOL" → "SOLUSDT"
- "рипл" / "XRP" → "XRPUSDT"
- "дож" / "DOGE" → "DOGEUSDT"
- "кардано" / "ADA" → "ADAUSDT"
instrumentType is always "CRYPTO". Market trades 24/7 — no session hours.
Crypto is more volatile than stocks — adjust risk parameters accordingly:
- Use wider stop-losses (3-10% for swing, 1-3% for scalping)
- Consider higher timeframes for less noise (4h, 1d preferred for swing)
- Funding rate awareness: perpetual futures have funding every 8h
- Volume spikes are more significant in crypto — use VOLUME indicator
Available indicators: SMA, EMA, RSI, MACD, BOLLINGER, PRICE, VOLUME, PRICE_CHANGE, SUPPORT, RESISTANCE, ATR, STOCHASTIC, VWAP, WILLIAMS_R.
For BETWEEN conditions use both value (lower bound) and valueTo (upper bound).
Respond in Russian for name and description fields.`

export const CRYPTO_INDICATOR_HINTS = [
  "Используй RSI с периодом 7 для крипто-скальпинга (высокая волатильность)",
  "Попробуй EMA crossover 9/21 на 4h таймфрейме — классика крипто",
  "Используй Bollinger Bands squeeze для ловли прорывов волатильности",
  "Примени VWAP для интрадей-стратегий на крипто-фьючерсах",
  "Используй ATR для адаптивных стоп-лоссов (крипто волатильнее акций в 3-5 раз)",
  "Попробуй RSI divergence на 1h — хорошо работает на BTC/ETH",
  "Используй Volume + MACD комбинацию для подтверждения трендов",
  "Примени стратегию mean reversion на BTC через Bollinger + RSI",
]

export const CRYPTO_RISK_PROFILES = [
  "Консервативный крипто: SL 3-5%, TP 8-15%, позиция 5-10%",
  "Умеренный крипто: SL 5-8%, TP 15-25%, позиция 10-15%",
  "Агрессивный крипто: SL 8-15%, TP 25-50%, позиция 15-25%",
  "Крипто-скальпинг: SL 1-2%, TP 2-5%, позиция 20-30%, 5m таймфрейм",
  "Крипто-свинг: SL 5-10%, TP 20-40%, позиция 10-15%, 4h-1d таймфрейм",
]
```

**Update `src/server/providers/ai/ai-prompts.ts`:**
- Add a function `getSystemPrompt(brokerType: string): string` that returns `CRYPTO_SYSTEM_PROMPT` for `'BYBIT'` and `SYSTEM_PROMPT` for `'TINKOFF'`
- Add a function `getIndicatorHints(brokerType: string): string[]` — returns crypto or RU hints
- Add a function `getRiskProfiles(brokerType: string): string[]` — returns crypto or RU profiles
- Import from `./ai-crypto-prompts`
- Export these new functions alongside existing constants (backward compat)

**Update all places that use SYSTEM_PROMPT directly** — search for imports of SYSTEM_PROMPT and check if they should use `getSystemPrompt(brokerType)` instead. The brokerType should be fetched from user settings. In AI chat route/action, pass the brokerType from the user's settings to select the right prompt.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ls src/server/providers/ai/ai-crypto-prompts.ts
    - grep -q 'CRYPTO_SYSTEM_PROMPT' src/server/providers/ai/ai-crypto-prompts.ts
    - grep -q 'BTCUSDT' src/server/providers/ai/ai-crypto-prompts.ts
    - grep -q 'getSystemPrompt' src/server/providers/ai/ai-prompts.ts
    - grep -q 'CRYPTO_RISK_PROFILES' src/server/providers/ai/ai-crypto-prompts.ts
  </acceptance_criteria>
  <done>Crypto-specific AI prompts created. Conditional prompt selection based on brokerType. Crypto tickers, risk profiles, and 24/7 awareness included.</done>
</task>

<task type="auto">
  <name>Task 2: Broker switch UI + settings action</name>
  <files>src/components/settings/broker-switch.tsx, src/server/actions/settings-actions.ts</files>
  <read_first>
    - src/server/actions/settings-actions.ts
    - src/server/repositories/broker-repository.ts
  </read_first>
  <action>
**Create or update `src/server/actions/settings-actions.ts`:**
Add server action `switchBrokerAction`:
```typescript
export const switchBrokerAction = async (brokerType: string): Promise<ApiResponse<void>> => {
  try {
    const userId = await getCurrentUserId()
    if (brokerType !== 'TINKOFF' && brokerType !== 'BYBIT') {
      return { error: 'Неверный тип брокера' }
    }
    const repo = new BrokerRepository()
    await repo.saveBrokerType(userId, brokerType)
    return { data: undefined }
  } catch (error) {
    return { error: error instanceof Error ? error.message : 'Ошибка переключения брокера' }
  }
}
```

Add server action `connectBybitAction`:
```typescript
export const connectBybitAction = async (apiKey: string, apiSecret: string): Promise<ApiResponse<void>> => {
  try {
    const userId = await getCurrentUserId()
    // Validate by attempting connection
    const provider = new BybitProvider()
    await provider.connect(`${apiKey}:${apiSecret}`)
    await provider.getAccounts()  // Verify credentials work
    await provider.disconnect()

    const repo = new BrokerRepository()
    await repo.saveBybitCredentials(userId, apiKey, apiSecret)
    return { data: undefined }
  } catch (error) {
    return { error: error instanceof Error ? error.message : 'Ошибка подключения к Bybit' }
  }
}
```

**Create `src/components/settings/broker-switch.tsx`** (per D-05, BYBIT-11):
- Component `BrokerSwitch` with props: `{ currentBroker: string }`
- Two cards/buttons: "T-Invest" and "Bybit" — styled with shadcn/ui Card or ToggleGroup
- Active broker highlighted (primary color)
- Clicking inactive broker calls `switchBrokerAction(brokerType)`
- Show toast on success/error via sonner
- When Bybit selected and no credentials saved: show form for API Key + API Secret inputs
- Bybit card shows: "Крипто: BTCUSDT, ETHUSDT... • 24/7 • Testnet" subtitle (per D-04, D-12)
- T-Invest card shows: "Акции, облигации, ETF • MOEX • Рубли" subtitle
- Use Lucide icons: `Bitcoin` for Bybit, `Building2` for T-Invest (or similar)

**Integration:** Find the settings page (search for settings or profile page) and add `<BrokerSwitch currentBroker={settings.brokerType} />`. The settings page should read brokerType from user settings server-side.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ls src/components/settings/broker-switch.tsx
    - grep -q 'switchBrokerAction' src/server/actions/settings-actions.ts
    - grep -q 'connectBybitAction' src/server/actions/settings-actions.ts
    - grep -q 'BrokerSwitch' src/components/settings/broker-switch.tsx
    - grep -q 'BYBIT' src/components/settings/broker-switch.tsx
    - grep -q 'TINKOFF' src/components/settings/broker-switch.tsx
    - grep -q 'getCurrentUserId' src/server/actions/settings-actions.ts
  </acceptance_criteria>
  <done>Broker switch component shows T-Invest/Bybit toggle. Server actions handle broker type switching and Bybit credential validation. Crypto-specific display info (24/7, USDT, testnet) shown on Bybit card.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Broker switch component exists and renders two broker options
- AI prompts conditionally select crypto or RU based on brokerType
- Settings actions validate credentials before saving
</verification>

<success_criteria>
User can switch between T-Invest and Bybit in settings (BYBIT-11). Bybit mode shows crypto context: pairs, 24/7, USDT (BYBIT-12). AI prompts adapted for crypto with appropriate tickers and risk profiles (BYBIT-13).
</success_criteria>

<output>
After completion, create `.planning/phases/14-bybit-provider-backend/14-06-SUMMARY.md`
</output>
