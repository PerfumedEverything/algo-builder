---
phase: 14-bybit-provider-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/types/broker.ts
  - src/core/types/strategy.ts
  - src/server/providers/broker/types.ts
  - src/server/providers/broker/index.ts
  - src/server/repositories/broker-repository.ts
  - src/server/services/broker-service.ts
autonomous: true
requirements: [BYBIT-01, BYBIT-12, BYBIT-14]

must_haves:
  truths:
    - "BrokerAccount.type includes BYBIT"
    - "PortfolioPosition.instrumentType includes CRYPTO"
    - "BrokerProvider interface has placeOrder and cancelOrder methods"
    - "getBrokerProvider(userId) reads brokerType from DB and returns correct provider"
    - "BrokerService methods pass userId to getBrokerProvider"
    - "User table has brokerType, bybitApiKey, bybitApiSecret columns"
  artifacts:
    - path: "src/core/types/broker.ts"
      provides: "Extended BrokerAccount and PortfolioPosition types with BYBIT/CRYPTO"
      contains: "BYBIT"
    - path: "src/server/providers/broker/types.ts"
      provides: "BrokerProvider interface with placeOrder + cancelOrder"
      contains: "placeOrder"
    - path: "src/server/providers/broker/index.ts"
      provides: "Async getBrokerProvider(userId) factory"
      contains: "getBrokerProvider"
    - path: "src/server/repositories/broker-repository.ts"
      provides: "brokerType + Bybit credentials read/write"
      contains: "bybitApiKey"
    - path: "src/server/services/broker-service.ts"
      provides: "Refactored BrokerService using async factory per method"
      contains: "getBrokerProvider"
  key_links:
    - from: "src/server/providers/broker/index.ts"
      to: "src/server/repositories/broker-repository.ts"
      via: "getBrokerType(userId) call"
      pattern: "getBrokerType"
    - from: "src/server/services/broker-service.ts"
      to: "src/server/providers/broker/index.ts"
      via: "await getBrokerProvider(userId)"
      pattern: "await getBrokerProvider"
---

<objective>
Extend types, DB schema, factory, and service layer to support multi-broker architecture.

Purpose: Foundation for BybitBrokerProvider — all downstream code depends on these type expansions and the async factory pattern.
Output: Extended types (BYBIT account, CRYPTO instrument, placeOrder/cancelOrder), DB migration for User.brokerType/bybitApiKey/bybitApiSecret, async getBrokerProvider(userId) factory, refactored BrokerService.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bybit-provider-backend/14-CONTEXT.md
@.planning/phases/14-bybit-provider-backend/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types and BrokerProvider interface</name>
  <files>src/core/types/broker.ts, src/core/types/strategy.ts, src/server/providers/broker/types.ts</files>
  <read_first>
    - src/core/types/broker.ts
    - src/core/types/strategy.ts
    - src/server/providers/broker/types.ts
  </read_first>
  <action>
**src/core/types/broker.ts:**
- `BrokerAccount.type`: change union from `"TINKOFF" | "SANDBOX"` to `"TINKOFF" | "SANDBOX" | "BYBIT"` (per D-10)
- `PortfolioPosition.instrumentType`: change union from `"STOCK" | "BOND" | "CURRENCY" | "FUTURES" | "ETF"` to `"STOCK" | "BOND" | "CURRENCY" | "FUTURES" | "ETF" | "CRYPTO"` (per D-10)
- `BrokerInstrument.type`: same — add `"CRYPTO"` to the union
- Add new type `PlaceOrderParams`:
  ```
  export type PlaceOrderParams = {
    symbol: string
    side: "BUY" | "SELL"
    orderType: "MARKET" | "LIMIT"
    quantity: number
    price?: number
    accountId: string
  }
  ```

**src/core/types/strategy.ts:**
- Add `"CRYPTO"` to `InstrumentType` if it's a separate type. If `InstrumentType` is not explicitly defined, check where instrumentType is used for strategy creation and ensure CRYPTO is accepted. Read the file fully to decide.

**src/server/providers/broker/types.ts:**
- Add `placeOrder(params: PlaceOrderParams): Promise<string>` to `BrokerProvider` interface (returns orderId)
- Add `cancelOrder(orderId: string, symbol: string): Promise<void>` to `BrokerProvider` interface
- Import `PlaceOrderParams` from `@/core/types`
- Add export of `PlaceOrderParams` from `@/core/types/index.ts` if barrel exists
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"BYBIT"' src/core/types/broker.ts
    - grep -q '"CRYPTO"' src/core/types/broker.ts
    - grep -q 'PlaceOrderParams' src/core/types/broker.ts
    - grep -q 'placeOrder' src/server/providers/broker/types.ts
    - grep -q 'cancelOrder' src/server/providers/broker/types.ts
  </acceptance_criteria>
  <done>BrokerAccount.type includes BYBIT, PortfolioPosition.instrumentType includes CRYPTO, BrokerProvider interface has placeOrder + cancelOrder, PlaceOrderParams type exported</done>
</task>

<task type="auto">
  <name>Task 2: DB migration + repository + factory + BrokerService refactor</name>
  <files>src/server/repositories/broker-repository.ts, src/server/providers/broker/index.ts, src/server/services/broker-service.ts, src/server/providers/broker/tinkoff-provider.ts</files>
  <read_first>
    - src/server/repositories/broker-repository.ts
    - src/server/providers/broker/index.ts
    - src/server/services/broker-service.ts
    - src/server/providers/broker/tinkoff-provider.ts
  </read_first>
  <action>
**DB migration (Supabase SQL):**
Run via Supabase dashboard or migration file:
```sql
ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "brokerType" TEXT DEFAULT 'TINKOFF';
ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "bybitApiKey" TEXT;
ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "bybitApiSecret" TEXT;
```
Per D-05: brokerType is TINKOFF by default. Per research Pitfall 2: separate columns for Bybit credentials.

**src/server/repositories/broker-repository.ts:**
- Add `brokerType` to the `.select()` in `getSettings()`: `.select("brokerToken, brokerAccountId, brokerType, bybitApiKey, bybitApiSecret")`
- Update `BrokerSettingsRow` type: add `bybitApiKey: string | null`, `bybitApiSecret: string | null`
- Remove hardcoded `brokerType: "TINKOFF"` — read from DB: `brokerType: data.brokerType ?? "TINKOFF"`
- Add method `getBrokerType(userId: string): Promise<string>` that queries only `brokerType`
- Add method `saveBybitCredentials(userId: string, apiKey: string, apiSecret: string): Promise<void>` — updates `bybitApiKey`, `bybitApiSecret`, and sets `brokerType` to `"BYBIT"`
- Add method `saveBrokerType(userId: string, brokerType: string): Promise<void>`

**src/server/providers/broker/index.ts:**
- Make `getBrokerProvider` async and accept `userId`: `export const getBrokerProvider = async (userId: string): Promise<BrokerProvider>`
- Import `BrokerRepository`
- Inside: read `brokerType` via `new BrokerRepository().getBrokerType(userId)`
- If `brokerType === 'BYBIT'` return `new BybitProvider()` (import from `./bybit-provider` — will exist in Plan 04, for now add a placeholder comment and keep returning TinkoffProvider for both, so TypeScript compiles)
- Actually, to avoid import errors: add a conditional dynamic import or simply keep `TinkoffProvider` for now with a TODO. Better: create a minimal stub `bybit-provider.ts` file (just the class declaration implementing BrokerProvider with all methods throwing `new Error('Not implemented — Plan 04')`)

**src/server/providers/broker/tinkoff-provider.ts:**
- Add `placeOrder` and `cancelOrder` stub methods that throw `new Error('Order placement not implemented for T-Invest in this phase')` — fulfills interface contract.

**src/server/services/broker-service.ts:**
- Remove `private provider = getBrokerProvider()` class-level field
- In EVERY method: replace `this.provider` with local `const provider = await getBrokerProvider(userId)` at the start of the method body
- `connect()`: `const provider = await getBrokerProvider(userId)` then `await provider.connect(token)`
- `getAccounts()`: same pattern
- `getPortfolio()`: same pattern
- `getInstruments()`: same pattern
- `getCurrentPrice()`: same pattern
- `getInstrumentPrice()`: same pattern
- `getCandles()`: same pattern
- `sandboxPayIn()`: same pattern
- `getConnectionStatus()`: does not use provider, no change needed
- `disconnect()`: needs provider, same pattern
- `selectAccount()`: does not use provider, no change needed
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'bybitApiKey' src/server/repositories/broker-repository.ts
    - grep -q 'bybitApiSecret' src/server/repositories/broker-repository.ts
    - grep -q 'getBrokerType' src/server/repositories/broker-repository.ts
    - grep -q 'async.*userId.*string.*BrokerProvider' src/server/providers/broker/index.ts
    - grep -q 'await getBrokerProvider(userId)' src/server/services/broker-service.ts
    - grep -q 'placeOrder' src/server/providers/broker/tinkoff-provider.ts
    - grep -q 'cancelOrder' src/server/providers/broker/tinkoff-provider.ts
    - npx tsc --noEmit exits with 0 or only pre-existing errors
  </acceptance_criteria>
  <done>DB columns added, BrokerRepository reads brokerType/bybitCredentials from DB, getBrokerProvider is async with userId param, BrokerService refactored to create provider per method call, TinkoffProvider has placeOrder/cancelOrder stubs, TypeScript compiles</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no new errors)
- `grep -r "getBrokerProvider()" src/ --include="*.ts" | grep -v "userId"` returns no hits (all callers updated)
- BrokerProvider interface has 10 methods (8 original + placeOrder + cancelOrder)
</verification>

<success_criteria>
All type expansions in place. DB migration applied. Factory is async, user-aware. BrokerService refactored. TypeScript compiles clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-bybit-provider-backend/14-01-SUMMARY.md`
</output>
