---
phase: 02.1-terminal-v2
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/components/terminal/tradingview-widget.tsx
  - src/components/terminal/price-bar.tsx
  - src/components/terminal/order-book.tsx
  - src/components/terminal/positions-panel.tsx
  - src/components/terminal/trade-history-panel.tsx
  - src/components/terminal/top-movers.tsx
  - src/app/(dashboard)/terminal/page.tsx
autonomous: false
requirements: [TERM-02, TERM-04]
deferred_requirements_note: "TERM-03 (AlgoPack) and TERM-05 (multiple chart layouts) are deferred per CONTEXT.md decisions"

must_haves:
  truths:
    - "User sees TradingView Advanced Chart widget with full indicator/drawing tool set for MOEX instruments"
    - "User sees realtime price bar (ticker, price, change %, H/L, volume, bid/ask) updating live"
    - "User sees order book with bid/ask depth and spread next to the chart"
    - "User sees their open positions with realtime P&L below the chart"
    - "User sees trade history for the selected instrument"
    - "User sees top gainers and top losers across all MOEX instruments"
  artifacts:
    - path: "src/components/terminal/tradingview-widget.tsx"
      provides: "TradingView Advanced Chart widget wrapper"
      exports: ["TradingViewWidget"]
    - path: "src/components/terminal/price-bar.tsx"
      provides: "Realtime price bar with ticker info"
      exports: ["PriceBar"]
    - path: "src/components/terminal/order-book.tsx"
      provides: "Order book bid/ask table with spread"
      exports: ["OrderBook"]
    - path: "src/components/terminal/positions-panel.tsx"
      provides: "Open positions with realtime P&L"
      exports: ["PositionsPanel"]
    - path: "src/components/terminal/trade-history-panel.tsx"
      provides: "Trade history for selected instrument"
      exports: ["TradeHistoryPanel"]
    - path: "src/components/terminal/top-movers.tsx"
      provides: "Top gainers and losers tables"
      exports: ["TopMovers"]
    - path: "src/app/(dashboard)/terminal/page.tsx"
      provides: "Complete terminal page with all panels per wireframe layout"
  key_links:
    - from: "src/app/(dashboard)/terminal/page.tsx"
      to: "src/hooks/use-price-stream.ts"
      via: "usePriceStream hook for realtime updates"
      pattern: "usePriceStream"
    - from: "src/components/terminal/tradingview-widget.tsx"
      to: "TradingView CDN"
      via: "script injection in useEffect"
      pattern: "embed-widget-advanced-chart"
    - from: "src/components/terminal/order-book.tsx"
      to: "src/server/actions/terminal-actions.ts"
      via: "getOrderBookAction called from page"
      pattern: "getOrderBookAction"
    - from: "src/components/terminal/top-movers.tsx"
      to: "src/server/actions/terminal-actions.ts"
      via: "getTopMoversAction"
      pattern: "getTopMoversAction"
---

<objective>
Build the complete Terminal v2 page UI: TradingView widget, price bar, order book, positions panel, trade history, top movers — all wired to the data layer from Plan 01.

Purpose: Replace the existing simple chart terminal with a professional trading terminal per Anton's wireframe (D-03).
Output: 6 new terminal components + fully rewritten terminal page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-terminal-v2/02.1-CONTEXT.md
@.planning/phases/02.1-terminal-v2/02.1-RESEARCH.md
@.planning/phases/02.1-terminal-v2/02.1-01-SUMMARY.md

<interfaces>
<!-- Types and functions from Plan 01 that this plan consumes -->

From src/core/types/terminal.ts (created in Plan 01):
```typescript
export type OrderBookLevel = { price: number; quantity: number }
export type OrderBookData = { bids: OrderBookLevel[]; asks: OrderBookLevel[]; spread: number }
export type TopMover = { ticker: string; shortName: string; price: number; changePct: number; volume: number; high: number; low: number }
export type PriceUpdate = { instrumentId: string; price: number; updatedAt: number }
```

From src/hooks/use-price-stream.ts (created in Plan 01):
```typescript
export function usePriceStream(): Map<string, PriceUpdate>
```

From src/server/actions/terminal-actions.ts (created in Plan 01):
```typescript
export const getOrderBookAction = async (figi: string, depth?: number): Promise<ApiResponse<OrderBookData>>
export const getTopMoversAction = async (topN?: number): Promise<ApiResponse<{ gainers: TopMover[]; losers: TopMover[] }>>
export const getOperationsByTickerAction = async (ticker: string): Promise<ApiResponse<PositionOperation[]>>
```

From src/core/types/broker.ts (existing):
```typescript
export type BrokerInstrument = { figi: string; ticker: string; name: string; type: string; currency: string; lot: number }
export type PortfolioPosition = { instrumentId: string; ticker: string; name: string; quantity: number; averagePrice: number; currentPrice: number; expectedYield: number; ... }
export type Portfolio = { totalAmount: number; positions: PortfolioPosition[]; ... }
```

From src/server/actions/broker-actions.ts (existing):
```typescript
export const getPortfolioAction = async (): Promise<ApiResponse<Portfolio | null>>
```

From src/components/shared/instrument-select.tsx (existing):
```typescript
// InstrumentSelect component with onInstrumentSelect callback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart and data display components (TradingView widget, price bar, order book)</name>
  <files>src/components/terminal/tradingview-widget.tsx, src/components/terminal/price-bar.tsx, src/components/terminal/order-book.tsx</files>
  <read_first>
    - src/core/types/terminal.ts (types from Plan 01)
    - src/core/types/broker.ts (BrokerInstrument type)
    - src/components/ui/table.tsx (shadcn Table component — use for order book)
    - src/components/ui/card.tsx (shadcn Card component)
    - src/components/ui/skeleton.tsx (Skeleton for loading states)
    - .planning/phases/02.1-terminal-v2/02.1-RESEARCH.md (Pattern 1: TradingView widget, Pattern 3: Order book)
  </read_first>
  <action>
Create 3 components in `src/components/terminal/`. All use `"use client"` directive, arrow function exports, Tailwind utility classes, shadcn/ui components.

**1. tradingview-widget.tsx** — per D-01, D-02:
- Props: `{ symbol: string; theme?: "light" | "dark" }`
- `useEffect` with `[symbol, theme]` deps: clear container innerHTML, create `<script>` tag with src `https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js`, set `script.innerHTML` to JSON config:
  ```json
  { "symbol": symbol, "interval": "D", "timezone": "Europe/Moscow", "theme": theme ?? "dark", "style": "1", "locale": "ru", "allow_symbol_change": false, "calendar": false, "hide_top_toolbar": false, "support_host": "https://www.tradingview.com", "width": "100%", "height": "100%" }
  ```
- Use `useRef<HTMLDivElement>` for container
- Cleanup: clear innerHTML on unmount
- Container div: `className="tradingview-widget-container h-full w-full"`
- Wrap in div with `className="h-[600px] rounded-lg border border-border overflow-hidden"`

**2. price-bar.tsx** — per D-07, D-08:
- Props: `{ instrument: BrokerInstrument | null; priceUpdate: PriceUpdate | undefined; orderBook: OrderBookData | null }`
- Display in a horizontal bar (flex row, gap-6, items-center):
  - Ticker + name (bold)
  - Current price (from priceUpdate?.price, format with `toLocaleString("ru-RU")`)
  - Change % (placeholder "---" — we don't have change% in PriceUpdate, show raw price for now)
  - High/Low (from orderBook: "---" if null)
  - Volume (placeholder "---")
  - Bid (orderBook?.bids[0]?.price) / Ask (orderBook?.asks[0]?.price)
- Green text if price exists, muted if no data
- Background: `bg-card border border-border rounded-lg px-6 py-3`

**3. order-book.tsx** — per D-05, D-06:
- Props: `{ data: OrderBookData | null; loading: boolean }`
- Simple table using shadcn Table component:
  - Header: "Цена" | "Объем"
  - Asks section (top, red/sell side): display `data.asks` reversed (highest at top, lowest near spread) — each row with red-tinted background `bg-red-500/10`, text `text-red-400`
  - Separator row showing spread: `Спред: ${data.spread.toFixed(2)}`
  - Bids section (bottom, green/buy side): display `data.bids` — each row with green-tinted background `bg-green-500/10`, text `text-green-400`
- Show depth: 10 levels per side (Claude's discretion per CONTEXT)
- Wrap in Card with header "Стакан"
- Loading state: Skeleton rows
- Price format: `toLocaleString("ru-RU", { minimumFractionDigits: 2 })`
- Quantity format: plain number with locale
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>TradingView widget wrapper, price bar, and order book components created with correct types, shadcn/ui usage, and loading states.</done>
</task>

<task type="auto">
  <name>Task 2: Portfolio and market components (positions panel, trade history, top movers)</name>
  <files>src/components/terminal/positions-panel.tsx, src/components/terminal/trade-history-panel.tsx, src/components/terminal/top-movers.tsx</files>
  <read_first>
    - src/core/types/terminal.ts (TopMover, PriceUpdate types)
    - src/core/types/broker.ts (PortfolioPosition, PositionOperation types)
    - src/hooks/use-price-stream.ts (usePriceStream hook API)
    - src/components/ui/table.tsx (shadcn Table component)
    - src/components/ui/card.tsx (shadcn Card component)
    - src/components/ui/skeleton.tsx (Skeleton for loading states)
  </read_first>
  <action>
Create 3 components in `src/components/terminal/`. All use `"use client"` directive, arrow function exports, Tailwind utility classes, shadcn/ui components.

**1. positions-panel.tsx** — per D-10:
- Props: `{ positions: PortfolioPosition[]; priceUpdates: Map<string, PriceUpdate> }`
- shadcn Table: columns "Тикер", "Кол-во", "Ср. цена", "Текущая", "П/У", "П/У %"
- For each position:
  - If `priceUpdates.has(position.instrumentId)`: use realtime price, calculate P&L live as `(realtimePrice - averagePrice) * quantity`
  - Else: use `position.currentPrice` and `position.expectedYield`
  - Color P&L: green if positive, red if negative
- Wrap in Card with header "Открытые позиции"
- Empty state: "Нет открытых позиций"

**2. trade-history-panel.tsx** — per D-11:
- Props: `{ operations: PositionOperation[]; ticker: string; loading: boolean }`
- shadcn Table: columns "Дата", "Тип", "Цена", "Кол-во", "Сумма"
- Format date with `toLocaleDateString("ru-RU")`
- Type: "Покупка" (green) for BUY, "Продажа" (red) for SELL
- Wrap in Card with header `История сделок: ${ticker}`
- Empty state: "Нет сделок по инструменту"
- Loading: Skeleton rows

**3. top-movers.tsx** — per D-09:
- Props: `{ gainers: TopMover[]; losers: TopMover[]; loading: boolean }`
- Two side-by-side cards in a grid (grid-cols-2 gap-4):
  - Left card "Лидеры роста": table with "Тикер", "Цена", "Изменение %"
    - Change % in green with + prefix and arrow up icon (Lucide `TrendingUp`)
  - Right card "Лидеры падения": table with same columns
    - Change % in red with arrow down icon (Lucide `TrendingDown`)
- Ticker column: bold ticker + muted shortName below
- Loading: Skeleton
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Positions panel, trade history panel, and top movers components created with correct types, realtime P&L calculation, shadcn/ui tables, and loading states.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite terminal page with full layout per wireframe (D-03)</name>
  <files>src/app/(dashboard)/terminal/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/terminal/page.tsx (current page to be fully replaced)
    - src/components/terminal/tradingview-widget.tsx (Task 1 output)
    - src/components/terminal/price-bar.tsx (Task 1 output)
    - src/components/terminal/order-book.tsx (Task 1 output)
    - src/components/terminal/positions-panel.tsx (Task 2 output)
    - src/components/terminal/trade-history-panel.tsx (Task 2 output)
    - src/components/terminal/top-movers.tsx (Task 2 output)
    - src/hooks/use-price-stream.ts (Plan 01: realtime price hook)
    - src/server/actions/terminal-actions.ts (Plan 01: server actions)
    - src/server/actions/broker-actions.ts (getPortfolioAction)
    - src/components/shared/instrument-select.tsx (InstrumentSelect component)
    - .planning/phases/02.1-terminal-v2/02.1-CONTEXT.md (D-03 wireframe layout, D-04 instrument binding)
  </read_first>
  <action>
Completely rewrite `src/app/(dashboard)/terminal/page.tsx`. Remove ALL existing lightweight-charts code per D-02. New layout per D-03 wireframe:

```
Top:      [InstrumentSelect] [PriceBar ...........................]
Middle:   [TradingView Widget (left ~70%)  ] [OrderBook (right ~30%)]
Below:    [PositionsPanel (left 50%)       ] [TradeHistory (right 50%)]
Bottom:   [TopMovers (gainers left 50%)    ] [TopMovers (losers right 50%)]
```

Implementation:
1. `"use client"` directive
2. State:
   - `instrument: BrokerInstrument | null` — selected instrument
   - `ticker: string` — for InstrumentSelect controlled value
   - `orderBook: OrderBookData | null` — fetched on instrument change + 5s interval
   - `positions: PortfolioPosition[]` — fetched once on mount
   - `operations: PositionOperation[]` — fetched on instrument change
   - `topMovers: { gainers: TopMover[]; losers: TopMover[] }` — fetched on mount + 60s interval
   - Loading states for each panel

3. Call `usePriceStream()` at component top level — get full price Map

4. Data fetching:
   - On mount: `getPortfolioAction()` -> set positions, `getTopMoversAction()` -> set topMovers
   - On instrument change: `getOrderBookAction(instrument.figi)` -> set orderBook, `getOperationsByTickerAction(instrument.ticker)` -> set operations
   - `setInterval` for order book refresh every 5 seconds (if instrument selected)
   - `setInterval` for top movers refresh every 60 seconds
   - Clear intervals on unmount

5. Layout structure (Tailwind):
   - Outer: `<div className="flex flex-col gap-4 p-4 h-full">`
   - Row 1 (instrument select + price bar): `<div className="flex items-center gap-4">` — InstrumentSelect (w-64) + PriceBar (flex-1)
   - Row 2 (chart + order book): `<div className="flex gap-4">` — TradingView (flex-1, use `key={instrument?.ticker}` for remount per D-01 and Pitfall 1) + OrderBook (w-[320px])
   - Row 3 (positions + trade history): `<div className="grid grid-cols-2 gap-4">`
   - Row 4 (top movers): TopMovers component handles its own 2-column grid

6. Pass `priceUpdates` Map to PositionsPanel and extract single PriceUpdate for PriceBar via `priceUpdates.get(instrument?.figi)`

7. TradingView widget: `<TradingViewWidget key={instrument?.ticker ?? "empty"} symbol={instrument ? \`MOEX:${instrument.ticker}\` : "MOEX:SBER"} theme="dark" />`
   - Use `key` prop for remount on ticker change (per Research Pattern 1)
   - Default to MOEX:SBER when no instrument selected

8. Empty state when no instrument: show TradingView widget with default SBER, hide order book / positions / trade history panels, still show top movers

9. Per D-04: All blocks except top movers are tied to the selected instrument
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "TradingViewWidget" src/app/(dashboard)/terminal/page.tsx
    - grep -q "PriceBar" src/app/(dashboard)/terminal/page.tsx
    - grep -q "OrderBook" src/app/(dashboard)/terminal/page.tsx
    - grep -q "PositionsPanel" src/app/(dashboard)/terminal/page.tsx
    - grep -q "TradeHistoryPanel" src/app/(dashboard)/terminal/page.tsx
    - grep -q "TopMovers" src/app/(dashboard)/terminal/page.tsx
    - grep -q "usePriceStream" src/app/(dashboard)/terminal/page.tsx
    - grep -q "getOrderBookAction" src/app/(dashboard)/terminal/page.tsx
    - grep -q "getTopMoversAction" src/app/(dashboard)/terminal/page.tsx
    - grep -q "getOperationsByTickerAction" src/app/(dashboard)/terminal/page.tsx
    - grep -q "getPortfolioAction" src/app/(dashboard)/terminal/page.tsx
    - grep -rL "lightweight-charts" src/app/(dashboard)/terminal/page.tsx (must NOT contain lightweight-charts)
    - grep -q "MOEX:" src/app/(dashboard)/terminal/page.tsx
  </acceptance_criteria>
  <done>Terminal page fully rewritten with TradingView widget, realtime price bar, order book, positions with live P&L, trade history, top movers. Layout matches Anton's wireframe. No lightweight-charts references remain. All data connected via server actions and SSE price stream.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of Terminal v2</name>
  <what-built>Complete Terminal v2 with TradingView widget, realtime price bar, order book (стакан), open positions with live P&L, trade history, and top gainers/losers panels. Layout per Anton's wireframe.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to http://localhost:3000/terminal
    2. Verify TradingView chart loads with MOEX:SBER by default (full indicator toolbar, drawing tools visible)
    3. Select a different instrument (e.g., GAZP) from the dropdown — chart should remount with MOEX:GAZP
    4. Verify price bar shows current price, bid/ask from order book
    5. Verify order book panel shows bid (green) and ask (red) levels with spread
    6. Verify open positions panel shows your portfolio positions
    7. Verify trade history shows operations for the selected instrument
    8. Verify top gainers and top losers tables show data at the bottom
    9. Verify the layout matches: price bar top, chart+order book middle, positions+history below, movers bottom
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npm run build` completes without errors
- All 6 components in src/components/terminal/ exist and export named components
- Terminal page imports all 6 components + usePriceStream + all server actions
- No references to lightweight-charts in terminal page
</verification>

<success_criteria>
- TradingView Advanced Chart widget renders with MOEX symbols and full toolbar (D-01)
- Old lightweight-charts code removed from terminal page (D-02)
- Layout matches wireframe: price bar / chart+orderbook / positions+history / movers (D-03)
- All panels tied to selected instrument except top movers (D-04)
- Order book shows bid/ask table with spread (D-05, D-06)
- Price bar and order book update live (D-07, D-08)
- Top movers show gainers/losers sorted by change % (D-09)
- Positions show realtime P&L (D-10)
- Trade history shows operations for selected ticker (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-terminal-v2/02.1-02-SUMMARY.md`
</output>
