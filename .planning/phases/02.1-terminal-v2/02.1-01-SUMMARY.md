---
phase: 02.1-terminal-v2
plan: 01
subsystem: ui
tags: [terminal, formatting, intl, order-book, price-bar, vitest]

requires: []
provides:
  - Terminal utility functions (formatPrice, formatVolume, formatChange, formatSpread, getChangeColor)
  - PriceBar component with live instrument data display
  - OrderBook component with bid/ask depth table and cumulative volume bars
affects: [02.1-terminal-v2]

tech-stack:
  added: []
  patterns:
    - "Russian locale number formatting via Intl.NumberFormat('ru-RU')"
    - "TDD: tests written first, verified RED, then implementation to GREEN"
    - "Proportional background bars using absolute positioned divs with dynamic width%"

key-files:
  created:
    - src/lib/terminal-utils.ts
    - src/__tests__/terminal/terminal-utils.test.ts
    - src/components/terminal/price-bar.tsx
    - src/components/terminal/order-book.tsx

key-decisions:
  - "formatChange uses Math.abs() + manual +/- prefix to avoid double-minus in Russian locale"
  - "OrderBook cumulative: asks cumulate from spread outward (reversed), bids from spread down"
  - "formatVolume: >= 1M => M, >= 10K => K (not 1K), under 10K => plain Intl format"

requirements-completed: [TERM-02, TERM-03, TERM-06]

duration: 10min
completed: 2026-03-24
---

# Phase 02.1 Plan 01: Terminal Utilities and Core Display Components Summary

**Russian-locale price/volume formatting library with TDD tests, PriceBar instrument header, and OrderBook depth table with proportional cumulative volume bars**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-24T16:10:00Z
- **Completed:** 2026-03-24T16:14:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- 5 formatting utility functions (formatPrice, formatVolume, formatChange, formatSpread, getChangeColor) with 22 unit tests all passing
- PriceBar component with responsive layout — single row on desktop, 2 rows on mobile
- OrderBook component with ask (red) and bid (green) rows, cumulative volume percentage bars, spread row, loading skeleton, empty state

## Task Commits

1. **Task 1: Terminal utility functions with tests** - `fcd6f4b` (feat)
2. **Task 2: PriceBar and OrderBook components** - `65d9379` (feat)

## Files Created/Modified
- `src/lib/terminal-utils.ts` - formatPrice, formatVolume, formatChange, formatSpread, getChangeColor
- `src/__tests__/terminal/terminal-utils.test.ts` - 22 unit tests (TDD RED → GREEN)
- `src/components/terminal/price-bar.tsx` - Horizontal instrument info bar with responsive layout
- `src/components/terminal/order-book.tsx` - Bid/ask depth table with cumulative volume visualization

## Decisions Made
- Used `Math.abs()` + manual sign prefix for `formatChange` to avoid Intl double-minus bug with negative values in Russian locale
- OrderBook cumulative calculation: asks are reversed (sorted highest-to-lowest for display, but cumulated lowest-to-highest from spread), bids cumulated top-to-bottom from spread
- Volume abbreviation threshold set at 10K for K suffix (not 1K) per plan specification

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None - both tasks completed cleanly.

## Next Phase Readiness
- PriceBar and OrderBook are ready for integration into the terminal layout (Plan 02.1-02)
- Formatting utilities available for any component in the terminal subsystem

---
*Phase: 02.1-terminal-v2*
*Completed: 2026-03-24*
