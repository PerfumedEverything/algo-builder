---
phase: 02.1-terminal-v2
plan: 02
subsystem: ui
tags: [terminal, positions, trade-history, order-book, layout, responsive, sse, polling]

requires:
  - phase: 02.1-terminal-v2
    plan: 01
    provides: PriceBar and OrderBook components, terminal-utils formatting library

provides:
  - PositionsPanel component with open positions table, P&L coloring, clickable rows
  - TradeHistoryPanel component with chronological trade list and buy/sell badges
  - Complete terminal page layout matching Anton's wireframe

affects: [02.1-terminal-v2]

tech-stack:
  added: []
  patterns:
    - "useEffect + setInterval for order book polling every 5s with cleanup on unmount"
    - "SSE price override: livePrice?.price ?? candleDerivedPrice"
    - "Responsive layout: lg:grid-cols-[1fr_300px] for chart+orderbook, lg:grid-cols-2 for panels"

key-files:
  created:
    - src/components/terminal/positions-panel.tsx
    - src/components/terminal/trade-history-panel.tsx
  modified:
    - src/app/(dashboard)/terminal/page.tsx

key-decisions:
  - "Volume field not available in CandlestickData<Time> type — set to 0 since no current data source provides it"
  - "Position click creates a BrokerInstrument stub with type STOCK, lot 1 — sufficient for chart display"
  - "OrderBook shows loading skeleton only on first load (orderBookLoading && !orderBook) to avoid flicker on poll"

requirements-completed: [TERM-01, TERM-04, TERM-05, TERM-06]

duration: 11min
completed: 2026-03-24
---

# Phase 02.1 Plan 02: Terminal Panels and Page Assembly Summary

**PositionsPanel and TradeHistoryPanel components wired into complete terminal page layout with responsive grid, SSE live prices, 5s order book polling, and position-click instrument switching**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-03-24T13:48:52Z
- **Completed:** 2026-03-24T13:59:00Z
- **Tasks:** 2 (+ checkpoint reached)
- **Files modified:** 3

## Accomplishments
- PositionsPanel: compact table with all positions, P&L absolute+percent with color coding, clickable rows that switch active instrument
- TradeHistoryPanel: chronological list with HH:MM / DD.MM timestamps, buy/sell colored badges, sorted newest-first
- Terminal page assembled per wireframe: selector + PriceBar + [chart|order-book] + [positions|trade-history]
- Order book polls every 5 seconds via setInterval with proper cleanup
- SSE live price fed into PriceBar with candle-derived fallback

## Task Commits

Each task was committed atomically:

1. **Task 1: PositionsPanel and TradeHistoryPanel components** - `76f4819` (feat)
2. **Task 2: Assemble terminal page layout per wireframe** - `f8dd111` (feat)

**Plan metadata:** (docs commit after checkpoint)

## Files Created/Modified
- `src/components/terminal/positions-panel.tsx` - Open positions table with P&L, clickable rows
- `src/components/terminal/trade-history-panel.tsx` - Chronological trade list with buy/sell badges
- `src/app/(dashboard)/terminal/page.tsx` - Complete terminal page with all panels, polling, SSE

## Decisions Made
- Volume stat in PriceBar shows 0 because `CandlestickData<Time>` has no volume field and `PriceUpdate` SSE type also lacks volume — a future enhancement could add volume to the SSE stream
- Position row click constructs a minimal `BrokerInstrument` stub (type=STOCK, lot=1) since the positions array doesn't include the full instrument record
- OrderBook loading state only shows skeleton on initial load to avoid poll-triggered flicker

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed TypeScript error: volume property missing on CandlestickData**
- **Found during:** Task 2 (page assembly, build verification)
- **Issue:** Plan's `sum(volume)` formula assumes CandlestickData has a volume field, but the lightweight-charts type does not include it
- **Fix:** Set volume to 0 constant since no current data source provides per-candle volume
- **Files modified:** src/app/(dashboard)/terminal/page.tsx
- **Verification:** Build passed without TypeScript errors
- **Committed in:** f8dd111 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 type/bug)
**Impact on plan:** Necessary type fix — no functional impact since volume display was already a "nice to have" indicator.

## Issues Encountered
- `CandlestickData<Time>` from lightweight-charts does not expose `volume` property. Chart-actions maps candles without volume. Future enhancement: add volume to `PriceUpdate` SSE events.

## Known Stubs
- `volume = 0` in terminal page.tsx (line ~174) — PriceBar always shows "0" for volume. No volume data source exists in current implementation. Future plan should add volume to PriceUpdate SSE type and broker candle mapping.

## Next Phase Readiness
- Terminal layout complete and awaiting visual verification (Task 3 checkpoint)
- After approval, ready to proceed to Phase 3 (Fundamentals)

---
*Phase: 02.1-terminal-v2*
*Completed: 2026-03-24*
