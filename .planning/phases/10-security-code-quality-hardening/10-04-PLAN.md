---
phase: 10-security-code-quality-hardening
plan: 04
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - src/server/providers/ai/ai-prompts.ts
  - src/server/providers/ai/ai-tools.ts
  - src/server/providers/ai/deepseek-provider.ts
  - src/server/services/correlation-service.ts
  - src/server/services/portfolio-analytics-service.ts
  - src/server/services/health-scoring-constants.ts
  - src/server/services/portfolio-health-service.ts
  - src/server/actions/analytics-ai-prompt.ts
  - src/server/actions/analytics-actions.ts
  - src/server/actions/paper-portfolio-actions.ts
  - src/server/actions/operation-actions.ts
  - src/server/providers/ai/index.ts
  - src/server/services/index.ts
  - src/server/actions/index.ts
autonomous: true
requirements: [SEC-09]

must_haves:
  truths:
    - "No source file exceeds 150 lines"
    - "All imports across the codebase resolve correctly after split"
    - "Build compiles without errors"
  artifacts:
    - path: "src/server/providers/ai/ai-prompts.ts"
      provides: "SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT, INDICATOR_HINTS, RISK_PROFILES, randomItem"
    - path: "src/server/providers/ai/ai-tools.ts"
      provides: "generateStrategyTool, VALID_INDICATORS, VALID_CONDITIONS, VALID_TIMEFRAMES"
    - path: "src/server/services/correlation-service.ts"
      provides: "Extracted correlation logic from PortfolioAnalyticsService"
    - path: "src/server/services/health-scoring-constants.ts"
      provides: "Scoring constants and helper extracted from PortfolioHealthService"
    - path: "src/server/actions/analytics-ai-prompt.ts"
      provides: "AI prompt building logic extracted from analytics-actions"
    - path: "src/server/actions/paper-portfolio-actions.ts"
      provides: "getPaperPortfolioAction extracted from operation-actions"
  key_links:
    - from: "src/server/providers/ai/deepseek-provider.ts"
      to: "src/server/providers/ai/ai-prompts.ts"
      via: "import SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT"
      pattern: "import.*ai-prompts"
    - from: "src/server/providers/ai/deepseek-provider.ts"
      to: "src/server/providers/ai/ai-tools.ts"
      via: "import generateStrategyTool"
      pattern: "import.*ai-tools"
---

<objective>
Split all files exceeding 150 lines into focused modules following project conventions.

Purpose: Enforce the 150-line-per-file rule from CLAUDE.md. Every file must have a single, clear responsibility.
Output: 6 new files extracted from 5 oversized files, all under 150 lines.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split deepseek-provider.ts (407 lines) and analytics-actions.ts (221 lines)</name>
  <files>
    src/server/providers/ai/ai-prompts.ts,
    src/server/providers/ai/ai-tools.ts,
    src/server/providers/ai/deepseek-provider.ts,
    src/server/actions/analytics-ai-prompt.ts,
    src/server/actions/analytics-actions.ts
  </files>
  <read_first>
    src/server/providers/ai/deepseek-provider.ts,
    src/server/actions/analytics-actions.ts
  </read_first>
  <action>
    **Split deepseek-provider.ts (407 lines -> ~150 lines):**

    1. Create `src/server/providers/ai/ai-prompts.ts`:
       - Move from deepseek-provider.ts: `INDICATOR_HINTS` array (lines 145-158), `RISK_PROFILES` array (lines 160-166), `randomItem` helper (line 168), `SYSTEM_PROMPT` template string (lines 170-185), `CHAT_SYSTEM_PROMPT` template string (lines 187-211)
       - Export all as named exports

    2. Create `src/server/providers/ai/ai-tools.ts`:
       - Move from deepseek-provider.ts: `VALID_INDICATORS` array (lines 6-9), `VALID_CONDITIONS` array (lines 10-20), `VALID_TIMEFRAMES` array (line 22), `generateStrategyTool` object (lines 24-143)
       - Import types `IndicatorType`, `ConditionType` from `@/core/types`
       - Import `ChatCompletionTool` from `openai/resources/chat/completions`
       - Export all as named exports

    3. Update `deepseek-provider.ts`:
       - Remove all moved constants/prompts/tools
       - Add imports: `import { SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT, INDICATOR_HINTS, RISK_PROFILES, randomItem } from "./ai-prompts"`
       - Add imports: `import { generateStrategyTool, VALID_INDICATORS, VALID_CONDITIONS, VALID_TIMEFRAMES } from "./ai-tools"`
       - Only the `DeepSeekProvider` class remains
       - Update barrel export in `src/server/providers/ai/index.ts` if it exists

    **Split analytics-actions.ts (221 lines -> ~130 lines):**

    4. Create `src/server/actions/analytics-ai-prompt.ts`:
       - Move `getFullPortfolioAiAnalysisAction` function (lines 129-220) to this file
       - Mark as `"use server"` at top
       - Import everything it needs: types, services, actions (getPortfolioAnalyticsAction, getCorrelationMatrixAction from analytics-actions, analyzeWithAiAction from ai-analysis-actions)
       - Export `getFullPortfolioAiAnalysisAction`

    5. Update `analytics-actions.ts`:
       - Remove `getFullPortfolioAiAnalysisAction`
       - Remove unused imports (`PortfolioHealthService`, `analyzeWithAiAction` if only used by the moved function)
       - Re-export from new file for backward compat: `export { getFullPortfolioAiAnalysisAction } from "./analytics-ai-prompt"`
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && wc -l src/server/providers/ai/deepseek-provider.ts src/server/providers/ai/ai-prompts.ts src/server/providers/ai/ai-tools.ts src/server/actions/analytics-actions.ts src/server/actions/analytics-ai-prompt.ts | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - deepseek-provider.ts is under 150 lines
    - ai-prompts.ts exists with SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT exports
    - ai-tools.ts exists with generateStrategyTool export
    - analytics-actions.ts is under 150 lines
    - analytics-ai-prompt.ts exists with getFullPortfolioAiAnalysisAction
    - All files under 150 lines
  </acceptance_criteria>
  <done>deepseek-provider.ts split into 3 files. analytics-actions.ts split into 2 files. All under 150 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Split remaining 3 oversized files + verify build</name>
  <files>
    src/server/services/correlation-service.ts,
    src/server/services/portfolio-analytics-service.ts,
    src/server/services/health-scoring-constants.ts,
    src/server/services/portfolio-health-service.ts,
    src/server/actions/paper-portfolio-actions.ts,
    src/server/actions/operation-actions.ts
  </files>
  <read_first>
    src/server/services/portfolio-analytics-service.ts,
    src/server/services/portfolio-health-service.ts,
    src/server/actions/operation-actions.ts
  </read_first>
  <action>
    **Split portfolio-analytics-service.ts (287 lines -> ~150 lines):**

    1. Create `src/server/services/correlation-service.ts`:
       - Extract `getCorrelationMatrix` method logic into a standalone class `CorrelationService` with a single `getCorrelationMatrix(userId, days)` method
       - Move the `toReturns` helper function
       - Import `sampleCorrelation` from `simple-statistics`, types from `@/core/types`, `BrokerService`, `MOEXProvider`
       - Export `CorrelationService`

    2. Update `portfolio-analytics-service.ts`:
       - Remove `getCorrelationMatrix` method and `toReturns` helper
       - Import and delegate to `CorrelationService` if needed for backward compat, OR update callers to use `CorrelationService` directly
       - Check analytics-actions.ts — if it calls `analyticsService.getCorrelationMatrix()`, update to use `new CorrelationService().getCorrelationMatrix()`
       - Update barrel export in `src/server/services/index.ts`

    **Split portfolio-health-service.ts (262 lines -> ~150 lines):**

    3. Create `src/server/services/health-scoring-constants.ts`:
       - Move scoring threshold constants, the `clamp` helper, and any static scoring lookup tables from portfolio-health-service.ts
       - If the service uses constants from `@/core/config/analytics-constants`, those stay where they are — only move constants defined locally in the health service file
       - Look for scoring bracket logic (the if/else chains that assign numeric scores based on threshold values). Extract those as pure functions: `scoreDiversification(hhi)`, `scoreRisk(...)`, `scorePerformance(...)` if they are cleanly separable
       - Export all

    4. Update `portfolio-health-service.ts`:
       - Import scoring helpers/constants from `./health-scoring-constants`
       - Remaining file should contain only the `PortfolioHealthService` class methods that orchestrate computation

    **Split operation-actions.ts (199 lines -> ~100 lines):**

    5. Create `src/server/actions/paper-portfolio-actions.ts`:
       - Move `PaperStrategyRow` type and `getPaperPortfolioAction` function (lines 11-198) to this file
       - Mark as `"use server"` at top
       - Import everything it needs: types, services, `getCurrentUserId`
       - Export `PaperStrategyRow` and `getPaperPortfolioAction`

    6. Update `operation-actions.ts`:
       - Remove `getPaperPortfolioAction` and `PaperStrategyRow`
       - Re-export for backward compat: `export { type PaperStrategyRow, getPaperPortfolioAction } from "./paper-portfolio-actions"`
       - Remove now-unused imports (`PriceCache`, `BrokerService` if only used by paper portfolio)

    **Verify build:**

    7. Run `npx next build` to confirm all imports resolve and no TypeScript errors.
       - If barrel exports (index.ts) need updating, update them
       - If any consumer imports from the old file paths directly (not through barrel), grep and fix
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && wc -l src/server/services/portfolio-analytics-service.ts src/server/services/correlation-service.ts src/server/services/portfolio-health-service.ts src/server/services/health-scoring-constants.ts src/server/actions/operation-actions.ts src/server/actions/paper-portfolio-actions.ts | tail -7</automated>
  </verify>
  <acceptance_criteria>
    - portfolio-analytics-service.ts under 150 lines
    - correlation-service.ts exists with CorrelationService
    - portfolio-health-service.ts under 150 lines
    - health-scoring-constants.ts exists
    - operation-actions.ts under 150 lines
    - paper-portfolio-actions.ts exists with getPaperPortfolioAction
    - `npx next build` succeeds (or `npx tsc --noEmit` at minimum)
  </acceptance_criteria>
  <done>All 5 oversized files split. No file exceeds 150 lines. Build compiles successfully.</done>
</task>

</tasks>

<verification>
- `find src -name "*.ts" -o -name "*.tsx" | xargs wc -l | sort -rn | head -20` — no file >150 lines
- `npx next build` or `npx tsc --noEmit` — no errors
- All barrel exports updated
</verification>

<success_criteria>
- SEC-09: All 5 files that exceeded 150 lines are now split into focused modules
- No source file exceeds 150 lines
- Build compiles without errors
- Backward compatibility maintained via re-exports
</success_criteria>

<output>
After completion, create `.planning/phases/10-security-code-quality-hardening/10-04-SUMMARY.md`
</output>
