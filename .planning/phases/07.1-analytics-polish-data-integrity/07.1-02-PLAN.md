---
phase: 07.1-analytics-polish-data-integrity
plan: 02
type: execute
wave: 2
depends_on: ["07.1-01"]
files_modified:
  - src/server/actions/analytics-actions.ts
  - src/core/config/ai-prompts.ts
  - src/components/portfolio/health-score-card.tsx
  - src/components/portfolio/diversification-advice.tsx
  - src/components/portfolio/correlation-warnings.tsx
  - src/components/portfolio/benchmark-card.tsx
  - src/components/portfolio/markowitz-comparison.tsx
  - src/components/portfolio/rebalancing-actions.tsx
  - src/app/(dashboard)/portfolio/page.tsx
autonomous: true
requirements: [APOL-01, APOL-02, APOL-03, APOL-04, APOL-05, APOL-06]
must_haves:
  truths:
    - "User sees Health Score card (1-100) with 3 colored sub-score bars and plain-language details"
    - "Markowitz donuts and rebalancing table are gone, replaced by diversification advice list"
    - "Benchmark card shows deposit comparison verdict in Russian"
    - "Correlation section has human-readable warnings below the heatmap"
    - "getPortfolioAnalyticsAction uses Promise.allSettled for per-metric error isolation"
    - "AI analysis receives ALL positions, health score, specific problems — no Markowitz tables"
    - "Full AI analysis button still works end-to-end"
  artifacts:
    - path: "src/components/portfolio/health-score-card.tsx"
      provides: "Health Score card with composite score and 3 sub-score breakdown"
      exports: ["HealthScoreCard"]
    - path: "src/components/portfolio/diversification-advice.tsx"
      provides: "List of actionable diversification recommendations"
      exports: ["DiversificationAdviceList"]
    - path: "src/components/portfolio/correlation-warnings.tsx"
      provides: "Human-readable correlation warnings"
      exports: ["CorrelationWarnings"]
    - path: "src/server/actions/analytics-actions.ts"
      provides: "Updated actions with Promise.allSettled + health score + no Markowitz"
      exports: ["getPortfolioAnalyticsAction", "getHealthScoreAction", "getFullPortfolioAiAnalysisAction"]
  key_links:
    - from: "src/server/actions/analytics-actions.ts"
      to: "src/server/services/portfolio-health-service.ts"
      via: "import PortfolioHealthService"
      pattern: "PortfolioHealthService"
    - from: "src/components/portfolio/health-score-card.tsx"
      to: "src/core/types/analytics.ts"
      via: "import HealthScore type"
      pattern: "import.*HealthScore"
    - from: "src/app/(dashboard)/portfolio/page.tsx"
      to: "src/components/portfolio/health-score-card.tsx"
      via: "import and render"
      pattern: "HealthScoreCard"
---

<objective>
Wire health scoring into server actions, rewrite AI prompt, create UI components, and rewire the portfolio page. Delete Markowitz UI components.

Purpose: User-facing delivery of all Phase 7.1 features — the backend from Plan 01 becomes visible.
Output: Updated portfolio analytics tab with Health Score, diversification advice, enhanced benchmark, correlation warnings, and rewritten AI analysis.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-analytics-polish-data-integrity/07.1-RESEARCH.md
@.planning/phases/07.1-analytics-polish-data-integrity/07.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must be complete before this plan runs) -->

From src/server/services/portfolio-health-service.ts:
```typescript
export class PortfolioHealthService {
  static computeHealthScore(params: {
    concentration: ConcentrationIndex
    sectorAllocation: SectorAllocation[]
    benchmark: BenchmarkComparison | null
    highCorrelationCount: number
    positionCount: number
  }): HealthScore

  static generateDiversificationAdvice(
    positions: PortfolioPosition[],
    concentration: ConcentrationIndex,
    sectorAllocation: SectorAllocation[],
  ): DiversificationAdvice[]

  static generateCorrelationWarnings(
    highPairs: CorrelationMatrix["highPairs"],
  ): CorrelationWarning[]

  static enhanceBenchmark(
    benchmark: BenchmarkComparison,
  ): EnhancedBenchmarkComparison
}
```

From src/core/types/analytics.ts (updated in Plan 01):
```typescript
export type HealthScoreLevel = "excellent" | "good" | "warning" | "danger" | "insufficient_data"
export type HealthSubScore = { score: number; details: string[] }
export type HealthScore = {
  total: number
  level: HealthScoreLevel
  diversification: HealthSubScore
  risk: HealthSubScore
  performance: HealthSubScore
}
export type DiversificationAdvice = {
  level: "success" | "warning" | "danger"
  icon: "check" | "alert-triangle" | "x-circle"
  text: string
}
export type CorrelationWarning = {
  tickers: [string, string]
  corr: number
  text: string
  isPositive: boolean
}
export type EnhancedBenchmarkComparison = BenchmarkComparison & {
  depositRateForPeriod: number
  depositDelta: number
  verdict: BenchmarkVerdict
  verdictText: string
}
```

From src/core/config/analytics-constants.ts:
```typescript
export const DEPOSIT_RATE_ANNUAL = 0.15
export const RF_RATE = 0.15
export const SECTOR_LABELS: Record<string, string>
```

From existing components to reference style:
- src/components/portfolio/concentration-card.tsx (card pattern: rounded-xl border bg-card p-4)
- src/components/portfolio/benchmark-card.tsx (current benchmark display, to be enhanced)
- src/components/portfolio/correlation-heatmap.tsx (heatmap that stays, warnings go below)

From src/server/actions/analytics-actions.ts (to be rewritten):
```typescript
export const getPortfolioAnalyticsAction  // MODIFY: Promise.allSettled
export const getMarkowitzOptimizationAction  // DELETE
export const getFullPortfolioAiAnalysisAction  // REWRITE: health score + problems instead of Markowitz
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server actions (Promise.allSettled + health score + AI prompt rewrite)</name>
  <files>
    src/server/actions/analytics-actions.ts
    src/core/config/ai-prompts.ts
  </files>
  <read_first>
    src/server/actions/analytics-actions.ts
    src/core/config/ai-prompts.ts
    src/server/services/portfolio-health-service.ts
    src/core/types/analytics.ts
    src/core/config/analytics-constants.ts
  </read_first>
  <action>
    **Step 1: Rewrite getPortfolioAnalyticsAction** to use Promise.allSettled per APOL-05.
    Replace the current Promise.all with Promise.allSettled for [sectorAllocation, assetTypeBreakdown, tradeSuccessBreakdown, benchmarkComparison, aggregateDividendYield].
    For each result: if "fulfilled" use value, if "rejected" use sensible default (empty array, null, { weightedYield: 0, positionYields: [] }).
    Concentration stays synchronous (no await needed).
    Return the same PortfolioAnalytics shape.

    **Step 2: Add getHealthScoreAction** — new server action:
    ```typescript
    export const getHealthScoreAction = async (): Promise<ApiResponse<HealthScore>> => {
      try {
        const userId = await getCurrentUserId()
        const portfolio = await brokerService.getPortfolio(userId)
        const positions = portfolio?.positions ?? []

        const [analyticsRes, corrRes] = await Promise.allSettled([
          getPortfolioAnalyticsAction(),
          getCorrelationMatrixAction(),
        ])

        const analytics = analyticsRes.status === "fulfilled" && analyticsRes.value.success ? analyticsRes.value.data : null
        const correlation = corrRes.status === "fulfilled" && corrRes.value.success ? corrRes.value.data : null

        const healthScore = PortfolioHealthService.computeHealthScore({
          concentration: analytics?.concentration ?? { hhi: 0, level: "diversified", dominantPositions: [] },
          sectorAllocation: analytics?.sectorAllocation ?? [],
          benchmark: analytics?.benchmarkComparison ?? null,
          highCorrelationCount: correlation?.highPairs?.length ?? 0,
          positionCount: positions.length,
        })

        return successResponse(healthScore)
      } catch (e) {
        return errorResponse((e as Error).message)
      }
    }
    ```

    **Step 3: Delete getMarkowitzOptimizationAction** entirely.
    Remove MarkowitzResult from the import statement at top. Remove any import of MarkowitzResult type.

    **Step 4: Rewrite getFullPortfolioAiAnalysisAction** per APOL-06.
    Remove Markowitz data assembly. Instead:
    - Compute health score via PortfolioHealthService.computeHealthScore
    - Generate diversification advice via PortfolioHealthService.generateDiversificationAdvice
    - Generate correlation warnings via PortfolioHealthService.generateCorrelationWarnings
    - Enhance benchmark via PortfolioHealthService.enhanceBenchmark (if benchmark exists)
    - Build message with ALL positions (not just top 5): ticker, name, quantity, price, expectedYield, weight%
    - Include health score: total, level, each sub-score with details
    - Include diversification advice items
    - Include benchmark verdict text
    - Include correlation warnings text
    - Include sector allocation summary
    Use `analyzeWithAiAction("fullPortfolio", message)` as before.

    **Step 5: Update AI_PROMPTS.fullPortfolio** in ai-prompts.ts:
    Replace Markowitz-focused prompt with health-focused prompt:
    ```
    Ты старший инвестиционный советник с экспертизой в управлении частными портфелями на Московской бирже.

    Проведи комплексный анализ портфеля по данным ниже:

    1. **Здоровье портфеля** — интерпретируй оценку здоровья и каждый суб-балл. Что критично?
    2. **Диверсификация** — оцени рекомендации, добавь свои. Какие действия приоритетны?
    3. **Доходность** — сравни с рынком и депозитом. Стоит ли менять стратегию?
    4. **Корреляции** — прокомментируй высокие корреляции. Как снизить взаимозависимость?
    5. **Конкретные рекомендации** — что продать, что докупить, чего избегать.

    Формат: структурированный markdown на русском языке с таблицами где уместно.
    ```
    Remove the `optimization` prompt entry too (it referenced Markowitz). Keep the AiAnalysisBlock type union — just remove "optimization" from it.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Promise.allSettled" src/server/actions/analytics-actions.ts
    - grep -q "getHealthScoreAction" src/server/actions/analytics-actions.ts
    - NOT grep -q "getMarkowitzOptimizationAction" src/server/actions/analytics-actions.ts
    - NOT grep -q "MarkowitzResult" src/server/actions/analytics-actions.ts
    - grep -q "PortfolioHealthService" src/server/actions/analytics-actions.ts
    - grep -q "computeHealthScore" src/server/actions/analytics-actions.ts
    - grep -q "generateDiversificationAdvice" src/server/actions/analytics-actions.ts
    - grep -q "Здоровье портфеля" src/core/config/ai-prompts.ts
    - NOT grep -q "Оптимизация Марковица" src/core/config/ai-prompts.ts
    - npx tsc --noEmit exits 0 (or only errors in files being modified in Task 2)
  </acceptance_criteria>
  <done>
    Server actions use Promise.allSettled. Health score action exists. Markowitz action deleted. AI prompt rewritten with health-focused structure. Full AI analysis sends complete data including all positions, health score, advice, and warnings.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI components + portfolio page rewire</name>
  <files>
    src/components/portfolio/health-score-card.tsx
    src/components/portfolio/diversification-advice.tsx
    src/components/portfolio/correlation-warnings.tsx
    src/components/portfolio/benchmark-card.tsx
    src/components/portfolio/markowitz-comparison.tsx
    src/components/portfolio/rebalancing-actions.tsx
    src/app/(dashboard)/portfolio/page.tsx
  </files>
  <read_first>
    src/app/(dashboard)/portfolio/page.tsx
    src/components/portfolio/benchmark-card.tsx
    src/components/portfolio/concentration-card.tsx
    src/components/portfolio/correlation-heatmap.tsx
    src/server/services/portfolio-health-service.ts
    src/core/types/analytics.ts
    src/core/config/analytics-constants.ts
  </read_first>
  <action>
    **Step 1: Create health-score-card.tsx** — new component:
    Props: `{ data: HealthScore | null; loading: boolean }`
    Layout: rounded-xl border bg-card p-4 (same as other cards).
    Show:
    - Title: "Здоровье портфеля"
    - Big number: data.total with color (green >= 75, yellow >= 50, red < 50)
    - Level text below number: "Портфель в хорошей форме" / "Есть что улучшить" / "Требует внимания" / "Недостаточно позиций для анализа"
    - 3 horizontal progress bars for sub-scores (diversification, risk, performance):
      Each bar: label, score/100, colored fill (emerald/yellow/red based on score thresholds), details list below
    - Use Lucide icons: Activity for health, Shield for risk, BarChart3 for performance, PieChart for diversification
    - insufficient_data level: show muted message, no bars

    **Step 2: Create diversification-advice.tsx** — new component:
    Props: `{ advice: DiversificationAdvice[]; loading: boolean }`
    Layout: rounded-xl border bg-card p-4.
    Title: "Рекомендации по диверсификации"
    For each advice item:
    - Icon: Check (emerald) for success, AlertTriangle (amber) for warning, XCircle (red) for danger
    - Text: advice.text
    - Use Lucide: Check, AlertTriangle, XCircle

    **Step 3: Create correlation-warnings.tsx** — new component:
    Props: `{ warnings: CorrelationWarning[]; loading: boolean }`
    If warnings is empty, don't render anything (return null).
    Layout: rounded-xl border border-amber-500/20 bg-card p-4.
    Title: "Взаимозависимость позиций"
    For each warning:
    - Icon: AlertTriangle (amber) for isPositive=false (high positive corr = risk), CheckCircle2 (emerald) for isPositive=true (negative corr = good)
    - Text: warning.text
    - Badge: correlation value (e.g. "0.85")

    **Step 4: Enhance benchmark-card.tsx** — modify existing:
    Accept new prop: `enhancedData?: EnhancedBenchmarkComparison | null`
    When enhancedData is provided, show ADDITIONALLY:
    - Verdict text in bold below the existing numbers (e.g. "Вы обгоняете рынок на 3.2%")
    - Deposit comparison line: "vs Депозит ({depositRateForPeriod * 100}%): {depositDelta > 0 ? '+' : ''}{depositDelta}%"
    - Color verdict text: emerald for beats_market, yellow for beats_deposit, red for loses_to_deposit
    - Small disclaimer text below: "Ценовая доходность без учёта дивидендов"
    Keep backward-compatible: if only `data` prop provided (no enhancedData), render as before.

    **Step 5: DELETE markowitz-comparison.tsx and rebalancing-actions.tsx** — these files are no longer used.

    **Step 6: Rewire portfolio page.tsx**:
    Remove imports: MarkowitzComparison, RebalancingActions, getMarkowitzOptimizationAction, MarkowitzResult.
    Add imports: HealthScoreCard, DiversificationAdviceList, CorrelationWarnings, getHealthScoreAction + PortfolioHealthService, EnhancedBenchmarkComparison, HealthScore, DiversificationAdvice, CorrelationWarning.

    Add state:
    ```typescript
    const [healthScore, setHealthScore] = useState<HealthScore | null>(null)
    const [diversificationAdvice, setDiversificationAdvice] = useState<DiversificationAdvice[]>([])
    const [correlationWarnings, setCorrelationWarnings] = useState<CorrelationWarning[]>([])
    const [enhancedBenchmark, setEnhancedBenchmark] = useState<EnhancedBenchmarkComparison | null>(null)
    ```

    Remove markowitzResult/markowitzLoading state.

    In fetchAnalytics:
    - Replace `getMarkowitzOptimizationAction()` with `getHealthScoreAction()` in the Promise.all
    - After getting analytics and correlation results:
      - Set healthScore from health score action result
      - Compute diversificationAdvice via `PortfolioHealthService.generateDiversificationAdvice(portfolio.positions, analytics.concentration, analytics.sectorAllocation)`
      - Compute correlationWarnings via `PortfolioHealthService.generateCorrelationWarnings(correlation.highPairs)`
      - Compute enhancedBenchmark via `PortfolioHealthService.enhanceBenchmark(analytics.benchmarkComparison)` if benchmarkComparison exists

    Update analytics tab layout:
    ```
    Row 1 (grid-cols-1 md:grid-cols-3): HealthScoreCard | BenchmarkCard (with enhancedData) | DividendYieldCard
    Row 2 (grid-cols-1 lg:grid-cols-2): CorrelationHeatmap + CorrelationWarnings below | SectorDonut + AssetTypeChart
    Row 3 (grid-cols-1 lg:grid-cols-2): ConcentrationCard + DiversificationAdviceList | TradeSuccessChart + InstrumentPnlTable
    AI button at bottom (unchanged)
    ```

    The CorrelationWarnings component goes directly below CorrelationHeatmap in the same column.
    The DiversificationAdviceList goes below ConcentrationCard in the same column.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "HealthScoreCard" src/app/(dashboard)/portfolio/page.tsx
    - grep -q "DiversificationAdvice" src/app/(dashboard)/portfolio/page.tsx
    - grep -q "CorrelationWarnings" src/app/(dashboard)/portfolio/page.tsx
    - grep -q "enhancedBenchmark" src/app/(dashboard)/portfolio/page.tsx
    - NOT grep -q "MarkowitzComparison" src/app/(dashboard)/portfolio/page.tsx
    - NOT grep -q "RebalancingActions" src/app/(dashboard)/portfolio/page.tsx
    - NOT grep -q "getMarkowitzOptimizationAction" src/app/(dashboard)/portfolio/page.tsx
    - file src/components/portfolio/markowitz-comparison.tsx does NOT exist
    - file src/components/portfolio/rebalancing-actions.tsx does NOT exist
    - grep -q "Здоровье портфеля" src/components/portfolio/health-score-card.tsx
    - grep -q "Рекомендации по диверсификации" src/components/portfolio/diversification-advice.tsx
    - grep -q "verdictText" src/components/portfolio/benchmark-card.tsx
    - npx next build succeeds
  </acceptance_criteria>
  <done>
    Health Score card visible in analytics tab. Diversification advice list replaces Markowitz donuts. Benchmark card shows deposit verdict. Correlation warnings appear below heatmap. Markowitz UI components deleted. Build passes. AI analysis button works with new prompt.
  </done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- `npx vitest run src/__tests__/portfolio-health.test.ts` passes
- `grep -r "MarkowitzComparison\|RebalancingActions\|getMarkowitzOptimizationAction" src/` — no hits outside deleted files
- Portfolio analytics tab shows: Health Score card, enhanced benchmark with deposit verdict, diversification advice list, correlation warnings below heatmap
- Full AI analysis button triggers and streams response with health-focused prompt
</verification>

<success_criteria>
1. Health Score card (1-100) with 3 sub-score bars visible in analytics tab
2. Markowitz donuts and rebalancing table completely replaced by diversification advice
3. Benchmark card shows "Вы обгоняете рынок" or "Депозит выгоднее" with clear Russian text
4. Correlation warnings in human language appear below the heatmap matrix
5. Server actions use Promise.allSettled — one metric failure doesn't break analytics tab
6. Full AI analysis sends complete portfolio data including health score and specific problems
7. Build passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-analytics-polish-data-integrity/07.1-02-SUMMARY.md`
</output>
