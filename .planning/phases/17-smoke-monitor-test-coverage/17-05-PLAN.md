---
phase: 17-smoke-monitor-test-coverage
plan: 05
type: execute
wave: 3
depends_on: [17-01]
files_modified:
  - src/__tests__/portfolio-analytics-service.test.ts
  - src/__tests__/indicator-calculator.test.ts
autonomous: true
requirements: [TEST-03, TEST-04]
must_haves:
  truths:
    - "PortfolioAnalyticsService test coverage expanded to >= 80% of public methods"
    - "IndicatorCalculator test coverage expanded with edge cases for all indicator types"
  artifacts:
    - path: "src/__tests__/portfolio-analytics-service.test.ts"
      provides: "Expanded portfolio analytics tests"
      min_lines: 100
    - path: "src/__tests__/indicator-calculator.test.ts"
      provides: "Expanded indicator calculator edge case tests"
      min_lines: 80
  key_links:
    - from: "src/__tests__/portfolio-analytics-service.test.ts"
      to: "src/server/services/portfolio-analytics-service.ts"
      via: "imports and tests all public methods"
      pattern: "PortfolioAnalyticsService"
    - from: "src/__tests__/indicator-calculator.test.ts"
      to: "src/server/services/indicator-calculator.ts"
      via: "tests edge cases for each indicator type"
      pattern: "IndicatorCalculator"
---

<objective>
Expand existing test coverage for PortfolioAnalyticsService and IndicatorCalculator to >= 80% by adding tests for uncovered methods and edge cases.

Purpose: These services produce the numbers users see — incorrect analytics or indicators mean wrong trading decisions.
Output: Expanded test files with new test cases covering uncovered methods and edge cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-smoke-monitor-test-coverage/17-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand PortfolioAnalyticsService tests (TEST-03)</name>
  <files>src/__tests__/portfolio-analytics-service.test.ts</files>
  <read_first>
    src/__tests__/portfolio-analytics-service.test.ts,
    src/server/services/portfolio-analytics-service.ts,
    src/core/types/index.ts
  </read_first>
  <behavior>
    Read the existing test file and the service file to identify which public methods lack test coverage. Then add tests for:
    - Every public method not currently tested
    - Edge cases: empty portfolio (no positions), single position portfolio
    - Error handling: broker service throws, repository returns empty
    - Correlation matrix with < 2 instruments (should return empty or skip)
    - Sector allocation with unknown instrument types
    - getBenchmarkComparison returns null on error (per Phase 06.1 decision)
    - getAnalytics with partial failures (Promise.allSettled per Phase 07.1)
  </behavior>
  <action>
Read the current test file first to understand existing coverage. Read the service file to enumerate all public methods.

For each public method NOT currently tested, add a describe block with:
1. Happy path test with mock data
2. Edge case test (empty data, null returns)
3. Error isolation test (one sub-service fails, others succeed)

Key patterns:
- Mock BrokerService, OperationService, StrategyRepository, MOEXProvider (already mocked in existing file)
- Use realistic PortfolioPosition mock data with correct types
- Verify Promise.allSettled isolation: if one service throws, others still return data

Add at least 8 new test cases to the existing file. Do NOT remove existing passing tests.
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx vitest run src/__tests__/portfolio-analytics-service.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/portfolio-analytics-service.test.ts` passes all tests
    - `grep -c "it(" src/__tests__/portfolio-analytics-service.test.ts` returns >= 12 (existing + 8 new)
    - New tests cover at least 3 different public methods not previously tested
    - Edge case for empty portfolio exists
    - Error isolation test exists (one mock throws, result still returns partial data)
  </acceptance_criteria>
  <done>PortfolioAnalyticsService has >= 12 total tests. All public methods have at least one test. Edge cases and error isolation covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand IndicatorCalculator edge case tests (TEST-04)</name>
  <files>src/__tests__/indicator-calculator.test.ts</files>
  <read_first>
    src/__tests__/indicator-calculator.test.ts,
    src/__tests__/indicator-accuracy.test.ts,
    src/server/services/indicator-calculator.ts,
    src/core/types/strategy.ts
  </read_first>
  <behavior>
    Read existing tests to identify gaps. Add edge case tests for:
    - Each indicator type (RSI, SMA, EMA, MACD, Bollinger) with minimum viable candle count (period + 1 for BollingerBands stability)
    - Empty candles array → should not throw, returns empty or null
    - Single candle → should handle gracefully
    - Candles with identical prices (flat market) → RSI should be 50 or NaN, not throw
    - Very large period vs short candle array → insufficient data handling
    - MACD with close prices all the same → histogram should be 0
    - Crossing detection edge: exact equality (value === threshold)
  </behavior>
  <action>
Read the current indicator-calculator.test.ts and indicator-accuracy.test.ts to understand existing coverage.

Add a new describe block "edge cases" with:
1. "handles empty candles array" — pass [] to each indicator, verify no throw
2. "handles single candle" — pass [oneCandle], verify no throw
3. "handles flat market (all same price)" — 30 candles with same OHLC, verify RSI is ~50 or NaN (not error), SMA/EMA equals the price
4. "handles insufficient data for period" — period=20, only 5 candles, verify graceful handling (null/undefined, not throw)
5. "BollingerBands needs period+1 candles for isStable" — verify with exactly period candles vs period+1
6. "MACD with flat prices" — histogram should be 0 or very close to 0
7. "crossing detection at exact boundary" — value exactly equals threshold

At least 7 new test cases. Do NOT remove existing tests.

Use the same candle fixture pattern from existing tests (create helper `makeCandle(close: number, i: number): Candle`).
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx vitest run src/__tests__/indicator-calculator.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/indicator-calculator.test.ts` passes all tests
    - `grep -c "it(" src/__tests__/indicator-calculator.test.ts` returns at least 7 more than current count
    - `grep "edge case\|empty candles\|flat market\|insufficient data" src/__tests__/indicator-calculator.test.ts` finds matches
    - No existing tests broken
  </acceptance_criteria>
  <done>IndicatorCalculator has comprehensive edge case coverage. Empty, single, flat, insufficient data cases all handled. No regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/portfolio-analytics-service.test.ts src/__tests__/indicator-calculator.test.ts` — all pass
- Combined: >= 15 new tests
</verification>

<success_criteria>
- PortfolioAnalyticsService: >= 12 total tests, all public methods covered
- IndicatorCalculator: >= 7 new edge case tests, all indicator types covered
- No regressions in existing tests
- Full `npm test` still passes
</success_criteria>

<output>
After completion, create `.planning/phases/17-smoke-monitor-test-coverage/17-05-SUMMARY.md`
</output>
