---
phase: 18-production-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/price-stream-worker.ts
  - scripts/bybit-stream-worker.ts
  - src/server/actions/strategy-actions.ts
  - src/server/actions/signal-actions.ts
  - src/server/providers/broker/bybit-provider.ts
  - src/server/actions/terminal-actions.ts
autonomous: true
requirements: [POLISH-06, POLISH-07, POLISH-08]

must_haves:
  truths:
    - "Workers reconnect to Redis after a transient disconnect without crash-looping"
    - "Fire-and-forget errors are logged with console.error, not silently swallowed"
    - "Bybit testnet vs production is controlled by BYBIT_TESTNET env var"
  artifacts:
    - path: "scripts/price-stream-worker.ts"
      provides: "Redis error/reconnect event handlers"
      contains: "redis.on"
    - path: "scripts/bybit-stream-worker.ts"
      provides: "Redis error handler + BYBIT_TESTNET env"
      contains: "BYBIT_TESTNET"
    - path: "src/server/actions/strategy-actions.ts"
      provides: "Fire-and-forget error logging"
      contains: "console.error"
    - path: "src/server/actions/signal-actions.ts"
      provides: "Fire-and-forget error logging"
      contains: "console.error"
    - path: "src/server/providers/broker/bybit-provider.ts"
      provides: "Env-controlled testnet"
      contains: "BYBIT_TESTNET"
  key_links:
    - from: "scripts/price-stream-worker.ts"
      to: "Redis"
      via: "error/reconnect event handlers"
      pattern: "redis\\.on\\(\"error\""
    - from: "scripts/bybit-stream-worker.ts"
      to: "BYBIT_TESTNET env"
      via: "process.env.BYBIT_TESTNET"
      pattern: "process\\.env\\.BYBIT_TESTNET"
---

<objective>
Harden workers against Redis disconnects, log fire-and-forget errors, and make Bybit testnet/prod configurable via env.

Purpose: Workers must survive transient Redis failures without crash-looping, hidden errors must be visible in logs, and Bybit environment must be switchable without code changes.
Output: Resilient workers, observable errors, env-controlled Bybit mode.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker Redis resilience + fire-and-forget logging</name>
  <files>scripts/price-stream-worker.ts, scripts/bybit-stream-worker.ts, src/server/actions/strategy-actions.ts, src/server/actions/signal-actions.ts</files>
  <read_first>scripts/price-stream-worker.ts, scripts/bybit-stream-worker.ts, src/server/actions/strategy-actions.ts, src/server/actions/signal-actions.ts</read_first>
  <action>
**Redis resilience for price-stream-worker.ts:**
Add error and reconnect handlers to both `redis` and `subscriberRedis` instances (after line 23):
```
redis.on("error", (err) => console.error("[Worker] Redis error:", err.message))
redis.on("reconnecting", (delay: number) => console.log("[Worker] Redis reconnecting in", delay, "ms"))
subscriberRedis.on("error", (err) => console.error("[Worker] Subscriber Redis error:", err.message))
subscriberRedis.on("reconnecting", (delay: number) => console.log("[Worker] Subscriber Redis reconnecting in", delay, "ms"))
```

Also update the Redis constructor options for both instances to add:
- `retryStrategy: (times: number) => Math.min(times * 500, 30000)` (exponential backoff capped at 30s)
- `reconnectOnError: (err: Error) => err.message.includes("READONLY") ? 2 : false` (reconnect on readonly errors)
- Keep existing `maxRetriesPerRequest: 3` and `lazyConnect: true`

**Redis resilience for bybit-stream-worker.ts:**
Add same error/reconnect handlers to the `redis` instance (after line 14):
```
redis.on("error", (err) => console.error("[BybitWorker] Redis error:", err.message))
redis.on("reconnecting", (delay: number) => console.log("[BybitWorker] Redis reconnecting in", delay, "ms"))
```
Add same `retryStrategy` and `reconnectOnError` options to the Redis constructor.

**Fire-and-forget logging:**
In `src/server/actions/strategy-actions.ts`, find line ~106:
```
new StrategyChecker().checkAll().catch(() => {})
```
Replace with:
```
new StrategyChecker().checkAll().catch((e) => console.error("[StrategyAction] background check failed:", e))
```

In `src/server/actions/signal-actions.ts`, find lines ~58 and ~102:
```
new SignalChecker().checkAll().catch(() => {})
```
Replace both with:
```
new SignalChecker().checkAll().catch((e) => console.error("[SignalAction] background check failed:", e))
```
  </action>
  <verify>
    <automated>grep -c 'redis.on("error"' scripts/price-stream-worker.ts scripts/bybit-stream-worker.ts && grep -c 'retryStrategy' scripts/price-stream-worker.ts scripts/bybit-stream-worker.ts && grep -c 'console.error.*background check' src/server/actions/strategy-actions.ts src/server/actions/signal-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - scripts/price-stream-worker.ts contains `redis.on("error"` (2 instances for redis + subscriberRedis)
    - scripts/price-stream-worker.ts contains `retryStrategy`
    - scripts/bybit-stream-worker.ts contains `redis.on("error"`
    - scripts/bybit-stream-worker.ts contains `retryStrategy`
    - src/server/actions/strategy-actions.ts contains `console.error("[StrategyAction] background check failed:"`
    - src/server/actions/signal-actions.ts contains `console.error("[SignalAction] background check failed:"` (2 occurrences)
    - No `.catch(() => {})` remains in strategy-actions.ts or signal-actions.ts
  </acceptance_criteria>
  <done>Workers have Redis error handlers with exponential backoff retry, fire-and-forget errors are logged</done>
</task>

<task type="auto">
  <name>Task 2: Bybit testnet/prod via BYBIT_TESTNET env</name>
  <files>src/server/providers/broker/bybit-provider.ts, scripts/bybit-stream-worker.ts, src/server/actions/terminal-actions.ts</files>
  <read_first>src/server/providers/broker/bybit-provider.ts, scripts/bybit-stream-worker.ts, src/server/actions/terminal-actions.ts</read_first>
  <action>
**bybit-provider.ts (line 45):**
Replace hardcoded `testnet: true` with:
```
this.client = new RestClientV5({ key, secret, testnet: process.env.BYBIT_TESTNET !== "false" })
```
This defaults to testnet=true (safe default) but can be set to production by setting `BYBIT_TESTNET=false`.

**bybit-stream-worker.ts (line 25):**
Replace hardcoded `testnet: true` in WebsocketClient constructor with:
```
testnet: process.env.BYBIT_TESTNET !== "false",
```

Also update the console.log on line 81 to be dynamic:
```
console.log(`[BybitWorker] Subscribing to ${SYMBOLS.length} symbols on ${process.env.BYBIT_TESTNET !== "false" ? "testnet" : "mainnet"}`)
```

**terminal-actions.ts (line 27):**
Replace hardcoded `testnet: true` in `getBybitInstrumentsAction`:
```
const client = new RestClientV5({ key: apiKey, secret: apiSecret, testnet: process.env.BYBIT_TESTNET !== "false" })
```
  </action>
  <verify>
    <automated>grep -n "testnet: true" src/server/providers/broker/bybit-provider.ts scripts/bybit-stream-worker.ts src/server/actions/terminal-actions.ts; echo "---"; grep -c "BYBIT_TESTNET" src/server/providers/broker/bybit-provider.ts scripts/bybit-stream-worker.ts src/server/actions/terminal-actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - No `testnet: true` hardcoded in src/server/providers/broker/bybit-provider.ts
    - No `testnet: true` hardcoded in scripts/bybit-stream-worker.ts
    - No `testnet: true` hardcoded in src/server/actions/terminal-actions.ts
    - src/server/providers/broker/bybit-provider.ts contains `BYBIT_TESTNET`
    - scripts/bybit-stream-worker.ts contains `BYBIT_TESTNET`
    - src/server/actions/terminal-actions.ts contains `BYBIT_TESTNET`
  </acceptance_criteria>
  <done>Bybit testnet/prod controlled by BYBIT_TESTNET env var (default: testnet for safety)</done>
</task>

</tasks>

<verification>
- `grep -rn 'testnet: true' src/ scripts/` returns 0 results (no hardcoded testnet)
- `grep -rn '.catch(() => {})' src/server/actions/` returns 0 results (no swallowed errors)
- `grep -c 'retryStrategy' scripts/price-stream-worker.ts scripts/bybit-stream-worker.ts` shows counts > 0
</verification>

<success_criteria>
1. All Bybit testnet references are env-controlled via BYBIT_TESTNET
2. Workers have Redis error handlers with retry strategy
3. Fire-and-forget errors produce console.error output
</success_criteria>

<output>
After completion, create `.planning/phases/18-production-polish/18-01-SUMMARY.md`
</output>
