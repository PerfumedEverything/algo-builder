---
phase: 04-ai-revolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/providers/ai/deepseek-provider.ts
  - src/components/strategy/ai-chat.tsx
  - src/components/strategy/strategy-preview-panel.tsx
  - src/components/strategy/strategy-dialog.tsx
  - src/components/strategy/index.ts
autonomous: true
requirements:
  - AIREV-01
  - AIREV-02

must_haves:
  truths:
    - "AI responds to free-form trading ideas without requiring a 4-step quiz sequence"
    - "AI calls create_strategy tool when it has enough context from the conversation"
    - "User sees a live strategy preview panel below the chat when AI has extracted strategy parameters"
    - "Preview panel updates on each assistant message that contains a strategy"
    - "New indicators (ATR, STOCHASTIC, VWAP, WILLIAMS_R) appear in the AI tool schema so AI can use them"
  artifacts:
    - path: "src/server/providers/ai/deepseek-provider.ts"
      provides: "Free-form system prompt, updated VALID_INDICATORS with 4 new types, updated tool schema"
      contains: "WILLIAMS_R"
    - path: "src/components/strategy/ai-chat.tsx"
      provides: "Free-form chat mode, onStrategyExtracted callback, initialContext support"
      contains: "onStrategyExtracted"
    - path: "src/components/strategy/strategy-preview-panel.tsx"
      provides: "Live preview panel component showing extracted strategy parameters"
      min_lines: 30
    - path: "src/components/strategy/strategy-dialog.tsx"
      provides: "Renders StrategyPreviewPanel, accepts initialContext prop"
      contains: "StrategyPreviewPanel"
  key_links:
    - from: "src/components/strategy/ai-chat.tsx"
      to: "src/components/strategy/strategy-dialog.tsx"
      via: "onStrategyExtracted callback updates extractedStrategy state"
      pattern: "onStrategyExtracted"
    - from: "src/components/strategy/strategy-dialog.tsx"
      to: "src/components/strategy/strategy-preview-panel.tsx"
      via: "passes extractedStrategy as prop"
      pattern: "StrategyPreviewPanel"
    - from: "src/server/providers/ai/deepseek-provider.ts"
      to: "src/components/strategy/ai-chat.tsx"
      via: "chatAboutStrategy returns AiChatResponse with optional strategy"
      pattern: "chatAboutStrategy"
---

<objective>
Transform AI strategy creation from rigid 4-step quiz to free-form conversation mode with a live strategy preview panel that updates as AI extracts parameters.

Purpose: Let users describe trading ideas naturally (AIREV-01) and see what AI understands in real time (AIREV-02), dramatically reducing friction in strategy creation.
Output: Updated DeepSeek provider with free-form prompts + new indicators in tool schema, refactored AiChat component, new StrategyPreviewPanel component, updated StrategyDialog.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-revolution/04-RESEARCH.md

<interfaces>
<!-- Current code the executor must modify -->

From src/server/providers/ai/deepseek-provider.ts (line 6):
```typescript
const VALID_INDICATORS: IndicatorType[] = ["SMA", "EMA", "RSI", "MACD", "BOLLINGER", "PRICE", "VOLUME", "PRICE_CHANGE", "SUPPORT", "RESISTANCE"]
```

From src/server/providers/ai/deepseek-provider.ts (lines 173-188) — current CHAT_SYSTEM_PROMPT:
```typescript
const CHAT_SYSTEM_PROMPT = `Ты — AI-помощник AculaTrade для создания торговых стратегий.
... (enforces 4-step sequential quiz order)
4. Когда собрал достаточно информации — вызови функцию create_strategy
...
- Если пользователь сразу описал конкретную стратегию с деталями — сразу генерируй через create_strategy`
```

From src/components/strategy/ai-chat.tsx:
```typescript
type AiChatProps = {
  onGenerated: (strategy: AiGeneratedStrategy) => void
}
const INITIAL_MESSAGE: ChatMessage = {
  role: "assistant",
  content: "..." // current quiz-style initial message
}
export const AiChat = ({ onGenerated }: AiChatProps) => { ... }
```

From src/components/strategy/strategy-dialog.tsx:
```typescript
type StrategyDialogProps = {
  // current props — no initialContext
}
export const StrategyDialog = ({ ... }: StrategyDialogProps) => {
  // renders <AiChat onGenerated={handleGenerated} />
}
```

From src/core/types/strategy.ts:
```typescript
export type AiGeneratedStrategy = {
  name: string
  instrument: string
  instrumentType: "STOCK" | "BOND" | "CURRENCY" | "FUTURES"
  timeframe: string
  description: string
  config: StrategyConfig
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DeepSeek provider — free-form system prompt + new indicators in tool schema</name>
  <files>
    src/server/providers/ai/deepseek-provider.ts
  </files>
  <read_first>
    src/server/providers/ai/deepseek-provider.ts,
    src/core/types/strategy.ts,
    src/server/providers/ai/types.ts
  </read_first>
  <action>
    **1. Update VALID_INDICATORS** array (line 6) to include 4 new types:
    ```typescript
    const VALID_INDICATORS: IndicatorType[] = [
      "SMA", "EMA", "RSI", "MACD", "BOLLINGER", "PRICE", "VOLUME", "PRICE_CHANGE", "SUPPORT", "RESISTANCE",
      "ATR", "STOCHASTIC", "VWAP", "WILLIAMS_R",
    ]
    ```

    **2. Replace CHAT_SYSTEM_PROMPT** with free-form version. Remove the rigid 4-step question order. New prompt:
    ```typescript
    const CHAT_SYSTEM_PROMPT = `Ты — AI-помощник AculaTrade. Помогай пользователю создать торговую стратегию через свободный диалог.

    Доступные индикаторы: SMA, EMA, RSI, MACD, Bollinger Bands, ATR, Stochastic, VWAP, Williams %R, а также цена, объём, уровни поддержки/сопротивления.
    Доступные условия: CROSSES_ABOVE, CROSSES_BELOW, GREATER_THAN, LESS_THAN, EQUALS, BETWEEN, ABOVE_BY_PERCENT, BELOW_BY_PERCENT, MULTIPLIED_BY.
    Таймфреймы: 1min, 5min, 15min, 1hour, 4hour, 1day, 1week.

    Правила:
    - Когда у тебя достаточно информации (инструмент + хотя бы один индикатор или стиль торговли), вызови create_strategy
    - Не задавай все вопросы по порядку — реагируй на то, что написал пользователь
    - Если пользователь сразу описал идею с деталями — сразу генерируй через create_strategy
    - Если описание расплывчатое — уточни ОДНО самое важное (не все сразу)
    - Всегда указывай и entry, и exit условия в стратегии
    - Используй новые индикаторы (ATR, Stochastic, VWAP, Williams %R) когда они подходят по контексту
    - Для BETWEEN условий используй value (нижняя граница) и valueTo (верхняя граница)
    - Отвечай на русском языке`
    ```

    **3. Update GENERATION_SYSTEM_PROMPT** (the one used in `generateStrategy`) similarly — add mention of new indicators and BETWEEN condition with valueTo.

    **4. Update generateStrategyTool schema** — the `create_strategy` function tool definition. In the condition items schema, add `valueTo` as optional number property:
    ```typescript
    // Inside the condition item properties (both entry and exit):
    valueTo: {
      type: "number" as const,
      description: "Upper bound for BETWEEN condition",
    },
    ```
    Make sure the `enum` for indicator in the tool schema uses `VALID_INDICATORS` (it already does at line 65/92 — just confirm it picks up the new values since it references the array).

    **5. Keep tool_choice behavior unchanged:**
    - `generateStrategy`: `tool_choice: { type: "function", function: { name: "create_strategy" } }` (forced)
    - `chatAboutStrategy`: no forced tool_choice (auto) — this is correct for free-form mode per RESEARCH pitfall 4

    **6. In the strategy validation section** (around line 304), ensure the validation of parsed strategy conditions allows `valueTo` to pass through (it's already in the JSON, just make sure it's not stripped).
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/server/providers/ai/deepseek-provider.ts VALID_INDICATORS array contains "ATR", "STOCHASTIC", "VWAP", "WILLIAMS_R"
    - CHAT_SYSTEM_PROMPT contains "свободный диалог" (not "4 шага" or sequential quiz text)
    - CHAT_SYSTEM_PROMPT contains "ATR, Stochastic, VWAP, Williams %R"
    - CHAT_SYSTEM_PROMPT contains "BETWEEN" and "valueTo"
    - generateStrategyTool schema contains `valueTo` property in condition items
    - chatAboutStrategy does NOT have forced tool_choice (remains auto)
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>DeepSeek provider uses free-form system prompt, all 14 indicators in tool schema, BETWEEN/valueTo in tool definition. Compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor AiChat to free-form mode + create StrategyPreviewPanel + update StrategyDialog</name>
  <files>
    src/components/strategy/ai-chat.tsx,
    src/components/strategy/strategy-preview-panel.tsx,
    src/components/strategy/strategy-dialog.tsx,
    src/components/strategy/index.ts
  </files>
  <read_first>
    src/components/strategy/ai-chat.tsx,
    src/components/strategy/strategy-dialog.tsx,
    src/components/strategy/strategy-form.tsx,
    src/components/strategy/index.ts,
    src/core/types/strategy.ts,
    src/server/actions/strategy-actions.ts
  </read_first>
  <action>
    **1. src/components/strategy/ai-chat.tsx** — Refactor for free-form mode:

    **a) Update props type:**
    ```typescript
    type AiChatProps = {
      onGenerated: (strategy: AiGeneratedStrategy) => void
      onStrategyExtracted?: (strategy: AiGeneratedStrategy) => void
      initialContext?: string
    }
    ```

    **b) Update INITIAL_MESSAGE** — replace quiz-style message with a friendly free-form greeting:
    ```typescript
    const INITIAL_MESSAGE: ChatMessage = {
      role: "assistant",
      content: "Привет! Опишите свою торговую идею — какой инструмент, какая логика входа и выхода. Я помогу собрать из неё стратегию. Можете описать подробно или начать с общей идеи — я уточню детали.",
    }
    ```

    **c) Handle initialContext** — if `initialContext` prop is provided, prepend it as the first assistant message before INITIAL_MESSAGE:
    ```typescript
    const getInitialMessages = (): ChatMessage[] => {
      if (initialContext) {
        return [
          { role: "assistant", content: `Анализ из терминала:\n\n${initialContext}` },
          { role: "assistant", content: "На основе этого анализа я могу создать стратегию. Опишите, какую стратегию хотите — или я предложу варианты на основе анализа." },
        ]
      }
      return [INITIAL_MESSAGE]
    }
    ```
    Use `getInitialMessages()` in useState initializer instead of `[INITIAL_MESSAGE]`.

    **d) Call onStrategyExtracted** — in the chat response handler, when `response.strategy` exists (AI returned a strategy via tool call), call `onStrategyExtracted?.(response.strategy)` BEFORE waiting for user to click "Apply". The existing `onGenerated` stays as-is for the "Apply" button click. This gives the preview panel real-time updates.

    **2. src/components/strategy/strategy-preview-panel.tsx** — NEW file. Create a collapsible panel showing extracted strategy parameters:
    ```typescript
    import type { AiGeneratedStrategy } from "@/core/types"
    import { ChevronDown, ChevronUp } from "lucide-react"
    import { useState } from "react"

    type StrategyPreviewPanelProps = {
      strategy: AiGeneratedStrategy
    }

    export const StrategyPreviewPanel = ({ strategy }: StrategyPreviewPanelProps) => {
      const [expanded, setExpanded] = useState(true)
      // Render:
      // - Header: "Превью стратегии" with collapse toggle (ChevronDown/ChevronUp)
      // - strategy.name (bold)
      // - strategy.instrument + strategy.timeframe badges
      // - strategy.description (text-sm text-muted-foreground)
      // - Entry conditions: map strategy.config.entry, show indicator + condition + value for each
      // - Exit conditions: map strategy.config.exit similarly
      // - Risks: stopLoss, takeProfit if set
      // - Use Tailwind: border rounded-lg p-4 bg-muted/50, badges for instrument/timeframe
      // - Condition display format: "RSI (14) > 70" or "Stochastic BETWEEN 20-80"
    }
    ```
    Use `getIndicatorConfig` from `@/core/config/indicators` to get labels. For BETWEEN, display: `{indicator} BETWEEN {value}–{valueTo}`.

    **3. src/components/strategy/strategy-dialog.tsx** — Update to integrate preview:

    **a) Add props:**
    ```typescript
    type StrategyDialogProps = {
      open: boolean
      onOpenChange: (open: boolean) => void
      onSuccess: () => void
      initialContext?: string
      initialInstrument?: string
    }
    ```

    **b) Add state for extracted strategy:**
    ```typescript
    const [extractedStrategy, setExtractedStrategy] = useState<AiGeneratedStrategy | null>(null)
    ```
    Reset to null when dialog opens (in useEffect on `open`).

    **c) Pass new props to AiChat:**
    ```typescript
    <AiChat
      onGenerated={handleGenerated}
      onStrategyExtracted={setExtractedStrategy}
      initialContext={initialContext}
    />
    ```

    **d) Render StrategyPreviewPanel** below the AiChat, conditionally:
    ```typescript
    {extractedStrategy && <StrategyPreviewPanel strategy={extractedStrategy} />}
    ```

    **e) If initialInstrument provided**, pass it to the strategy form's default instrument field (look at how the form is used after "Apply" — ensure the instrument is pre-filled).

    **4. src/components/strategy/index.ts** — Add export for StrategyPreviewPanel:
    ```typescript
    export { StrategyPreviewPanel } from "./strategy-preview-panel"
    ```
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/strategy/ai-chat.tsx AiChatProps contains `onStrategyExtracted?: (strategy: AiGeneratedStrategy) => void`
    - src/components/strategy/ai-chat.tsx AiChatProps contains `initialContext?: string`
    - src/components/strategy/ai-chat.tsx INITIAL_MESSAGE content contains "Опишите свою торговую идею" (not quiz steps)
    - src/components/strategy/ai-chat.tsx calls `onStrategyExtracted` when response.strategy exists
    - src/components/strategy/strategy-preview-panel.tsx exists and exports `StrategyPreviewPanel`
    - src/components/strategy/strategy-preview-panel.tsx renders strategy name, instrument, conditions
    - src/components/strategy/strategy-dialog.tsx accepts `initialContext?: string` and `initialInstrument?: string` props
    - src/components/strategy/strategy-dialog.tsx renders `<StrategyPreviewPanel>` when extractedStrategy is not null
    - src/components/strategy/index.ts exports StrategyPreviewPanel
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>AI chat uses free-form conversation mode. Live strategy preview panel renders below chat and updates on each AI response containing a strategy. StrategyDialog accepts initialContext for terminal seeding (used by Plan 03).</done>
</task>

</tasks>

<verification>
```bash
cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx tsc --noEmit --pretty && npm run test -- --run
```
TypeScript compiles cleanly. All existing tests pass.
</verification>

<success_criteria>
- AI chat greets user with free-form message instead of quiz steps
- AI system prompt mentions all 14 indicators including ATR, Stochastic, VWAP, Williams %R
- AI tool schema includes valueTo for BETWEEN conditions
- StrategyPreviewPanel shows strategy parameters updating live during conversation
- StrategyDialog accepts initialContext prop for terminal context seeding
- All code compiles, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-revolution/04-02-SUMMARY.md`
</output>
