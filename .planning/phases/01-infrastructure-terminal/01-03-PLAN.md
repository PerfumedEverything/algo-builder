---
phase: 01-infrastructure-terminal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/portfolio/ai-analysis-button.tsx
  - src/core/config/ai-prompts.ts
  - src/server/actions/ai-analysis-actions.ts
  - src/components/broker/lot-analysis-dialog.tsx
  - src/components/broker/portfolio-view.tsx
  - src/__tests__/ai-analysis-button.test.ts
autonomous: true
requirements:
  - AIAN-01
  - AIAN-02

must_haves:
  truths:
    - "User can click an AI analysis button on any analytics block and see a dialog with streaming DeepSeek response"
    - "Different analytics blocks (chart, risk, fundamental, optimization, portfolio, lot) each use their own specialized system prompt"
    - "Existing lot analysis dialog on portfolio page continues to work after refactor"
  artifacts:
    - path: "src/components/portfolio/ai-analysis-button.tsx"
      provides: "Reusable AI analysis button+dialog component"
      exports: ["AiAnalysisButton"]
    - path: "src/core/config/ai-prompts.ts"
      provides: "System prompts per analytics block"
      exports: ["AI_PROMPTS"]
    - path: "src/server/actions/ai-analysis-actions.ts"
      provides: "Generic AI analysis server action"
      exports: ["analyzeWithAiAction"]
    - path: "src/__tests__/ai-analysis-button.test.ts"
      provides: "Unit tests for AI prompts config and action"
  key_links:
    - from: "src/components/portfolio/ai-analysis-button.tsx"
      to: "src/server/actions/ai-analysis-actions.ts"
      via: "Server action call"
      pattern: "analyzeWithAiAction"
    - from: "src/server/actions/ai-analysis-actions.ts"
      to: "src/core/config/ai-prompts.ts"
      via: "Prompt lookup"
      pattern: "AI_PROMPTS"
---

<objective>
Extract reusable AiAnalysisButton from lot-analysis-dialog.tsx and create system prompt configuration for all analytics blocks.

Purpose: Every analytics block in phases 1-5 needs an "AI Analysis" button. Extracting it now avoids duplication and ensures consistent UX. System prompts config enables different analytical contexts.
Output: Reusable AiAnalysisButton component, AI prompts config, generic server action, unit tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-terminal/01-CONTEXT.md

<interfaces>
From src/components/broker/lot-analysis-dialog.tsx:
```typescript
type LotAnalysisDialogProps = {
  ticker: string
  operations: PositionOperation[]
  currentPrice: number
}
// Pattern: Button -> Dialog -> handleAnalyze() -> analyzeLotAction() -> setResult -> Markdown render
```

From src/server/actions/portfolio-actions.ts:
```typescript
const ANALYSIS_PROMPT = `Ты профессиональный бухгалтер...`
export const analyzeLotAction = async (ticker, operations, currentPrice): Promise<ApiResponse<string>> => {
  // getCurrentUserId(), OpenAI client with DEEPSEEK_API_KEY + DEEPSEEK_BASE_URL
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI prompts config, generic server action, and unit tests</name>
  <files>
    src/core/config/ai-prompts.ts,
    src/server/actions/ai-analysis-actions.ts,
    src/__tests__/ai-analysis-button.test.ts
  </files>
  <read_first>
    src/server/actions/portfolio-actions.ts,
    src/core/config/env.ts,
    src/components/broker/lot-analysis-dialog.tsx
  </read_first>
  <action>
    Per D-08 and AIAN-02: Different system prompts per analytics block.

    1. Create `src/core/config/ai-prompts.ts`:
       - Export `type AiAnalysisBlock = "lot" | "chart" | "risk" | "fundamental" | "optimization" | "portfolio"`
       - Export `AI_PROMPTS: Record<AiAnalysisBlock, string>` with:
         - `lot`: Move existing ANALYSIS_PROMPT from portfolio-actions.ts (the accountant/FIFO prompt)
         - `chart`: "Ты профессиональный технический аналитик фондового рынка. Проанализируй свечные данные OHLCV инструмента как профессиональный трейдер. Определи: 1. Текущий тренд (восходящий/нисходящий/боковой) 2. Ключевые уровни поддержки и сопротивления 3. Паттерны свечей (если есть) 4. Объёмный профиль 5. Рекомендация (покупать/продавать/ждать) с обоснованием. Формат: структурированный markdown на русском языке."
         - `risk`: placeholder "Ты профессиональный риск-менеджер..." (Phase 2)
         - `fundamental`: placeholder "Ты фундаментальный аналитик..." (Phase 3)
         - `optimization`: placeholder "Ты портфельный управляющий..." (Phase 5)
         - `portfolio`: placeholder "Ты инвестиционный советник..." (Phase 5)

    2. Create `src/server/actions/ai-analysis-actions.ts`:
       - `"use server"` directive
       - `export const analyzeWithAiAction = async (block: AiAnalysisBlock, userMessage: string): Promise<ApiResponse<string>>`
       - Auth: `await getCurrentUserId()`
       - Get DEEPSEEK_API_KEY and DEEPSEEK_BASE_URL from `getEnv()`
       - If no API key, return `{ success: false, error: "AI провайдер не настроен" }`
       - Create OpenAI client with DeepSeek config (same as portfolio-actions.ts)
       - Lookup system prompt: `AI_PROMPTS[block]`
       - Call `client.chat.completions.create({ model: "deepseek-chat", messages: [{ role: "system", content: systemPrompt }, { role: "user", content: userMessage }] })`
       - Return `{ success: true, data: response.choices[0].message.content }`
       - try/catch -> AppError handling

    3. Create `src/__tests__/ai-analysis-button.test.ts`:
       - Test: `AI_PROMPTS` has exactly 6 keys: "lot", "chart", "risk", "fundamental", "optimization", "portfolio"
       - Test: Each prompt in `AI_PROMPTS` is a non-empty string
       - Test: `AiAnalysisBlock` type covers all 6 block types (verify via AI_PROMPTS keys)
       - Test: Mock `analyzeWithAiAction` call verifies it looks up the correct prompt from `AI_PROMPTS[block]` (mock OpenAI client and verify system message content matches AI_PROMPTS["chart"] when block="chart")
       - Test: `analyzeWithAiAction` returns `{ success: false, error }` when API key is missing
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && npx vitest run src/__tests__/ai-analysis-button.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/core/config/ai-prompts.ts contains `export type AiAnalysisBlock`
    - src/core/config/ai-prompts.ts contains `export const AI_PROMPTS`
    - src/core/config/ai-prompts.ts has entries for "lot", "chart", "risk", "fundamental", "optimization", "portfolio"
    - src/server/actions/ai-analysis-actions.ts contains `"use server"`
    - src/server/actions/ai-analysis-actions.ts contains `export const analyzeWithAiAction`
    - src/server/actions/ai-analysis-actions.ts contains `AI_PROMPTS[block]`
    - src/server/actions/ai-analysis-actions.ts contains `getCurrentUserId`
    - src/__tests__/ai-analysis-button.test.ts exists and passes
  </acceptance_criteria>
  <done>AI prompts config exists with 6 block types, generic server action accepts block type and user message, unit tests verify prompt config and action behavior</done>
</task>

<task type="auto">
  <name>Task 2: Extract reusable AiAnalysisButton and refactor lot-analysis-dialog</name>
  <files>
    src/components/portfolio/ai-analysis-button.tsx,
    src/components/broker/lot-analysis-dialog.tsx,
    src/components/broker/portfolio-view.tsx
  </files>
  <read_first>
    src/components/broker/lot-analysis-dialog.tsx,
    src/components/broker/portfolio-view.tsx,
    src/server/actions/ai-analysis-actions.ts
  </read_first>
  <action>
    Per D-08 and AIAN-01: Reuse lot-analysis-dialog.tsx pattern for generic component.

    1. Create `src/components/portfolio/ai-analysis-button.tsx`:
       - `"use client"` directive
       - `type AiAnalysisButtonProps = { title: string; triggerLabel?: string; triggerIcon?: React.ReactNode; analyzeAction: () => Promise<{ success: boolean; data?: string; error?: string }>; variant?: "default" | "outline" | "ghost"; size?: "default" | "sm" | "icon" }`
       - Default triggerLabel: "Анализ с ИИ"
       - Default triggerIcon: `<Bot className="h-4 w-4" />`
       - Component structure (extracted from lot-analysis-dialog.tsx):
         - `const [open, setOpen] = useState(false)`
         - `const [loading, setLoading] = useState(false)`
         - `const [result, setResult] = useState<string | null>(null)`
         - `const [error, setError] = useState<string | null>(null)`
         - `handleAnalyze`: calls `analyzeAction()`, sets result/error
         - `handleOpen`: on dialog open, auto-triggers analysis if no result
         - Render: `<Dialog>` with `<DialogTrigger asChild>` wrapping `<Button>`, `<DialogContent>` with:
           - `<DialogHeader><DialogTitle>{title}</DialogTitle></DialogHeader>`
           - Loading state: `<Loader2 className="h-6 w-6 animate-spin" />`
           - Error state: red text with error message
           - Result state: `<Markdown>{result}</Markdown>` (react-markdown)
           - DialogContent className: `"max-w-2xl max-h-[80vh] overflow-y-auto"`
       - Import Dialog, Button from shadcn/ui; Bot, Loader2 from lucide-react; Markdown from react-markdown
       - Named export: `export const AiAnalysisButton`

    2. Refactor `src/components/broker/lot-analysis-dialog.tsx`:
       - Replace internal dialog/button/loading/markdown logic with `<AiAnalysisButton>`
       - Keep the same props (`ticker`, `operations`, `currentPrice`)
       - Create a wrapper that builds the `analyzeAction` callback:
         ```
         const analyzeAction = () => analyzeLotAction(ticker, operations, currentPrice)
         return <AiAnalysisButton title={`Анализ лотов ${ticker}`} analyzeAction={analyzeAction} triggerLabel="Анализ лотов" triggerIcon={<Layers className="h-4 w-4" />} size="sm" variant="outline" />
         ```
       - This preserves backward compatibility: portfolio-view.tsx still uses `<LotAnalysisDialog>` with same props

    3. Verify portfolio-view.tsx still renders LotAnalysisDialog correctly (no changes needed if lot-analysis-dialog.tsx keeps same export name and props).
  </action>
  <verify>
    <automated>cd /Users/daratimofeeva/Desktop/Projects/web/algo-builder && grep -c "export const AiAnalysisButton" src/components/portfolio/ai-analysis-button.tsx && grep -c "AiAnalysisButton" src/components/broker/lot-analysis-dialog.tsx && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/portfolio/ai-analysis-button.tsx contains `export const AiAnalysisButton`
    - src/components/portfolio/ai-analysis-button.tsx contains `"use client"`
    - src/components/portfolio/ai-analysis-button.tsx contains `Dialog` and `Markdown`
    - src/components/portfolio/ai-analysis-button.tsx contains `analyzeAction` prop
    - src/components/broker/lot-analysis-dialog.tsx imports `AiAnalysisButton`
    - src/components/broker/lot-analysis-dialog.tsx still exports `LotAnalysisDialog`
    - TypeScript compilation has no errors related to these files
  </acceptance_criteria>
  <done>AiAnalysisButton is reusable for all analytics blocks, lot-analysis-dialog uses it internally, no regression in portfolio page</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npx vitest run src/__tests__/ai-analysis-button.test.ts` passes
- AiAnalysisButton exported from portfolio module
- LotAnalysisDialog still works (uses AiAnalysisButton internally)
- AI_PROMPTS has 6 block entries
- analyzeWithAiAction accepts block type
</verification>

<success_criteria>
- AiAnalysisButton is a standalone reusable component accepting any analyzeAction callback
- System prompts configured for all 6 analytics blocks (chart prompt complete, others placeholder)
- Existing lot-analysis-dialog refactored to use AiAnalysisButton without breaking portfolio page
- Unit tests verify prompt config integrity and action behavior
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-terminal/01-03-SUMMARY.md`
</output>
